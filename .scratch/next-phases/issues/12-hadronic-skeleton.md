# notebooks/hadronic_eos — skeleton, knobs, modes and parametrisation

Type: task
Status: open
Blocked by: 03, 04, 26
Parent: ../map.md

## Question

Stage 1, first half. Author `notebooks/hadronic_eos.py` in jupytext `py:percent`
format and pair it:

    jupytext --set-formats ipynb,py:percent notebooks/hadronic_eos.py
    jupytext --sync notebooks/hadronic_eos.py

The `.py` is the source; the `.ipynb` is regenerated by `--sync`. The notebook
calls library functions and carries its own plotting code — no `notebook_api`
module, no helper module beside it (§11).

Models: `zl`, `sfho`, `dd2`, `did`. Copy the knobs cell, the
unsupported-combination pattern and the table-naming convention from ticket 04
rather than reinventing them.

This ticket delivers: the knobs cell; a section per mode (§3) exercising
`eos_point` and `eos_table` across the selected models; and the parametrisation
section — published sets, plus NMP inversion for the models carrying `nmp.py`
with `Q_sat`/`K_sym` reported as predictions. `zl` has no `nmp.py`; **state that
in the notebook rather than faking it.**

Cross-check every gap the notebook reports against `docs/DEFERRED.md` and record
in the answer any gap that is real but unrecorded.

Done when `jupytext --to notebook --execute` runs the notebook to completion on a
small grid with no traceback.
