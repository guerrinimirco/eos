"""Accelerated re-expressions of the mixed solve (CLAUDE.md section 9).

Deletable: remove this directory and the engine still gives the same numbers,
only slower -- the reference path is the numeric Jacobian in `solver`.
"""
