\documentclass[aps,prc,onecolumn,superscriptaddress,nofootinbib,10pt]{revtex4-2}
\usepackage{amsmath,amssymb}
\usepackage{slashed}   % \slashed\partial in the Lagrangian
\usepackage{booktabs}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\newcommand{\nB}{n_B}
\newcommand{\nC}{n_C}
\newcommand{\nS}{n_S}
\newcommand{\nq}{n_q}
\newcommand{\muB}{\mu_B}
\newcommand{\muC}{\mu_C}
\newcommand{\muS}{\mu_S}
\newcommand{\munue}{\mu_{\nu_e}}
\newcommand{\Lam}{\Lambda}
\newcommand{\GS}{G_S}
\newcommand{\GD}{G_D}
\newcommand{\GV}{G_V}
\newcommand{\trace}{\mathrm{tr}}

\begin{document}

\title{Three-flavour NJL with colour superconductivity \\
       --- as implemented in \texttt{eos/njl}}

\author{M.~Guerrini}
\affiliation{University of Ferrara}

\begin{abstract}
The \texttt{eos/njl} subpackage implements the three-flavour
Nambu--Jona-Lasinio model of dense quark matter with a diquark condensate: the
scalar channel that breaks chiral symmetry, the 't~Hooft determinant that ties
the flavours together, the colour-antitriplet diquark channel that condenses
into a colour superconductor, and a vector channel whose repulsion controls the
high-density stiffness. Almost nothing in it is an input: the constituent
masses come out of a gap equation, the effective bag constant is a derived
vacuum pressure difference, and \emph{which} colour-superconducting pattern the
matter is in is an outcome chosen by free energy rather than a declaration.
This note states the Lagrangian, every parameter with its value and the vacuum
observable it is fitted to, the cut medium integrals at both temperatures and
the surface term that distinguishes the two pressure forms, the Dirac sea in
closed form, the gap equations for masses and for gaps, the
Dirac-basis quasiparticle spectrum and the Hellmann--Feynman kernels it
supplies, the density-dependent vector coupling and the rearrangement
self-energy it owes, the lepton and thermal gases that join the totals, the
residual of every equilibrium mode as the solver assembles it, every quantity a
solved point returns, and the phase-adapter surface the hybrid engine
consumes. It is written from the code, as its specification:
every equation here is asserted either by
\texttt{eos/njl/verify/run\_full\_check.py}, by \texttt{test/njl}, or --- for
the shared pairing machinery --- by \texttt{test/general/test\_pairing.py}.
\end{abstract}

\maketitle

\section{Orientation: reading this model from a bag model}
\label{sec:orientation}

Readers who know the bag-model quark phases of this repository
(\texttt{eos/vmit}, \texttt{eos/alphabag}) will find every equation below
familiar in form and different in status. A bag model \emph{declares} what NJL
\emph{derives}, and that single fact accounts for the whole difference in
structure and in cost.

\begin{center}
\footnotesize
\begin{tabular}{p{0.20\textwidth}p{0.34\textwidth}p{0.38\textwidth}}
\toprule
quantity & \texttt{vmit} / \texttt{alphabag} & \texttt{njl} \\
\midrule
quark masses & parameters $m_u,m_d,m_s$, constant &
  $M_u,M_d,M_s$ \emph{solved} from a gap equation at every point, falling from
  $\simeq 368$ MeV to $\simeq m_f$ across chiral restoration \\
bag constant & an input $B$ &
  \emph{derived}: a vacuum pressure difference, reported, never set \\
pairing gap & \texttt{alphabag}: an input $\Delta_0$ with a BCS $\Delta(T)$
  imposed & $\Delta_1,\Delta_2,\Delta_3$ \emph{solved} from three gap
  equations \\
pairing pattern & \texttt{alphabag}: \texttt{cfl} is a \emph{mode} the caller
  picks & an \emph{outcome}: every candidate is solved and the free energy
  picks the winner \\
colour neutrality & absent (CFL is neutral by construction) &
  two extra unknowns $\mu_3,\mu_8$ and two extra rows $n_3 = n_8 = 0$ \\
momentum integrals & to infinity & \emph{cut} at $\Lam$ (vacuum) and
  $\Lam_{\rm UV}$ (medium) \\
Dirac sea & absent, renormalised into $B$ &
  explicit, in closed form, subtracted at the vacuum \\
vector repulsion & \texttt{vmit}: $V = a\hbar c \sum_q n_q$, one constant $a$ &
  $\Sigma_V = dW/d\nq$, with $\GV$ optionally a function of the density \\
unknowns per point & 2--4 & 5 (unpaired, no vector) to 11 (CFL, trapped,
  with vector) \\
\bottomrule
\end{tabular}
\end{center}

Two consequences are worth internalising before reading further.

\emph{There is no ``quark gas plus a correction''.} In \texttt{alphabag} the
CFL phase is the unpaired gas plus an analytic $\Delta^2\mu^2$ term. Here the
paired phase is a \emph{different spectrum}: the gap mixes particles with
holes, the quasiparticle energies come out of a matrix diagonalisation at every
momentum, and the pairing piece of $\Omega$ is the difference between that
spectrum and the unpaired one (Sec.~\ref{sec:pairing}). The \texttt{alphabag}
form is a limit of it, and Sec.~\ref{sec:bagcompare} says in what sense.

\emph{A ``bag constant'' in the NJL literature is one of three different
numbers}, differing by well-defined terms; comparing them blindly is a
20--25\% error. Sec.~\ref{sec:bagconstants} gives all three, with the published
values each reproduces.

\section{Conventions}
\label{sec:conventions}

Units are natural, $\hbar = c = k_B = 1$, with MeV throughout: momenta, masses
and chemical potentials in MeV, densities in MeV$^3$, and $\Omega$, $P$ and
$\varepsilon$ in MeV$^4$. Every public boundary of the package converts to
fm-based units, densities in fm$^{-3}$ and $P$, $\varepsilon$ in MeV\,fm$^{-3}$,
through $(\hbar c)^3 = (197.3269804\ \mathrm{MeV\,fm})^3$.

\paragraph{Modes.} There are nine colour--flavour modes $j = (f,a)$ with
$f \in \{u,d,s\}$ and $a \in \{r,g,b\}$, indexed \emph{flavour-major},
$j = 3 i_f + i_a$. The spin degeneracy of one mode is $g = 2$; the Dirac sea of
one flavour carries $g_{\rm sea} = 2N_c = 6$, since the vacuum is not resolved
by colour.

\paragraph{Charges.} $q_u = +\tfrac23$, $q_d = q_s = -\tfrac13$, and
\emph{strangeness is $s_s = +1$ per $s$ quark}, the opposite of the PDG sign
and used consistently throughout this repository. $C$ is the charge of
strongly-interacting matter only; the leptons are excluded from it and enter
through the separate condition of total electric neutrality.

\paragraph{Colour generators.} $(T_3)_{r,g,b} = (+\tfrac12,-\tfrac12,0)$ and
\begin{equation}
  (T_8)_{r,g,b} = \left(\tfrac13,\ \tfrac13,\ -\tfrac23\right)
  = \frac{\lambda_8}{\sqrt3} .
\end{equation}
\emph{Three} normalisations of $T_8$ are in circulation and mixing them
corrupts $\mu_8$ by factors of $1.15$ to $1.7$. R\"uster \textit{et
al.}~\cite{Ruester2005}, Pagliara and Schaffner-Bielich~\cite{Pagliara2008} and
Kunkel \textit{et al.}~\cite{Kunkel2026} use the halved Gell-Mann
$\sqrt3\,T_8 = \mathrm{diag}(\tfrac12,\tfrac12,-1)$, for which
$\mu_8^{\rm theirs} = (2/\sqrt3)\,\mu_8^{\rm ours} = 1.1547\,\mu_8^{\rm ours}$;
Buballa~\cite{Buballa2005} and Steiner, Reddy and Prakash~\cite{Steiner2002}
use the full $\lambda_8$, for which $\mu_8^{\rm ours} =
\sqrt3\,\mu_8^{\rm theirs}$. As a worked consequence, the CFL result
$\mu_8 = -\tfrac1{2\sqrt3} m_s^2/\mu$ of Ref.~\cite{Steiner2002} reads
$\mu_8 = -\tfrac12 m_s^2/\mu$ here: at $m_s = 300$, $\mu = 450$ MeV that is
$-57.7$ MeV in their convention and $-100.0$ MeV in ours.

\paragraph{Mode potentials.} With all five conserved-charge potentials,
\begin{equation}
  \mu_{(f,a)} = \frac{\muB}{3} + q_f \muC + s_f \muS
              + (T_3)_a\,\mu_3 + (T_8)_a\,\mu_8 ,
  \qquad \mu_e = -\muC ,
  \label{eq:modemu}
\end{equation}
the last of which is beta equilibrium in the sign convention
$\muC = \mu_p - \mu_n$ used across this repository.

\paragraph{Two naming traps in the sources.} The $G_D$ of
Ref.~\cite{Steiner2002} is the 't~Hooft coupling, not the diquark one (theirs
is $G_{\rm DIQ}$); and $\Delta_2$ in the R\"uster/Buballa index convention
means the \emph{ud} gap, not the second largest.

\section{The Lagrangian and what is kept}
\label{sec:lagrangian}

\begin{align}
  \mathcal{L} =\ & \bar q\,(i\slashed\partial - \hat m)\,q
   + \GS \sum_{a=0}^{8}\Big[ (\bar q\,\tau_a q)^2
                           + (\bar q\, i\gamma_5 \tau_a q)^2 \Big]
   \nonumber\\
   & - K \Big\{ \det\nolimits_f \big[\bar q (1+\gamma_5) q\big]
              + \det\nolimits_f \big[\bar q (1-\gamma_5) q\big] \Big\}
   \nonumber\\
   & + \GD \sum_{\eta}
        \big(\bar q\, i\gamma_5\, \epsilon_\eta \lambda^A_\eta\, C\, \bar q^{\,t}\big)
        \big(q^{t}\, C\, i\gamma_5\, \epsilon_\eta \lambda^A_\eta\, q\big)
   - \GV\,(\bar q \gamma^\mu q)^2 .
  \label{eq:lagrangian}
\end{align}
The scalar, vector and diquark mean fields all arise from \emph{one}
Hubbard--Stratonovich step: none is more fundamental than the others, and the
gap equation $\partial\Omega/\partial\Delta_\eta = 0$ and the mass equation are
the same kind of statement. Two places where the analogy nevertheless breaks
matter for the implementation. First, the condensation-energy normalisations
differ by a factor two --- the scalar cost is
$\sum_f (M_f-m_f)^2/(8\GS)$ and the pairing cost
$\sum_\eta \Delta_\eta^2/(4\GD)$ --- so $\eta_D = \GD/\GS = 1$ does
\emph{not} mean ``equally strong channels''. Second, $\sigma$ enters the
quasiparticle diagonal while $\Delta$ is strictly off-diagonal: the gap matrix
has identically zero diagonal, mixes particles with holes, and therefore needs
the doubled Dirac basis of Sec.~\ref{sec:bdg}. That is also why the gap kernel
carries branch signs where the mass equation does not.

\paragraph{Mean-field masses.} Including the determinant cross-terms, which are
the coefficients most often dropped or mis-signed,
\begin{equation}
  M_u = m_u - 4\GS \phi_u + 2K\,\phi_d \phi_s , \qquad
  M_d = m_d - 4\GS \phi_d + 2K\,\phi_u \phi_s , \qquad
  M_s = m_s - 4\GS \phi_s + 2K\,\phi_u \phi_d ,
  \label{eq:massgap}
\end{equation}
with $\phi_f = \langle \bar q_f q_f\rangle$, negative in the broken phase.

\paragraph{What is omitted.} The 't~Hooft term expanded in the presence of
diquark condensates generates a cross-term $\propto \sum_\alpha \sigma_\alpha
|\Delta_\alpha|^2$. Most treatments omit it; Baym \textit{et
al.}~\cite{Baym2018} include it with a coupling $K' \simeq K$. It is
\emph{not} implemented here, and consequently $\eta_D$ must be read as an
effective coupling that has absorbed it. Nothing here invents a coefficient by
analogy with the mass cross-terms.

\section{The parameters}
\label{sec:parameters}

Every function in this package takes the parameter set as its first argument
and none of them reaches for a default on the caller's behalf: the set is one
frozen record, hashable and safely shared between processes. It has three
tiers, and the tiers are not decoration --- they say which numbers an inference
run is allowed to move.

\subsection{Tier 1: the vacuum fit, never sampled}

The set of Rehberg, Klevansky and H\"ufner~\cite{Rehberg1996}, fitted to the
four vacuum observables $m_\pi = 135.0$, $f_\pi = 92.4$, $m_K = 497.7$ and
$m_{\eta'} = 957.8$ MeV.

\begin{center}
\begin{tabular}{lllc}
\toprule
symbol & value & derived & fixed by \\
\midrule
$\Lam$          & $602.3$ MeV  & ---
  & the fit as a whole \\
$\GS \Lam^2$    & $1.835$      & $\GS = 5.0584\times10^{-6}$ MeV$^{-2}$
  & $m_\pi$, $f_\pi$ \\
$K \Lam^5$      & $12.36$      & $K = 1.5594\times10^{-13}$ MeV$^{-5}$
  & $m_{\eta'}$ \\
$m_u = m_d$     & $5.5$ MeV    & ---
  & $m_\pi$ \\
$m_s$           & $140.7$ MeV  & ---
  & $m_K$ \\
\bottomrule
\end{tabular}
\end{center}

The \emph{dimensionless} combinations are what is stored, because they are what
the fit determines; $\GS$ and $K$ in the table are $1.835/\Lam^2$ and
$12.36/\Lam^5$ evaluated at that $\Lam$, and every equation of this note takes
them in those units. Re-sampling one of these five numbers breaks the vacuum
phenomenology the whole model is anchored to: they move only if the entire fit
is redone, and $(f_\pi, \phi)$ alone leave a two-root degeneracy, so $m_\pi$
and $m_K$ must stay in any refit. Every vacuum number of
Sec.~\ref{sec:reproduces} is this set with no further tuning.

\subsection{Tier 2: structural choices, declared per run}

These are fields of the same record but they change the \emph{equations}, not a
number in them, so they are declared rather than sampled.

\begin{center}
\begin{tabular}{lll}
\toprule
symbol & shipped value & what it selects \\
\midrule
$\GV$ form & \texttt{"constant"}
  & one of \texttt{constant}, \texttt{power\_law}, \texttt{gluon\_exchange};
    Eq.~\eqref{eq:GVforms} \\
$\alpha$ & $2/3$
  & the power-law exponent, used only by \texttt{power\_law} \\
$n_{\rm ref}$ & $0.48$ fm$^{-3}$
  & the power law's reference \emph{quark} density,
    i.e.\ $\nB = n_{\rm sat}$ \\
$\lambda = \Lam_{\rm UV}/\Lam$ & $10$
  & the regularization scheme; $\lambda = 1$ is the conventional sharp
    cutoff (Sec.~\ref{sec:rg}) \\
\bottomrule
\end{tabular}
\end{center}

\noindent
The lepton content is structural in the same sense and is carried by the
species flags rather than the parameter record: \texttt{csc} (the pairing
sector of Sec.~\ref{sec:pairing}: with it off there are no gaps, no
quasiparticle spectrum to diagonalise and $\mu_3 = \mu_8 = 0$ identically),
\texttt{muons} (on by default), \texttt{thermal\_neutrinos} and
\texttt{photons} (both off by default). The three flags this model does not
have --- \texttt{hyperons}, \texttt{deltas}, \texttt{thermal\_mesons} ---
raise rather than being ignored: there are no baryons here to be strange or
resonant, and the mesons of Eq.~\eqref{eq:lagrangian} are the auxiliary fields
of the four-fermion terms, eliminated in favour of $\GS$, $K$ and $\GD$, so
they carry no independent thermal population at mean field.

\subsection{Tier 3: the sampled vector}

\begin{center}
\begin{tabular}{llll}
\toprule
symbol & shipped value & range & role \\
\midrule
$\eta_D = \GD/\GS$    & $0.75$ & $0.5$--$1.5$ & diquark strength; $0.75$ is the Fierz value \\
$\eta_V = \GV/\GS$    & $0$    & $0$--$1$     & vector repulsion, \texttt{constant} and \texttt{power\_law} forms \\
$\GV^0/\GS$           & $0.5$  & $0$--$1$     & vector repulsion, \texttt{gluon\_exchange} form \\
$M_g$                 & $500$ MeV & $400$--$800$ MeV & the non-perturbative gluon mass of that form \\
\bottomrule
\end{tabular}
\end{center}

$\eta_D = 1$ does \emph{not} mean ``equally strong channels'': the
condensation costs are $\sum_f (M_f - m_f)^2/(8\GS)$ against
$\sum_\eta \Delta_\eta^2/(4\GD)$, and the factor two between them is channel
and Fierz counting. $\eta_D$ also has to absorb the 't~Hooft--diquark
cross-term this model omits (Sec.~\ref{sec:lagrangian}), so it is an effective
coupling and a paper using it should say so.

\subsection{The published sets}

\begin{center}
\begin{tabular}{ll}
\toprule
name & what it changes against the tier-3 defaults \\
\midrule
\texttt{rkh} & nothing --- the shipped default, and the set every number in
  Sec.~\ref{sec:reproduces} was produced at \\
\texttt{rg\_njl1} & $\eta_D = 1.45$, $\eta_V = 0.7$ \\
\texttt{kunkel} & an alias of \texttt{rg\_njl1} \\
\texttt{gluon\_exchange} & the \texttt{gluon\_exchange} form,
  $\GV^0/\GS = 0.5$, $M_g = 500$ MeV \\
\bottomrule
\end{tabular}
\end{center}

\texttt{rg\_njl1} is ``parameter set 1'' of the RG-NJL family --- the soft one
of the three published sets --- and the set Ref.~\cite{Kunkel2026} uses. With the
default $\lambda = 10$ the couplings \emph{and} the regularization are theirs,
which is what makes this a reproduction of that model rather than a borrowing
of its numbers: the two were never independent, since RG-consistent gaps run
well above sharp-cutoff ones at the same coupling.

\subsection{The constants the shared sectors carry}

The leptons and the thermal gases of Sec.~\ref{sec:leptons} are not fitted
here and their constants are the repository's:
$m_e = 0.510999$ MeV and $m_\mu = 105.6584$ MeV, both with spin degeneracy
$g_e = g_\mu = 2$; neutrinos massless with $g_\nu = 1$ per flavour, particles
and antiparticles summed; photons massless with $g_\gamma = 2$. The
conversion between the natural units used inside and the fm-based public
boundary is $(\hbar c)^3$ with $\hbar c = 197.3269804$ MeV\,fm.


\paragraph{Three routes to a parameter set.} Model parameters are always
arguments, so all three have to exist. \emph{By name:}
\texttt{Parameters.default()} is the shipped RKH set, and
\texttt{Parameters.named(name)} takes any of the three published sets ---
\texttt{rkh}, \texttt{kunkel}, \texttt{gluon\_exchange} --- an unknown name
raising \texttt{KeyError} that lists them. \emph{A new set:} every field
carries a default, so \texttt{Parameters(G\_D=..., G\_V=...)} names only what
changes; the dataclass is frozen, so \texttt{dataclasses.replace} is how a set
already in hand is modified. \emph{From nuclear-matter parameters:} no route,
and none is missing --- NJL has no nuclear sector, so there is no
\texttt{nmp.py} and nothing to invert; its parameters are fixed by vacuum data
instead.

\section{The cut medium integrals}
\label{sec:medium}

One mode of mass $M$ at effective potential $\mu^{*}$ and temperature $T$,
integrated to a cutoff $\Lam$, with $E = \sqrt{k^2+M^2}$ and the Fermi
functions $f^{\pm} = [1+e^{(E \mp \mu^{*})/T}]^{-1}$. The $\Lam$ of this
section is the cutoff on the \emph{medium} integral, which is $\Lam_{\rm UV}$
and not the vacuum's $\Lam$ whenever $\lambda > 1$ (Sec.~\ref{sec:rg}); at
$T = 0$ and unpaired the distinction is invisible, since the $\min$ below makes
these integrals self-limiting at $k_F$:
\begin{align}
  n     &= \frac{g}{2\pi^2}\int_0^{\Lam}\!\! dk\, k^2 \,(f^{+}-f^{-}) ,
  &
  \rho_s &= \frac{g}{2\pi^2}\int_0^{\Lam}\!\! dk\, k^2 \frac{M}{E}\,(f^{+}+f^{-}) ,
  \label{eq:medium1}\\
  \varepsilon &= \frac{g}{2\pi^2}\int_0^{\Lam}\!\! dk\, k^2\, E\,(f^{+}+f^{-}) ,
  &
  s &= \frac{g}{2\pi^2}\int_0^{\Lam}\!\! dk\, k^2 \sum_{\pm}
      \left[ \frac{x_{\pm}}{T} f^{\pm} + \ln\!\big(1+e^{-x_{\pm}/T}\big)\right] ,
  \label{eq:medium2}
\end{align}
with $x_{\pm} = E \mp \mu^{*}$, and the pressure
\begin{equation}
  P_{\log} = \frac{g}{2\pi^2}\int_0^{\Lam}\!\! dk\, k^2\, T
    \Big[ \ln\!\big(1+e^{-(E-\mu^{*})/T}\big)
        + \ln\!\big(1+e^{-(E+\mu^{*})/T}\big) \Big] .
  \label{eq:Plog}
\end{equation}
Antiparticles \emph{subtract} in $n$ and \emph{add} in $\rho_s$,
$\varepsilon$ and $P$. At $T = 0$ the occupations become step functions and
$T\ln(1+e^{-x/T}) \to \max(-x,0)$; $s$ vanishes identically.

\paragraph{The $T = 0$ limit, in closed form.} At $T = 0$ the occupations
become step functions, $f^{+} = \theta(\mu^{*} - E)$ and $f^{-} = 0$ for
$\mu^{*} > 0$, so with
\begin{equation}
  k_F = \min\!\Big(\sqrt{\mu^{*2} - M^2},\ \Lam\Big) , \qquad
  E_F = \sqrt{k_F^2 + M^2}
  \label{eq:kF}
\end{equation}
every one of Eqs.~\eqref{eq:medium1}--\eqref{eq:Plog} integrates to
\begin{align}
  n &= \frac{g}{6\pi^2}\,k_F^3 ,
  &
  \rho_s &= \frac{g}{2\pi^2}\,\frac{M}{2}
       \Big[ k_F E_F - M^2 \operatorname{arcsinh}(k_F/M) \Big] ,
  \label{eq:T0a}\\
  \varepsilon &= \frac{g}{2\pi^2}\,\frac18
       \Big[ k_F (2k_F^2 + M^2) E_F - M^4 \operatorname{arcsinh}(k_F/M) \Big] ,
  &
  P_{\log} &= \mu^{*} n - \varepsilon , \qquad s = 0 ,
  \label{eq:T0b}
\end{align}
and \emph{all five vanish exactly} when $|\mu^{*}| \le M$: a mode too heavy for
its own potential is not in the medium, and that is a statement rather than an
optimisation. Eqs.~\eqref{eq:T0a}--\eqref{eq:T0b} are the sea integrals
\eqref{eq:seascalar}--\eqref{eq:seaeps} with $\Lam$ replaced by $k_F$ and
$g_{\rm sea}$ by $g$, which is the same integral over a different interval;
the $\min$ in Eq.~\eqref{eq:kF} is where a cut theory differs from an
uncut one, and $P_{\log} = \mu^{*} n - \varepsilon$ is the $T = 0$ statement of
$\Omega = \varepsilon - \mu n$ for one mode.

\paragraph{The surface term.} The second standard pressure form,
\begin{equation}
  P_{k^4} = \frac{g}{6\pi^2}\int_0^{\Lam}\!\! dk\,
            \frac{k^4}{E}\,(f^{+}+f^{-}) ,
\end{equation}
is obtained from Eq.~\eqref{eq:Plog} by parts, and the boundary term does
\emph{not} vanish when the integral is cut:
\begin{equation}
  P_{\log} - P_{k^4} = \frac{g}{6\pi^2}\,\Lam^3\, T
    \Big[ \ln\!\big(1+e^{-(E_\Lam-\mu^{*})/T}\big)
        + \ln\!\big(1+e^{-(E_\Lam+\mu^{*})/T}\big) \Big] ,
  \qquad E_\Lam = \sqrt{\Lam^2+M^2} .
  \label{eq:surface}
\end{equation}
It is not small: $0.1\%$ of $P$ at $(M,\mu^{*},T) = (100,500,20)$ MeV,
$10.5\%$ at $(40,590,30)$, $36.3\%$ at $(140,700,5)$ and $39.9\%$ at
$(140,700,50)$. \emph{Every assembly in this implementation uses
$P_{\log}$.} At $T=0$ with $k_F < \Lam$ the two agree, which is exactly why
the error would hide until a table were built at finite temperature.

\paragraph{Quadrature.} Gauss--Legendre on one panel over $[0,\Lam]$ cannot
resolve the Fermi step. Breakpoints are placed at each Fermi momentum
$k_{F,j} = \sqrt{\mu^{*2}_j - M_{f(j)}^2}$ and, at $T>0$, at
$k_{F,j} \pm 25T$, and each panel is integrated separately; \emph{the cutoff is
imposed as the panel upper limit before the panels are built}, because
filtering breakpoints afterwards can delete the Fermi-surface break and revert
silently to single-panel accuracy.

\subsection{The same integrals in \texttt{vmit} and \texttt{alphabag}}
\label{sec:bagcompare}

Eqs.~\eqref{eq:medium1}--\eqref{eq:Plog} are the ideal-gas integrals the bag
models of this repository also evaluate. Four things differ, and each is a
place where a bag-model instinct gives the wrong answer here.

\begin{enumerate}
\item \emph{The upper limit is finite.} \texttt{vmit} and \texttt{alphabag}
  integrate to infinity, because a bag model has no cutoff to regularise: the
  four-fermion interaction is what needs one, and they have none. The cut is
  not decoration --- it is what makes $\phi_f$ finite, and it is what
  Sec.~\ref{sec:rg} exists to repair.
\item \emph{The mass is a solved quantity.} $M$ in Eqs.~\eqref{eq:medium1} is
  $M_f$ from the gap equation, not a parameter. Its density dependence is why
  the NJL energy density is not a sum of independent flavour integrals: moving
  $\mu_u$ moves $M_s$.
\item \emph{The pressure must come from the logarithm.} In \texttt{vmit} and
  \texttt{alphabag} the two pressure forms agree identically, because the upper
  limit is infinite and the surface term \eqref{eq:surface} vanishes; here they
  agree only at $T=0$ with $k_F < \Lam$.
\item \emph{The quadrature has to be told where the Fermi surface is.} The bag
  models sidestep this entirely by using the JEL expansion, which is a closed
  form and has no quadrature to break.
\end{enumerate}

\begin{center}
\footnotesize
\begin{tabular}{p{0.16\textwidth}p{0.24\textwidth}p{0.24\textwidth}p{0.28\textwidth}}
\toprule
 & \texttt{vmit} & \texttt{alphabag} & \texttt{njl} \\
\midrule
degrees of freedom & 3 flavours, $g=6$ & 3 flavours, $g=6$ ($+$ gluons)
  & 9 colour-flavour modes, $g=2$ \\
mass in the integral & $m_q$ (parameter) & $m_q$ (parameter) & $M_f$ (solved) \\
upper limit & $\infty$ & $\infty$ & $\Lam_{\rm UV}$ \\
evaluated by & JEL expansion & JEL $+$ massless closed forms
  & panelled Gauss--Legendre \\
$P$ from & JEL & closed forms & $P_{\log}$, never $P_{k^4}$ \\
Dirac sea & none & none & $-\sum_f \varepsilon_{{\rm sea},f}$, closed form \\
condensate cost & none & none
  & $C = 2\GS\sum_f\phi_f^2 - 4K\phi_u\phi_d\phi_s$ \\
pairing & none
  & $+(\mu_u^2+\mu_d^2+\mu_s^2)\Delta^2/\pi^2$, $\Delta$ imposed
  & $\delta\Omega_{\rm pair}$ from a 36-state spectrum, $\Delta$ solved \\
perturbative QCD & none & $\alpha_s$ multiplying the free-gas terms
  & none: the four-fermion couplings \emph{are} the interaction \\
vector & $P_V = \varepsilon_V = \tfrac12 a\hbar c\,\nq^2$ & none
  & $W = \GV(\nq)\nq^2$, $\Sigma_V = dW/d\nq$ \\
bag & $\pm B/(\hbar c)^3$ & $\pm B/(\hbar c)^3$
  & $-\Omega_{\rm vac}$, derived \\
colour potentials & --- & --- & $\mu_3,\mu_8$, solved from $n_3 = n_8 = 0$ \\
\bottomrule
\end{tabular}
\end{center}

The Euler relation $\varepsilon + P = Ts + \sum_i \mu_i n_i$ holds in all
three, and in all three it is an audit rather than a definition.

\paragraph{Where the \texttt{alphabag} CFL term sits.} That model adds
$\delta P = (\mu_u^2+\mu_d^2+\mu_s^2)\Delta^2/[\pi^2 (\hbar c)^3]$, the
standard CFL condensation energy of Alford, Braby, Paris and
Reddy~\cite{AlfordBraby2005}: nine gapped modes at a common $\mu$, counted,
with $\Delta$ an \emph{input}. Its counterpart here is not one term but the
pair $\delta\Omega_{\rm pair} + D$ of Sec.~\ref{sec:totals} --- the
change in the spectrum, plus the $\sum_\eta \Delta_\eta^2/(4\GD)$ cost of
making the condensate --- evaluated where it is stationary in $\Delta$, which
is the gap equation. At that point the two scale the same way,
$\sim \mu^2\Delta^2$, and they differ in three ways that matter at
$\Delta \sim 100$ MeV: the gap is \emph{solved} rather than declared; the nine
modes are \emph{not} degenerate, since the $s$ mass breaks the locking and the
three gaps are three numbers; and the paired densities and entropy are not
obtained by differentiating a $\Delta^2\mu^2$ term but by the
Hellmann--Feynman pass of Eqs.~\eqref{eq:dn}--\eqref{eq:ds}, which is the
largest single departure and is worth tens of percent.

\section{The Dirac sea, the condensates and the vacuum}
\label{sec:vacuum}

The vacuum integrals of one flavour, in closed form with $g_{\rm sea} = 6$ and
$R = \sqrt{\Lam^2+M^2}$:
\begin{align}
  \rho_s^{\rm vac}(M) &= \frac{g_{\rm sea}}{2\pi^2}\,\frac{M}{2}
      \Big[ \Lam R - M^2 \operatorname{arcsinh}(\Lam/M) \Big] ,
  \label{eq:seascalar}\\
  \varepsilon_{\rm sea}(M) &= \frac{g_{\rm sea}}{2\pi^2}\,\frac18
      \Big[ \Lam (2\Lam^2+M^2) R - M^4 \operatorname{arcsinh}(\Lam/M) \Big] ,
  \label{eq:seaeps}
\end{align}
and $\partial \varepsilon_{\rm sea}/\partial M = \rho_s^{\rm vac}$ identically.
The condensate of one flavour is the sea's scalar density with the medium's own
occupation subtracted, summed over the three colours of that flavour:
\begin{equation}
  \phi_f = -\rho_s^{\rm vac}(M_f) + \sum_{a} \rho_{s,(f,a)}
           + \delta\rho_{s,f} ,
  \label{eq:phi}
\end{equation}
where $\delta\rho_{s,f}$ is the pairing correction of
Sec.~\ref{sec:pairing}. The condensate cost entering both $\Omega$ and
$\varepsilon$ is
\begin{equation}
  C = 2\GS \sum_f \phi_f^2 - 4K\,\phi_u \phi_d \phi_s .
  \label{eq:C}
\end{equation}

\paragraph{The vacuum solution.} At $\mu = T = 0$ Eqs.~\eqref{eq:massgap} and
\eqref{eq:phi} close on themselves and are solved by a \emph{damped fixed
point} on the masses,
\begin{equation}
  M \leftarrow M + \lambda\,\big(M_{\rm new}[\phi(M)] - M\big), \qquad
  \lambda = 0.3 ,
\end{equation}
and not by a root finder on the condensates, which diverges and returns masses
that increase with density. The vacuum constant is
$\Omega_{\rm vac} = \varepsilon_{\rm vac}
= -\sum_f \varepsilon_{\rm sea}(M_f^{\rm vac}) + C^{\rm vac}$, and the equality
of the two is what makes the Euler relation survive the subtraction.

\paragraph{Vacuum diagnostics.} The pion decay constant from the quark loop,
\begin{equation}
  f_\pi^2 = 2 M^2 I_2 , \qquad
  I_2 = \frac{N_c}{4\pi^2}\Big[\operatorname{arcsinh}(\Lam/M)
        - \Lam/\sqrt{\Lam^2+M^2}\Big] ,
  \label{eq:fpi}
\end{equation}
and the effective bag constant, $\Omega$ at fixed masses evaluated at the
current masses minus at the broken-phase ones,
\begin{equation}
  B_{\rm eff} = \Omega\big[M_f = m_f\big] - \Omega\big[M_f = M_f^{\rm vac}\big] .
  \label{eq:Beff}
\end{equation}
$B_{\rm eff}$ is a \emph{derived} quantity here, not an input the way a bag
constant is in a bag model.

\section{The pairing sector}
\label{sec:pairing}

The pairing sector follows R\"uster \textit{et al.}~\cite{Ruester2005};
Ref.~\cite{Alford2008} is the review of the phases it can realise.

The machinery of this section is \texttt{eos/general/pairing.py}, shared with
the colour-dielectric model, because the pairing sector of the two is the same
sector.

\subsection{The gap matrix}
\label{sec:gapmatrix}

\begin{equation}
  G_{(fa),(gb)} = \sum_\eta \Delta_\eta\, \epsilon^{ab\eta}\,\epsilon_{fg\eta}
                = \sum_\eta \Delta_\eta\, (B_\eta)_{(fa),(gb)} ,
  \label{eq:G}
\end{equation}
so $\Delta_1$ pairs $d$ with $s$, $\Delta_2$ pairs $u$ with $s$, and
$\Delta_3$ pairs $u$ with $d$ --- the 2SC gap. The matrix is symmetric with
identically zero diagonal. Its eigenvalue multiplicities are a \emph{derived}
property of the pattern and are never assigned by hand; at $\Delta_0 = 60$ MeV,

\begin{center}
\begin{tabular}{lll}
\toprule
pattern & $\Delta$ & spectrum of $G$ [MeV] \\
\midrule
unpaired & $(0,0,0)$ & $0\,(\times 9)$ \\
2SC & $(0,0,\Delta_0)$ & $-60\,(\times2)$, $0\,(\times5)$, $+60\,(\times2)$ \\
CFL & $(\Delta_0,\Delta_0,\Delta_0)$ & $-60\,(\times5)$, $+60\,(\times3)$, $+120$ \\
uSC & $(0,\Delta_0,\Delta_0)$ & $\pm84.85$, $\pm60\,(\times2)$, $0\,(\times3)$ \\
dSC & $(\Delta_0,0,\Delta_0)$ & as uSC \\
\bottomrule
\end{tabular}
\end{center}
With independent gaps the $\pm\sqrt2\,\Delta_0$ eigenvalue generalises to
$\pm\sqrt{\Delta_2^2+\Delta_3^2}$.

\subsection{The quasiparticle spectrum}
\label{sec:bdg}

The mean-field inverse propagator is diagonalised in the \emph{full Dirac
basis}: four components per colour--flavour mode --- two particle, two
antiparticle --- so $36$ states at each momentum, with the gap mixing them.
This is Appendix~A of R\"uster \emph{et al.}, Phys. Rev. D \textbf{72},
034004 (2005), and it is what the published MUSES NJL module diagonalises.

The $36$ states block-diagonalise. Six of the nine modes pair off pairwise,
$(d_r,u_g)$ through $\Delta_3$, $(s_r,u_b)$ through $\Delta_2$ and
$(s_g,d_b)$ through $\Delta_1$; each such pair $(a,b)$ with gap $\Delta_\eta$
contributes two $4\times4$ blocks, one for each sign $s = \pm1$,
\begin{equation}
  M^{(s)}_{ab}(k) = \begin{pmatrix}
    -s\mu^{*}_a + sM_a & k & 0 & -\Delta_\eta \\
    k & -s\mu^{*}_a - sM_a & \Delta_\eta & 0 \\
    0 & \Delta_\eta & s\mu^{*}_b + sM_b & k \\
    -\Delta_\eta & 0 & k & s\mu^{*}_b - sM_b
  \end{pmatrix} .
  \label{eq:block4}
\end{equation}
The remaining triple $(u_r, d_g, s_b)$ is coupled by all three gaps at once
and gives one $12\times12$ block: slot $m$ (mode $j$) carries the four rows
$4m + (0,1,2,3)$ with diagonal
$(-\mu^{*}_j - M_j,\ -\mu^{*}_j + M_j,\ \mu^{*}_j - M_j,\ \mu^{*}_j + M_j)$
and $k$ at $(4m,4m{+}1)$ and $(4m{+}2,4m{+}3)$, and the gap $\Delta_\eta$
joining slots $A$ and $B$ enters at
\begin{equation}
  (4A{+}0,\,4B{+}3) = -\Delta_\eta, \quad
  (4A{+}1,\,4B{+}2) = +\Delta_\eta, \quad
  (4A{+}2,\,4B{+}1) = +\Delta_\eta, \quad
  (4A{+}3,\,4B{+}0) = -\Delta_\eta ,
  \label{eq:block12}
\end{equation}
symmetrically, with $\Delta_3$ joining slots $(0,1)$, $\Delta_2$ slots $(0,2)$
and $\Delta_1$ slots $(1,2)$. Every matrix is real symmetric, its spectrum
comes in $\pm$ pairs, and $\Omega$ wants the positive half --- taken as
\emph{half the sum over all $36$ of} $|\lambda_a|$, so that a branch crossing
zero inside a gapless window needs no bookkeeping: its partner crosses back,
and $\partial|\lambda|/\partial x = \mathrm{sign}(\lambda)\,
\partial\lambda/\partial x$ carries the branch sign automatically.

A mode that \emph{no nonzero gap touches} is left out of the blocks
altogether, and out of the unpaired reference of Eq.~\eqref{eq:dOmega} with
it. Its four Dirac components are then exactly the unpaired ones and it
contributes nothing --- but computed as a difference it would be zero minus an
ill-conditioned number, since an ungapped branch crosses zero at its own Fermi
surface with its partner at $-0$ in the same block. Leaving it out makes the
zero exact; carrying it stalls the equilibrium solve at $10^{-8}$.

\paragraph{Why not the on-shell reduction.} Solving the free Dirac problem
first, fixing $E_f = \sqrt{k^2+M_f^2}$ and pairing the on-shell modes in an
$18\times18$ Bogoliubov--de~Gennes problem
$H^r = \big(\begin{smallmatrix} \mathrm{diag}(\xi^r) & G \\
G & -\mathrm{diag}(\xi^r)\end{smallmatrix}\big)$, $\xi^r_j = E_{f(j)} -
r\mu^{*}_j$, is the familiar construction and gives, for one gap,
\begin{equation}
  E^{\pm} = \sqrt{(\bar E - \bar\mu)^2 + \Delta^2} \pm
            \Big[\tfrac12 (E_d - E_u) - \delta\mu\Big] ,
  \qquad \bar E = \tfrac12 (E_u + E_d),\ \
  \bar\mu = \tfrac12(\mu^{*}_{ur}+\mu^{*}_{dg}),\ \
  \delta\mu = \tfrac12(\mu^{*}_{dg}-\mu^{*}_{ur}) .
  \label{eq:twosc}
\end{equation}
It drops the particle--antiparticle mixing the gap induces, and what controls
that is the \emph{mass mismatch of the pair}: at equal paired masses
Eq.~\eqref{eq:twosc} reproduces the exact spectrum to $10^{-13}$ MeV whatever
the mass is, and the two part company as the masses differ --- $3.4$ MeV at
$M = (5.5, 300)$ MeV, $\Delta = 80$ MeV, $k = 320$ MeV. So the reduction is
harmless for 2SC, which pairs $u$ with $d$, and it is not harmless for CFL,
which pairs both of them with $s$: at parameter set 1 it moves the CFL
branches by up to $11$ MeV, the pressure by $6$--$16\%$, and the
$2\text{SC}\to\text{CFL}$ transition density by $9\%$. $E^{-}$ may be
\emph{negative}: that is the gapless window, the BCS blocking region, not an
error.

\subsection{The pairing potential}
\label{sec:dOmega}

Written as a \emph{correction}, a difference from the unpaired spectrum:
\begin{equation}
  \delta\Omega_{\rm pair} = -\frac{1}{2\pi^2}
    \int_0^{\Lam}\!\! dk\, k^2 \left[
    \frac12 \sum_{a=1}^{36} \varphi(|\lambda_a|)
    - \sum_{r=\pm}\sum_{j \in \mathcal C} \varphi(|\xi^r_j|) \right] ,
  \qquad
  \varphi(x) = x + 2T \ln\!\big(1+e^{-x/T}\big) ,
  \label{eq:dOmega}
\end{equation}
with $\xi^r_j = E_{f(j)} - r\mu^{*}_j$ and $\mathcal C$ the modes the blocks
cover,
with $\varphi(x) = x$ at $T = 0$. This vanishes \emph{identically} at
$\Delta = 0$, which is what makes the unpaired phase a clean limit of the same
code, and in the clean weak-coupling limit it obeys the BCS logarithm,
$-\delta\Omega_{\rm pair} \to (2/\pi^2)\,\mu^2\Delta^2[\ln(2\Lam/\Delta)-\tfrac12]$.
Both signs of $r$ are summed: the antiparticle branches contribute $8.8\%$ of
the particle piece at $\Lam = 600$ MeV and $17.1\%$ at $1000$ MeV.

The $|\xi_j|$ subtraction kinks at each of the nine $k_{F,j}$, so the pairing
quadrature is split there (and at $k_{F,j}\pm 25T$) exactly as in
Sec.~\ref{sec:medium}. It is the \emph{splitting} that buys the accuracy, not
the node count: at $100$ nodes per panel the relative error is
$3\times10^{-14}$ where a single panel of $800$ nodes reaches
$2\times10^{-7}$. The shipped rule is $24$ Gauss--Legendre nodes per panel,
overridable per call.

\subsection{The gap equations, and everything else the same pass returns}
\label{sec:kernels}

Since $\varphi'(x) = \tanh(x/2T)$, differentiating Eq.~\eqref{eq:dOmega} by
Hellmann--Feynman gives, with $|V_a\rangle$ the eigenvector of $\lambda_a$,
$w_a = \tfrac12\,\mathrm{sign}(\lambda_a)\tanh(|\lambda_a|/2T)$, and the
derivative matrices read off Eqs.~\eqref{eq:block4}--\eqref{eq:block12} ---
$\partial H/\partial\mu^{*}_j$ and $\partial H/\partial M_f$ \emph{diagonal},
carrying the row signs $s_\mu$ and $s_M$ of that mode or flavour, and
$\partial H/\partial\Delta_\eta$ the placement pattern of the gap itself ---
\begin{align}
  \frac{\Delta_\eta}{2\GD}
  - \frac{1}{2\pi^2}\int\!\! dk\, k^2 \sum_{a=1}^{36} w_a
    \Big\langle V_a \Big| \frac{\partial H}{\partial \Delta_\eta}
    \Big| V_a \Big\rangle &= 0 ,
  \label{eq:gapeq}\\
  \delta n_j = \frac{1}{2\pi^2}\int\!\! dk\, k^2
    \left[ \sum_{a} w_a
      \Big\langle V_a \Big| \frac{\partial H}{\partial \mu^{*}_j}
      \Big| V_a \Big\rangle
      + \sum_{r} r \tanh\!\frac{\xi^r_j}{2T}\right] ,
  \label{eq:dn}\\
  \delta\rho_{s,f} = -\frac{1}{2\pi^2}\int\!\! dk\, k^2
    \left[\sum_{a} w_a
      \Big\langle V_a \Big| \frac{\partial H}{\partial M_f}
      \Big| V_a \Big\rangle
      - \sum_{r}\sum_{j \in f \cap\, \mathcal C}
        \frac{M_f}{E_j}\tanh\!\frac{\xi^r_j}{2T}\right] ,
  \label{eq:drhos}\\
  \delta s = \frac{1}{2\pi^2}\int\!\! dk\, k^2
    \left[\frac12\sum_{a} \psi(|\lambda_a|)
    - \sum_{r}\sum_{j\in\mathcal C} \psi(|\xi^r_j|)\right] ,
  \qquad
  \psi(x) = 2\ln\!\big(1+e^{-x/T}\big)
            + \frac{x}{T}\Big[1 - \tanh\frac{x}{2T}\Big] .
  \label{eq:ds}
\end{align}
The unpaired terms are written out rather than diagonalised: they are
analytic, they are what makes every correction vanish \emph{identically} at
$\Delta = 0$, and computing them twice by two routes is exactly how that
cancellation would fail.

All four come from \emph{one} quadrature pass and one set of
diagonalisations. Both flavours of that pass are shipped and agree to
$10^{-12}$: the reference one batches \texttt{numpy.linalg.eigh} over the
quadrature, the compiled one runs a Householder reduction and an
implicit-shift QL iteration (EISPACK's \texttt{tred2}/\texttt{tql2}) node by
node under numba, and is $1.8\times$ faster.

Three points about Eqs.~\eqref{eq:gapeq}--\eqref{eq:ds} are the ones that
return a plausible wrong answer if got wrong.

\paragraph{The kernel is not $\Delta/|E|$.} That form --- obtained by
differentiating $|E|$ as though every branch were positive --- is wrong by a
factor $12.0$ at $\Delta = 40$ MeV, $1.7$ at $60$ and $1.3$ at $80$ for
$\mu_u = 400$, $\mu_d = 500$ MeV, and it makes the gap \emph{grow} with the
mismatch, which is the opposite of the physics. It agrees with
Eq.~\eqref{eq:gapeq} only where every branch is positive.

\paragraph{Paired densities and entropy are not the unpaired integrals.} At
$\muB = 1400$ MeV, $T = 20$ MeV, $\Delta_3 = 80$ MeV the unpaired density
formula is wrong by $-21.1\%$ on the paired $u$ modes and $+11.6\%$ on the
paired $d$ modes. The entropy is worse: in a fully gapped phase the ratio of
paired to unpaired entropy is $2\times10^{-4}$ at $T = 5$ MeV.

\paragraph{The gap equation has three roots.} With a mismatch,
$R(\Delta) = \Delta/2\GD - \mathrm{kernel}$ vanishes at $\Delta = 0$, at a
barrier maximum, and at the physical BCS root. A fixed bracket returns
whichever it happens to contain. At $\mu^{*} = 450$ MeV, $\eta_D = 0.75$ the
roots are $92.71$ MeV at zero mismatch and $(32.4,\,92.71)$, $(52.5,\,92.71)$,
$(59.9,\,92.71)$ at $\delta\mu = 50, 60, 65$ MeV; the free-energy crossover
sits at $\delta\mu_c = 63.59$ MeV, a fraction $0.970$ of the weak-coupling
Clogston--Chandrasekhar value $\Delta_0/\sqrt2$, the $3\%$ deficit being the
finite cutoff.

\section{Regularization: the vacuum at $\Lam$, the medium at $\Lam_{\rm UV}$}
\label{sec:rg}

A sharp cutoff on \emph{every} momentum integral is what the model was fitted
with, and it is unusable at the densities the model is applied to. With
$\Lam = 602.3$ MeV and quark potentials of $400$--$500$ MeV the medium reaches
the cutoff: the 2SC gap peaks at $\mu^{*} = 0.83\,\Lam$ and is identically zero
by $\mu^{*} = 1.13\,\Lam$, where the loop contributions are cut off entirely and
the model silently returns a free gas. Measured here at $\eta_D = 1$:
%
\begin{center}
\begin{tabular}{lcccccc}
\toprule
$\mu^{*}/\Lam$ & $0.664$ & $0.830$ & $0.996$ & $1.129$ & $1.328$ & $1.494$ \\
\midrule
$\Delta_3$ [MeV], $\lambda = 1$
  & $141.06$ & $149.48$ & $113.33$ & $0.00$ & $0.00$ & $0.00$ \\
$\Delta_3$ [MeV], $\lambda = 10$
  & $166.71$ & $214.51$ & $251.50$ & $276.16$ & $306.85$ & $327.65$ \\
\bottomrule
\end{tabular}
\end{center}

The fix is renormalization-group consistency~\cite{Braun2018,Gholami2024}:
require that the answer not depend on the scale the theory is initialized at,
%
\begin{equation}
  \lim_{\Lam_{\rm UV}\to\infty}
  \Lam_{\rm UV}\,\frac{\partial \Gamma}{\partial \Lam_{\rm UV}} = 0 .
  \label{eq:rgconsistency}
\end{equation}
%
The vacuum keeps the cutoff it was \emph{fitted} at and only the medium runs,
%
\begin{equation}
  \Omega = \mathcal{V}(\chi)
  - \frac{1}{2\pi^2}\left[
      \int_0^{\Lam} \!\! dp\, p^2\, \mathcal{A}_{\rm vac}(\chi)
    + \int_0^{\Lam_{\rm UV}} \!\! dp\, p^2
      \big( \mathcal{A}(\mu, T, \chi) - \mathcal{A}_{\rm vac}(\chi) \big)
    \right] ,
  \label{eq:rgsplit}
\end{equation}
%
with $\mathcal{A}_{\rm vac}(\chi) = \mathcal{A}(\mu = 0, T = 0, \chi)$ the
vacuum integrand \emph{at the same condensates} --- gaps included, which is the
half that is easy to drop. The $\Delta$-dependent Dirac sea is a vacuum
quantity and keeps $\Lam$; leaving it in the medium remainder makes that
remainder diverge \emph{quadratically} in $\Lam_{\rm UV}$ instead of
logarithmically, and no counterterm of the form below can cancel that.

$\lambda = \Lam_{\rm UV}/\Lam$ is the one parameter that says which scheme is
in use; it is $10$ by default, and $\lambda = 1$ is the conventional
sharp-cutoff model exactly and not approximately.

\subsection{The medium divergence and its counterterm}

In an unpaired phase the medium remainder converges and nothing more is needed:
at $T = 0$ the occupations are step functions cut at $k_F$ and the cutoff never
binds, so unpaired results are independent of $\lambda$ to the last bit. With a
gap the cancellation that makes $f_{\rm vac} = 2\sqrt{M^2 + p^2}$ no longer
happens and the remainder carries a logarithm. Summing the six Cooper pairs
$(i,j)$ with mean potential $\bar\mu_{ij} = (\mu^{*}_i + \mu^{*}_j)/2$,
%
\begin{equation}
  \Omega_{\rm med} \;\sim\;
  -\frac{1}{\pi^2}\sum_{(ij)} \bar\mu_{ij}^{\,2}\,\Delta_\eta^2
   \,\ln \Lam_{\rm UV} .
  \label{eq:mediumdivergence}
\end{equation}
%
The counterterm is the \emph{massless} scheme of Ref.~\cite{Gholami2024}
(their Eq.~C7), which sets $M = 0$ in the renormalization factors and is
therefore the scheme with a closed form --- and the one Ref.~\cite{Kunkel2026}
uses throughout:
%
\begin{equation}
  \delta\Omega_{\rm ct} = \frac{1}{\pi^2}
  \sum_{(ij)} \bar\mu_{ij}^{\,2}\,\Delta_\eta^2\; g(\Delta_\eta) ,
  \label{eq:counterterm}
\end{equation}
\begin{equation}
  g(\Delta) = \frac{\Lam}{\sqrt{\Lam^2 + \Delta^2}}
            - \frac{\Lam_{\rm UV}}{\sqrt{\Lam_{\rm UV}^2 + \Delta^2}}
            + \ln\!\left[
              \frac{\Lam_{\rm UV} + \sqrt{\Lam_{\rm UV}^2 + \Delta^2}}
                   {\Lam + \sqrt{\Lam^2 + \Delta^2}} \right] .
  \label{eq:countertermshape}
\end{equation}
%
The six pairs are the two colour choices per gap, $\Delta_1$ pairing $d$ with
$s$, $\Delta_2$ pairing $u$ with $s$ and $\Delta_3$ pairing $u$ with $d$:
$(d_g, s_b), (d_b, s_g)$; $(u_r, s_b), (u_b, s_r)$; $(u_r, d_g), (u_g, d_r)$.
Within a pair the colour potential $\mu_3$ cancels, so the two pairs of one gap
share a $\bar\mu$ and the sum may equally be written over the three gaps with a
coefficient $2/\pi^2$, which is how Eq.~C7 states it.

Two limits carry the design. $g \to \ln(\Lam_{\rm UV}/\Lam)$ for large
$\Lam_{\rm UV}$, which is the logarithm Eq.~\eqref{eq:mediumdivergence} needs
cancelled; and $g = 0$ \emph{identically} at $\Lam_{\rm UV} = \Lam$. The second
is why one parameter carries the scheme and no boolean stands beside it: at
$\lambda = 1$ the counterterm is not small, it is zero.

It is not only a term in $\Omega$. It depends on the potentials, so it carries
a density, and on the gaps, so it enters the gap equations:
%
\begin{equation}
  \delta n_j = -\frac{\partial\, \delta\Omega_{\rm ct}}{\partial \mu^{*}_j}
             = -\frac{1}{\pi^2} \sum_{(ij) \ni j}
                \bar\mu_{ij}\,\Delta_\eta^2\, g(\Delta_\eta) ,
  \label{eq:countertermdensity}
\end{equation}
\begin{equation}
  \frac{d}{d\Delta}\Big[\Delta^2 g\Big]
  = 3\Delta\left(\frac{\Lam}{A} - \frac{\Lam_{\rm UV}}{B}\right)
  + 2\Delta \ln\!\left[\frac{\Lam_{\rm UV} + B}{\Lam + A}\right]
  + \Delta^3\left(\frac{\Lam_{\rm UV}}{B^3} - \frac{\Lam}{A^3}\right) ,
  \label{eq:countertermgap}
\end{equation}
%
with $A = \sqrt{\Lam^2 + \Delta^2}$ and $B = \sqrt{\Lam_{\rm UV}^2 + \Delta^2}$,
so the gap row of the residual is
$\Delta_\eta/(2\GD) - \mathrm{kernel}_\eta + \partial\,\delta\Omega_{\rm ct}/
\partial\Delta_\eta$. Adding the counterterm to $\Omega$ alone would leave the
Euler relation violated and the gaps solving the wrong equation, and both
failures look like a plausible equation of state. It carries no explicit $T$
and no mass, so it contributes to neither $s$ nor the scalar density.

\subsection{Cost, and one numerical trap}

Three quadrature passes instead of one --- the medium at $\Lam_{\rm UV}$, and
the vacuum block at $\Lam_{\rm UV}$ and at $\Lam$ --- over a momentum range ten
times wider. At $\lambda = 1$ the two vacuum blocks are the same integral and
the single pass is taken directly.

The panels have to follow the cutoff. The pairing quadrature breaks at each
Fermi momentum, which at $\lambda = 1$ covers the whole interval; at
$\lambda = 10$ it leaves everything between the highest $k_F$ and $6023$ MeV in
\emph{one} panel, across which no number of Gauss nodes resolves a $1/p$ tail.
That mis-integrates the pairing potential by $4.0\times10^{-7}$ relative ---
small enough to look like round-off, large enough to make a warm-started table
and a cold point solve disagree past their convergence gate. Geometric panels
at $\Lam_{\rm UV}/2, \Lam_{\rm UV}/4, \dots$ down to the highest Fermi momentum
bring it to $2.9\times10^{-13}$ at the same node count.

\subsection{What it buys}

The gaps rise monotonically instead of collapsing (the table above), and the
density ceiling $\Lam^3/\pi^2 = 2.881$ fm$^{-3}$ --- a regularization artifact
rather than physics --- moves to $\Lam_{\rm UV}^3/\pi^2$ and stops binding. The
residual $\lambda$-dependence is the $\mathcal{O}(1/p^3)$ tail the asymptotic
expansion of Eq.~\eqref{eq:mediumdivergence} drops: doubling $\lambda$ from
$10$ to $20$ moves $\Omega$ and the gaps by $1.4\times10^{-3}$ (2SC) and
$1.9\times10^{-3}$ (CFL).

\section{The vector sector}
\label{sec:vector}

With $\nq = \sum_j n_j$ the total quark density, the vector interaction energy
and self-energy are
\begin{equation}
  W(\nq) = \GV(\nq)\,\nq^2 , \qquad
  \Sigma_V = \frac{dW}{d\nq} = \big(2 - \alpha_{\rm eff}\big)\,\GV(\nq)\,\nq ,
  \qquad
  \alpha_{\rm eff} = -\frac{d\ln \GV}{d\ln \nq} ,
  \qquad
  \mu^{*}_j = \mu_j - \Sigma_V .
  \label{eq:vector}
\end{equation}
Three forms are implemented, selected by the tier-2 choice of
Sec.~\ref{sec:parameters}:
\begin{equation}
  \GV(\nq) = \begin{cases}
    \eta_V \GS ,
      & \texttt{constant} \\[4pt]
    \eta_V \GS \left(\dfrac{n_{\rm ref}}{\nq}\right)^{\alpha} ,
      & \texttt{power\_law} \\[8pt]
    \dfrac{(\GV^0/\GS)\,\GS}{1 + u} , \quad
      u = \dfrac{8 k_F^2}{9 M_g^2} , \quad
      k_F = \left(\tfrac12 \pi^2 \nq\right)^{1/3} ,
      & \texttt{gluon\_exchange}
  \end{cases}
  \label{eq:GVforms}
\end{equation}
with $\alpha_{\rm eff} = 0$, $\alpha$ and $\tfrac23\,u/(1+u)$ respectively.
The power law's $\eta_V \GS$ is the coupling \emph{at} $n_{\rm ref}$, so the
constant form is the $\alpha = 0$ member of the same family; $n_{\rm ref}$ is
declared in fm$^{-3}$ and multiplied by $(\hbar c)^3$ before it meets $\nq$.
The gluon-exchange form takes its strength from its own field $\GV^0/\GS$
rather than from $\eta_V$, because a run using it is not a run at a constant
coupling of the same size.

\paragraph{Why the density dependence exists.} Writing
$\varepsilon = \sum_i C_i n^{p_i}$, each term contributes
$P_i = C_i (p_i-1) n^{p_i}$, so
\begin{equation}
  c_s^2 = \frac{\sum_i C_i p_i (p_i-1) n^{p_i-1}}
               {\sum_i C_i p_i n^{p_i-1}} ,
  \qquad\Longrightarrow\qquad
  c_s^2(n\to\infty) = \max\!\big(1-\alpha,\ \tfrac13\big)
\end{equation}
for $\GV \sim n^{-\alpha}$, since the vector term then has $p_V = 2-\alpha$
against the free-quark $4/3$. Constant $\GV$ gives $\alpha = 0$ and
$c_s^2 \to 1$, Zel'dovich behaviour. At $\alpha = 2/3$ the vector term is
conformal \emph{identically, not asymptotically}: $P_{\rm int}/\varepsilon_{\rm
int} = 1-\alpha = 1/3$ at every density. The gluon-exchange form of Eq.~\eqref{eq:GVforms} reaches
$\alpha_{\rm eff} = 2/3$ as a consequence of its own structure, with no tuning
--- $\alpha_{\rm eff} = 0.062,\ 0.460,\ 0.608,\ 0.653$ at
$\nq = 10^6, 10^8, 10^9, 10^{10}$ MeV$^3$ --- which is why it is the
recommended variant. Pairing does not change the asymptotics:
$c_s^2 \to \tfrac13 + \tfrac29 \Delta^2/\mu^2$, which dies as $1/\mu^2$.

\paragraph{The rearrangement term is mandatory.} Once $\GV$ depends on the
density, $\Sigma_V$ is $dW/d\nq$ and not $2\GV \nq$; the ratio is $0.833$ at
$\alpha = 1/3$ and $0.667$ at $2/3$, and using the naive form shifts $P$ by
about $5\%$ and breaks $n = dP/d\mu$ at the first digit. (A coupling that
depends on a mean \emph{field} needs no such term, because it enters through
that field's own equation of motion. A density-dependent one does.)

\section{The totals}
\label{sec:totals}

With $D = \sum_\eta \Delta_\eta^2/(4\GD)$ the pairing cost and $C$ of
Eq.~\eqref{eq:C}, the matter sector alone --- no leptons, no photons ---
assembles as
\begin{align}
  \Omega &= -\sum_j P_{{\rm med},j} - \sum_f \varepsilon_{{\rm sea},f}
            + C - \big(\Sigma_V \nq - W\big)
            + \delta\Omega_{\rm pair} + D
            \ -\ \Omega_{\rm vac} ,
  \label{eq:Omega}\\
  \varepsilon &= \ \ \sum_j \varepsilon_{{\rm med},j}
            - \sum_f \varepsilon_{{\rm sea},f}
            + C + W + \varepsilon_{\rm pair}
            \ -\ \varepsilon_{\rm vac} ,
  \label{eq:eps}\\
  \varepsilon_{\rm pair} &= \delta\Omega_{\rm pair} + D
            + T\,\delta s + \sum_j \mu^{*}_j\, \delta n_j ,
  \qquad
  s = \sum_j s_{{\rm med},j} + \delta s ,
  \qquad
  P = -\Omega ,
  \label{eq:epspair}
\end{align}
and both carry the \emph{same} vacuum constant, so
$\Omega_{\rm vac} = \varepsilon_{\rm vac}$ exactly and the Euler relation
survives the subtraction. The conserved-charge sums are
\begin{equation}
  n_j = n_{{\rm med},j} + \delta n_j, \qquad
  \nq = \sum_j n_j , \qquad
  \nB = \frac{\nq}{3} , \qquad
  \nC = \sum_j q_{f(j)} n_j , \qquad
  \nS = \sum_j s_{f(j)} n_j ,
  \label{eq:charges}
\end{equation}
and the colour densities that neutrality sets to zero,
\begin{equation}
  n_3 = \sum_f \big(n_{(f,r)} - n_{(f,g)}\big) , \qquad
  n_8 = \sum_f \big(n_{(f,r)} + n_{(f,g)} - 2 n_{(f,b)}\big) ,
  \label{eq:colourdens}
\end{equation}
these being the generator densities up to the constants $\tfrac12$ and
$\tfrac13$; a row that must vanish does not care about its normalisation.

\paragraph{Euler.} Eqs.~\eqref{eq:Omega}--\eqref{eq:epspair} give
\begin{equation}
  \varepsilon + P = T s + \sum_j \mu_j n_j
  \label{eq:euler}
\end{equation}
with the \emph{physical} mode potentials, since
$\sum_j \mu^{*}_j n_j + \Sigma_V \nq = \sum_j \mu_j n_j$. This is audited at
every solved point. Three assembly bugs found during development each produced
a plausible equation of state and each was caught here: a sign error in
$\varepsilon$ ($\mathcal{O}(1)$), the pairing cost and $\delta\Omega_{\rm pair}$
dropped from both sums ($7.7\times10^{-3}$, small enough to pass for
quadrature), and $\delta s$ dropped, which fails only at $T>0$.

\subsection{Leptons, photons and the untracked neutrinos}
\label{sec:leptons}

The leptons are added to the totals \emph{after} the matter and are not part of
the phase: they feel no field the quarks feel, they carry no colour, and a
phase does not own them --- which is why $\Omega$, $\varepsilon$ and $s$ of
Eqs.~\eqref{eq:Omega}--\eqref{eq:epspair} contain none of them and why the
phase-adapter block of Sec.~\ref{sec:adapter} does not either. \emph{Their
integrals are not cut}: the cutoff of Sec.~\ref{sec:medium} regularises the
four-fermion interaction, and a free lepton has none, so its momentum integral
runs to infinity.

One charged lepton $\ell$ of mass $m_\ell$ and degeneracy $g_\ell = 2$ at
potential $\mu_\ell$, with $E = \sqrt{k^2+m_\ell^2}$ and
$f^{\pm} = [1+e^{(E \mp \mu_\ell)/T}]^{-1}$:
\begin{align}
  n_\ell &= \frac{g_\ell}{2\pi^2}\int_0^{\infty}\!\! dk\, k^2 (f^{+} - f^{-}) ,
  &
  \varepsilon_\ell &= \frac{g_\ell}{2\pi^2}\int_0^{\infty}\!\! dk\, k^2 E\,(f^{+}+f^{-}) ,
  \label{eq:lepton1}\\
  P_\ell &= \frac{g_\ell}{2\pi^2}\int_0^{\infty}\!\! dk\, k^2\, T
    \Big[\ln\!\big(1+e^{-(E-\mu_\ell)/T}\big)
       + \ln\!\big(1+e^{-(E+\mu_\ell)/T}\big)\Big] ,
  &
  s_\ell &= \frac{\varepsilon_\ell + P_\ell - \mu_\ell n_\ell}{T} .
  \label{eq:lepton2}
\end{align}
These are the same integrands as Eqs.~\eqref{eq:medium1}--\eqref{eq:Plog} with
the upper limit at infinity, so their $T=0$ limit is
Eqs.~\eqref{eq:T0a}--\eqref{eq:T0b} with $k_F = \sqrt{\mu_\ell^2 - m_\ell^2}$
and no $\min$. They are evaluated by the JEL expansion of
\texttt{eos/general}, which is the one home the Fermi integrals of this
repository have; the closed forms above are what it approximates and what any
alternative implementation is checked against.

For a \emph{massless} species --- every neutrino here --- they close in
elementary functions, with $g_\nu = 1$ per flavour and antineutrinos included:
\begin{equation}
  n_\nu = \frac{g_\nu}{6\pi^2}\big(\mu_\nu^3 + \pi^2 \mu_\nu T^2\big) ,
  \quad
  P_\nu = \frac{g_\nu}{24\pi^2}\Big(\mu_\nu^4 + 2\pi^2\mu_\nu^2 T^2
          + \tfrac{7\pi^4}{15}T^4\Big) ,
  \quad
  \varepsilon_\nu = 3 P_\nu ,
  \quad
  s_\nu = \frac{g_\nu}{6}\Big(\mu_\nu^2 T + \tfrac{7\pi^2}{15}T^3\Big) ,
  \label{eq:neutrino}
\end{equation}
and the photon gas, massless with $g_\gamma = 2$ and $\mu_\gamma = 0$, is
Stefan--Boltzmann,
\begin{equation}
  P_\gamma = \frac{\pi^2}{45}T^4 , \qquad
  \varepsilon_\gamma = 3 P_\gamma , \qquad
  s_\gamma = \frac{4\pi^2}{45}T^3 , \qquad
  n_\gamma = \frac{2\zeta(3)}{\pi^2}T^3 .
  \label{eq:photon}
\end{equation}
Eq.~\eqref{eq:neutrino} at $\mu_\nu = 0$ gives $P_\nu/P_\gamma = 7/8$, which is
the check that the degeneracy is the one stated.

\paragraph{Which species are present, and at which potential.} Electrons
always, at
\begin{equation}
  \mu_e = \munue - \muC
  \label{eq:mue}
\end{equation}
in a beta-equilibrium mode --- Eq.~\eqref{eq:modemu} again, in the sign
convention $\muC = \mu_p - \mu_n$ --- and muons wherever the \texttt{muons}
flag allows them, at $\mu_\mu = \mu_e - \munue$, which is muon-decay
equilibrium $\mu^- \leftrightarrow e^- \bar\nu_e \nu_\mu$ with a transparent
muon family. The electron neutrino appears only when it is trapped,
$\munue \neq 0$; free streaming means $\munue = 0$ and then Eq.~\eqref{eq:neutrino}
gives it neither lepton number nor pressure, which is what free streaming is.
The \texttt{thermal\_neutrinos} flag adds the flavours \emph{not} tracked in
the composition as $\mu = 0$ gases: three of them when the electron neutrino
free-streams, two when it is trapped, since the trapped flavour is already
counted at its own potential. Photons follow the flag of the same name.

In a fixed-fraction mode with \texttt{leptons=True} none of this applies: there
$\muC$ is an unknown fixed by the charge condition, so the leptons are solved
\emph{after} the matter, from the one condition
$n_e + n_\mu = \nC$ at a single potential with $\mu_\mu = \mu_e$ (no neutrinos:
a fixed-$Y_C$ table is not a trapped one). Where the matter turns out
\emph{negatively} charged, $\nC \le 0$, there is nothing for electrons to
neutralize and the lepton blocks are empty with $\mu_e = 0$; positrons are not
added. With \texttt{leptons=False} the result is charged matter, which is what
a mixed-phase construction needs per pure phase.

\section{The solve}
\label{sec:solve}

\subsection{The unknown vector}

\begin{equation}
  x = \Big(\underbrace{M_u, M_d, M_s}_{\text{always}},\ \
      \underbrace{\{\Delta_\eta\}_{\eta \in \text{pattern}}}_{\text{free gaps}},\ \
      \underbrace{\mu_3, \mu_8}_{\text{if the pattern pairs}},\ \
      \underbrace{\Sigma_V}_{\text{if } \GV \neq 0},\ \
      \underbrace{\muB, \muC}_{\text{always}},\ \
      \underbrace{\muS}_{\text{if } Y_S \text{ held}},\ \
      \underbrace{\munue}_{\text{if } Y_{L_e} \text{ held}} \Big) .
\end{equation}

\subsection{The rows, in the order the solver assembles them}

\begin{align}
  &M_f - \big[m_f - 4\GS \phi_f + 2K \phi_g \phi_h\big] = 0 ,
   && f = u,d,s \quad \text{(3 rows)} \\
  &\frac{\Delta_\eta}{2\GD} - \mathrm{kernel}_\eta = 0 ,
   && \text{one per free gap, Eq.~\eqref{eq:gapeq}} \\
  &n_3 = 0 , \qquad n_8 = 0 ,
   && \text{if the pattern pairs, Eq.~\eqref{eq:colourdens}} \\
  &\Sigma_V - \frac{dW}{d\nq} = 0 ,
   && \text{if } \GV \neq 0 \\
  &\nB - \nB^{\rm target} = 0 ,
   && \text{always} \\
  &\nC - n_e - n_\mu = 0 \ \ \text{or}\ \ \nC - Y_C \nB = 0 ,
   && \text{neutrality, or the held charge} \\
  &\nS - Y_S \nB = 0 ,
   && \text{if } Y_S \text{ held} \\
  &(n_e + n_{\nu_e}) - Y_{L_e}\nB = 0 ,
   && \text{if } Y_{L_e} \text{ held.}
\end{align}
Each row is divided by the scale of the quantity it balances --- $\muB$ for a
potential, $\nB$ for a density, $(\muB/3)^3/\pi^2$ for a colour row --- and the
state is accepted when the largest scaled component is below
$10^{-10}$. Without that scaling the norm would be dominated by the colour
rows, which are MeV$^3$, by twenty orders of magnitude.

\subsection{The modes}

\begin{center}
\begin{tabular}{llll}
\toprule
mode & variables & extra unknowns & rows replaced \\
\midrule
\texttt{beta\_eq\_neutrinoless} & $(\nB,T)$ & --- & neutrality; $\muS = 0$ \\
\texttt{beta\_eq\_neutrino\_trapped} & $(\nB, Y_{L_e}, T)$ & $\munue$ & neutrality $+$ $Y_{L_e}$ \\
\texttt{fixed\_YC} & $(\nB, Y_C, T)$ & --- & $\nC = Y_C \nB$; $\muS = 0$ \\
\texttt{fixed\_YC\_YS} & $(\nB, Y_C, Y_S, T)$ & $\muS$ & $\nC = Y_C \nB$, $\nS = Y_S \nB$ \\
\bottomrule
\end{tabular}
\end{center}
In a fixed-fraction mode with \texttt{leptons=True} the neutralizing leptons are
solved \emph{after} the matter, at the single potential that makes the total
system neutral: they feel no field the matter feels and nothing about them
feeds back. With \texttt{leptons=False} the result is charged matter, which is
what a mixed-phase construction needs per pure phase. Wherever a temperature is
accepted, the entropy per baryon may be given instead, as an outer
one-dimensional solve for $T$.

\subsection{The pattern is not a mode}
\label{sec:enumeration}

Which condensates survive is decided by free energy, not declared. Every
enumerated candidate --- unpaired, 2SC, CFL, and one asymmetric free seed that
can land on uSC, dSC or an unequal-gap state --- is solved to
self-consistency, and the converged one with the lowest $f = \varepsilon - Ts$
is returned; at fixed $\muB$ (which is what the phase adapter does) the
criterion is the largest $P$ instead, and the two agree. Every point reports
the winner, the three gaps, $\mu_3$, $\mu_8$ and whether the state is gapless.

Two seeding facts. CFL is electrically neutral \emph{without} electrons, so its
seed puts $\muC$ at zero; seeded with an electron-bearing potential the solve
reaches a spurious point with an $11\%$ flavour-density spread. And in an
unpaired region $\mu_8$ is unconstrained --- $n_8$ vanishes identically at
$\mu_8 = 0$ --- so it is pinned there rather than solved for.

\paragraph{The residual handed to the root finder is dimensionless.} The rows
above span twenty orders of magnitude in their raw units --- mass rows in MeV
against gap and colour rows in MeV$^3$ --- and a root finder terminates on its
own view of the residual, not on the scaled one the gate is stated in. So each
row is divided by its scale \emph{before} the vector is handed over, with a
matching tolerance. Without that, every row but the largest is driven only as
far as the largest one needs, and a paired solve reports $10^{-8}$ where it can
reach $10^{-16}$; note in particular that a gap row is a \emph{density}, since
$\Delta_\eta/2\GD$ carries MeV$^3$, and judging it against a potential is four
orders of magnitude too strict.

\section{The public interface}
\label{sec:api}

Every entry point takes the parameter set first and none reaches for a default
one. \emph{Non-convergence is a return value}; a malformed \emph{call} still
raises, because that is a programming error a sampler would otherwise re-make a
million times.

\paragraph{\texttt{eos\_point(par, mode, species, n\_B, T | SnB, \dots)}}
returns a status object carrying \texttt{ok}, a \texttt{message} and the
\texttt{point}. Arguments and their legal values:

\begin{center}
\footnotesize
\begin{tabular}{p{0.20\textwidth}p{0.38\textwidth}p{0.34\textwidth}}
\toprule
argument & legal values & default \\
\midrule
\texttt{par} & any \texttt{Parameters} (Sec.~\ref{sec:parameters}) & required \\
\texttt{mode} & \texttt{beta\_eq\_neutrinoless},
  \texttt{beta\_eq\_neutrino\_trapped}, \texttt{fixed\_YC},
  \texttt{fixed\_YC\_YS} & required \\
\texttt{species} & a \texttt{SpeciesFlags} & all flags \texttt{False} \\
\texttt{n\_B} & $>0$, fm$^{-3}$ & required \\
\texttt{T} / \texttt{SnB} & exactly one: $T \ge 0$ in MeV, or entropy per
  baryon (an outer solve for $T$ bracketed over $[0.2,400]$ MeV) & --- \\
\texttt{leptons} & \texttt{True}/\texttt{False}/\texttt{None}; on a
  beta-equilibrium mode \texttt{True} is ignored and \texttt{False} raises &
  \texttt{None}, i.e.\ \texttt{False} \\
\texttt{x0} & a converged \texttt{point.x}, or a $\{$pattern$\to$vector$\}$
  mapping & \texttt{None} (cold) \\
\texttt{patterns} & any of \texttt{unpaired}, \texttt{2SC}, \texttt{uSC},
  \texttt{dSC}, \texttt{CFL}, \texttt{free} &
  the enumeration of Sec.~\ref{sec:enumeration} \\
\texttt{backend} & \texttt{reference}, \texttt{fast} & \texttt{reference} \\
\texttt{pair\_nodes\_per\_panel} & $\ge 1$ & 24 \\
conditions & $Y_C$, $Y_S$, $Y_{L_e}$ as the mode requires; $Y_{L_\mu}$ raises
  & --- \\
\bottomrule
\end{tabular}
\end{center}

A pattern restriction is not a guarantee: a pattern declares which gaps are
\emph{free}, and a free gap may come out zero, so a \texttt{CFL}-restricted
call can return a 2SC state. The point names both, the layout it was solved in
and the pattern its gaps realise.

\paragraph{\texttt{eos\_table(par, mode, species, axes, \dots)}} solves a grid
over $\{\nB\}\times\{T$ or $S/B\}$ and, optionally, over the fractions the mode
fixes. The density axis is warm-started and a missed step is bisected back
towards the last solved point up to six times, which is what carries the sweep
across the strange-quark onset and the pairing onset. It reports through the
repository's progress callback --- \texttt{\{mode, line, n\_lines, temp\_key,
temp, fracs, n\_solved, n\_requested, elapsed\_s\}} --- plus one key this model
adds, \texttt{pattern}. The row it emits is Sec.~\ref{sec:tablerow}.

\paragraph{\texttt{eos\_response(par, mode, species, frozen, \dots)}} returns
the second derivatives of Sec.~\ref{sec:responses}. One freeze is implemented,
\texttt{equilibrium}; anything else raises naming what is missing.

\paragraph{\texttt{zero\_pressure\_point(par, species, \dots)}} locates the
self-bound surface of Sec.~\ref{sec:zeropressure}.

\paragraph{The layers below.} \texttt{solve} is the enumeration without the
argument validation; \texttt{solve\_pattern} is one mode in one declared
pattern; \texttt{thermo\_from\_mu} is the phase-adapter surface of
Sec.~\ref{sec:adapter}; \texttt{state\_at} evaluates one state with no
equilibrium condition imposed; \texttt{vacuum\_solution} and
\texttt{bag\_constant} return the vacuum of Sec.~\ref{sec:vacuum} and the
$B_{\rm eff}$ of Sec.~\ref{sec:bagconstants}.

\section{What a solved point returns}
\label{sec:returns}

\subsection{The point}

Every entry below is fm-based: densities in fm$^{-3}$, potentials and masses in
MeV, $P$, $\varepsilon$ and $f$ in MeV\,fm$^{-3}$, $s$ in fm$^{-3}$.

\begin{center}
\begin{tabular}{@{}l p{0.63\textwidth}@{}}
\toprule
field & what it is \\
\midrule
\texttt{converged}, \texttt{error}
  & the status, and the largest \emph{scaled} row of
    Sec.~\ref{sec:solve}. \textbf{Test it first}: when \texttt{converged} is
    false every other field is the best iterate reached, not a state \\
\texttt{mode}
  & which of the four equilibria was closed \\
\midrule
$\nB$, $T$
  & the independent variables \\
$Y_C = \nC/\nB$, $Y_S = \nS/\nB$
  & the non-leptonic charge and strangeness fractions,
    imposed in a fixed-fraction mode and an outcome in a beta-equilibrium one \\
$Y_{L_e} = (n_e + n_{\nu_e})/\nB$
  & the electron-family lepton fraction \\
\midrule
\texttt{pattern}, \texttt{gapless}
  & which candidate of Sec.~\ref{sec:enumeration} won, and whether a
    quasiparticle branch has reached zero. Both are part of the answer, not
    diagnostics \\
$\Delta_1, \Delta_2, \Delta_3$
  & the gaps [MeV], zero in whichever channels the winning pattern does not
    pair \\
$M_u, M_d, M_s$
  & the constituent masses [MeV] of Eq.~\eqref{eq:massgap} \\
\midrule
$\muB, \muC, \muS, \mu_3, \mu_8$
  & the conserved-charge and colour potentials [MeV] \\
$\mu_e, \mu_{\nu_e}$
  & the lepton potentials of Sec.~\ref{sec:leptons} \\
\midrule
$n_u, n_d, n_s$
  & the three \emph{flavour} densities, $n_f = \sum_a n_{(f,a)}$ \\
$n_e, n_\mu, n_{\nu_e}$
  & the lepton densities \\
$Y_u, Y_d, Y_s, Y_e, Y_{\nu_e}$
  & the same divided by $\nB$ \\
\midrule
$P$, $\varepsilon$, $s$
  & the totals: matter (Eqs.~\eqref{eq:Omega}--\eqref{eq:epspair}) plus
    leptons plus the thermal gases (Sec.~\ref{sec:leptons}) \\
$f = \varepsilon - T s$
  & the free-energy density, and the quantity the pattern enumeration ranks
    candidates by \\
\midrule
\texttt{\_state}, \texttt{x}
  & the matter block, and the converged unknown vector --- which is what a
    warm start is. \texttt{\_state} is INTERNAL and in natural units
    [MeV$^n$]: the leading underscore says it is not on the fm-based boundary
    this section opens with. Everything a caller needs is on the point above,
    in fm; \texttt{\_state.euler\_residual()} is dimensionless and is why the
    block stays reachable \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{$n_s$ is the strange-quark density, not a scalar density.} Every
other model in this repository returns a field called $n_s$ meaning the
\emph{scalar} density, computed through the trace identity
$n_s = (\varepsilon - 3P)/m^{*}$. \textbf{Here it is the number density of $s$
quarks}, the third entry of the flavour triplet $(n_u, n_d, n_s)$, and the
collision is a real one: a caller moving from a hadronic model to this one and
reading $n_s$ as a scalar density gets a plausible number that means something
else. This model's scalar densities are per flavour, are called $\rho_{s,f}$,
and are not returned on the point at all --- they enter through the condensates
$\phi_f$ of Eq.~\eqref{eq:phi}, which are.

The trace identity itself does not survive here, and not for want of care:
$\varepsilon - 3P = M \rho_s$ holds mode by mode for the \emph{medium} pieces
alone and with $P_{k^4}$ rather than $P_{\log}$, and the assembled
$\varepsilon$ and $P$ of Eqs.~\eqref{eq:Omega}--\eqref{eq:eps} additionally
carry the Dirac sea, the condensate cost $C$, the vector terms and the pairing
correction, each entering the two with different weight. So $\rho_{s,f}$ is
integrated from Eq.~\eqref{eq:medium1} and corrected by
Eq.~\eqref{eq:drhos}, and the trace identity is not used as a definition
anywhere in this model.

\paragraph{$s$ likewise is integrated, not divided.} The general identity
\begin{equation}
  s = \frac{\varepsilon + P - \sum_j \mu_j n_j}{T}
  \label{eq:sident}
\end{equation}
is Eq.~\eqref{eq:euler} rearranged and it holds exactly, but it is used as an
\emph{audit} rather than as the definition. $s$ comes from the entropy
integrand of Eq.~\eqref{eq:medium2} plus the pairing correction
Eq.~\eqref{eq:ds}, because Eq.~\eqref{eq:sident} is a difference of three
numbers of order $10^9$ divided by $T$, and in a cold nearly-degenerate gas ---
or in a fully gapped phase, where $s$ is genuinely $e^{-\Delta/T}$ small ---
the cancellation eats every significant digit. Integrating costs one array in
a pass already being made and leaves Euler available as the check it is used as.

\subsection{The table row}
\label{sec:tablerow}

\texttt{eos\_table} sweeps the density axis with a warm start, bisecting a
missed step back towards the last solved point --- which is what carries it
across the two thresholds a cold NJL density axis has, the strange quark's
onset and the pairing onset. Its rows carry
$\nB$, $T$, $P$, $\varepsilon$, $s$, $s/\nB$, $\muB$, $\muC$, $\muS$, $\mu_e$,
$Y_C$, $Y_S$, $Y_u$, $Y_d$, $Y_s$, $Y_e$, $Y_{\mu^-}$, $M_u$, $M_d$, $M_s$, the
pattern, the gapless flag, the three gaps, $\mu_3$ and $\mu_8$, plus
$Y_{\nu_e}$ and $\mu_{\nu_e}$ where the mode traps. Two further columns exist
so that a quark table and a hadronic one concatenate without renaming:
$\chi = 1$ and \texttt{phase = "Q"}, saying that the matter is entirely
deconfined.

The progress callback is the repository's, one dictionary shape for every
model, invoked once per completed line: \texttt{mode}, \texttt{line},
\texttt{n\_lines}, \texttt{temp\_key}, \texttt{temp}, \texttt{fracs},
\texttt{n\_solved}, \texttt{n\_requested}, \texttt{elapsed\_s} --- plus one key
this model adds, \texttt{pattern}, because which phase a line ended in is the
first thing a reader of a colour-superconducting table wants to know.

\subsection{The response functions}
\label{sec:responses}

A second derivative is defined only once one says what is held fixed, so
\texttt{eos\_response} names its conditioning. \emph{One freeze is
implemented}, \texttt{equilibrium}: nothing is held, the composition
re-equilibrates under the perturbation, and so does the pairing pattern ---
every neighbour of the stencil re-runs the enumeration of
Sec.~\ref{sec:enumeration}. A caller wanting the derivative \emph{within} one
pattern restricts the enumeration instead. Anything else --- holding the
species fractions, holding $Y_C$, holding the gaps, or the susceptibility
matrix $\chi_{ab} = \partial n_a/\partial\mu_b$ --- raises saying so, and is
recorded in \texttt{docs/DEFERRED.md}.

Everything is a central difference along a re-solved sequence of the mode's own
equilibrium, with $\sigma = s/\nB$ the entropy \emph{per baryon}
\cite{TypelCompOSE2015}:
\begin{align}
  c_{s,\rm iso}^2 &= \frac{(\partial P/\partial \nB)_T}
                          {(\partial \varepsilon/\partial \nB)_T} ,
  &
  C_V &= T \left(\frac{\partial \sigma}{\partial T}\right)_{\nB} ,
  \label{eq:resp1}\\
  C_P &= T\left[ \left(\frac{\partial \sigma}{\partial T}\right)_{\nB}
        - \left(\frac{\partial P}{\partial T}\right)_{\nB}
          \frac{(\partial \sigma/\partial \nB)_T}{(\partial P/\partial \nB)_T}
        \right] ,
  &
  c_{s,\rm ad}^2 &= \frac{C_P}{C_V}\, c_{s,\rm iso}^2 ,
  \label{eq:resp2}\\
  \Gamma_{\rm th} &= 1 + \frac{P(\nB,T) - P(\nB,0)}
                              {\varepsilon(\nB,T) - \varepsilon(\nB,0)} .
  &&
  \label{eq:resp3}
\end{align}
$C_P$ is the usual Jacobian rotation, holding the pressure by letting the
density move with the temperature. Both sound speeds are named for the thermal
variable they hold and never returned as a bare $c_s^2$; at $T = 0$ the ratio
$C_P/C_V$ is one by construction and the two coincide, so only
$c_{s,\rm iso}^2$ and $c_{s,\rm ad}^2$ come back there. $\Gamma_{\rm th}$
returns \texttt{nan} rather than a signed nonsense where the cold reference is
not below the hot one.

In a fully gapped phase $C_V$ is exponentially small at low $T$, because the
paired entropy is: that suppression is real physics, not a numerical failure,
and it is what makes a colour superconductor cool differently from unpaired
quark matter. The asymptotics of $c_s^2$ are the point of the vector sector of
Sec.~\ref{sec:vector}; pairing does not change them, since
$c_s^2 \to \tfrac13 + \tfrac29 \Delta^2/\mu^2$ dies as $1/\mu^2$.

\section{The phase-adapter surface}
\label{sec:adapter}

\texttt{eos/mixed} consumes this model through one function: given
$(\muB, \muC, \muS, T)$ it returns the phase block, having closed the model's
own internal system --- masses, gaps, $\Sigma_V$ \emph{and the two colour
potentials}. Colour neutrality is internal because $\mu_3$ and $\mu_8$ are not
conserved charges of the mixed system: no hadronic phase carries them and there
is nothing across the interface for them to equilibrate with. The slot carries
the \emph{physical} baryon potential, and the seed is not cacheable: the seed
chooses the root, so caching it would change physics rather than speed.

The rows it drives to zero are Sec.~\ref{sec:solve}'s \emph{without} the
density row and without the mode's charge rows, since the three potentials are
given rather than solved for:
\begin{equation}
  x_{\rm int} = \Big( M_u, M_d, M_s,\ \{\Delta_\eta\}_{\rm free},\
  (\mu_3, \mu_8)_{\rm if\ paired},\ (\Sigma_V)_{\rm if\ \GV \neq 0} \Big) ,
\end{equation}
with the three mass equations, one gap equation per free gap, $n_3 = n_8 = 0$
where the pattern pairs, and $\Sigma_V = dW/d\nq$ where there is a vector
coupling --- in that order, scaled as in Sec.~\ref{sec:solve}. The block
handed back carries $T$, $\muB$, $\muC$, $\muS$; the three flavour densities
$n_u, n_d, n_s$; the flavour potentials
$\mu_f = \muB/3 + q_f \muC + s_f \muS$ and their effective partners
$\mu_f - \Sigma_V$; the effective masses $M_f$; $\nB$, $\nC$, $\nS$; $P$,
$\varepsilon$, $s$ and $\sum_j \mu_j n_j$; and, as declared fields, the three
masses, the three gaps, $\mu_3$, $\mu_8$ and $\Sigma_V$. The adapter
enumerates the patterns at every call and keeps the one with the largest $P$,
which at fixed potentials is the stable one; the \emph{label} of that winner
is not one of the declared fields --- it comes back as the key of the
warm-start mapping the adapter returns alongside the block, and the gaps in the
block are what say whether and how the phase paired. No lepton enters it, by
Sec.~\ref{sec:leptons}. There is no thermo-at-given-\emph{densities} surface,
so a mixed-phase response that would need one raises
(Sec.~\ref{sec:notimplemented}).

\section{What the implementation reproduces}
\label{sec:reproduces}

\begin{center}
\begin{tabular}{lll}
\toprule
quantity & computed & reference \\
\midrule
$M_u$ (vacuum) & $367.648$ MeV & $367.7$~\cite{Rehberg1996} \\
$M_s$ (vacuum) & $549.479$ MeV & $549.5$ \\
$(-\phi_u)^{1/3}$ & $241.946$ MeV & $241.9$ \\
$(-\phi_s)^{1/3}$ & $257.688$ MeV & $257.7$ \\
$f_\pi$ & $92.391$ MeV & $92.4$ \\
2SC gap at $\mu^{*}=450$ MeV, $\eta_D = 0.75$ & $92.71$ MeV & --- \\
Clogston ratio $\delta\mu_c/(\Delta_0/\sqrt2)$ & $0.970$ & $1$ (weak coupling) \\
\midrule
\multicolumn{3}{l}{\emph{Neutral solved points at $\muB = 1500$ MeV, $T = 0$,
$\eta_D = 0.75$:}} \\
\multicolumn{3}{@{}p{0.92\textwidth}@{}}{unpaired:
  $M = (9.84, 8.55, 265.59)$ MeV, $\muC = -34.20$ MeV,
  $\nB = 1.4319$ fm$^{-3}$, $P = 302.12$ MeV\,fm$^{-3}$} \\
\multicolumn{3}{@{}p{0.92\textwidth}@{}}{2SC:
  $\Delta_3 = 95.50$ MeV, $\mu_3 = 0$, $\mu_8 = -2.46$ MeV,
  $M_s = 243.13$ MeV, $\muC = -62.27$ MeV, $\nB = 1.4887$ fm$^{-3}$,
  $P = 324.75$ MeV\,fm$^{-3}$} \\
\bottomrule
\end{tabular}
\end{center}

\subsection{A second published vacuum, as an independent check}

The Hatsuda--Kunihiro set ($\Lam = 631.4$ MeV, $\GS\Lam^2 = 1.835$,
$K\Lam^5 = 9.29$, $m_{u,d} = 5.5$ MeV, $m_s = 135.7$ MeV) is not shipped as a
named set, but the same code run at it gives $M_{u,d} = 335.5$ MeV and
$M_s = 528.1$ MeV against the $336$ and $528$ quoted in
Ref.~\cite{Baym2018}. Two independent vacuum fits reproduced by one gap
equation is a stronger statement than one.

\subsection{The three published ``bag constants'', and how they differ}
\label{sec:bagconstants}

This is the single most-mixed-up number in the NJL compact-star literature.
Three quantities are all called $B$; all three are reported by this
implementation, and they differ by well-defined terms:
\begin{align}
  B_{\rm eff} &= \Omega[M_f = m_f] - \Omega[M_f = M_f^{\rm vac}] ,
  \label{eq:Beff2}\\
  B_{\rm sea} &= B_{\rm eff} - C(m_f) ,
  \label{eq:Bsea}\\
  B_0 &= -\Omega_{\rm vac} = P_{\rm NJL}(\mu = T = 0) ,
  \label{eq:B0}
\end{align}
where $C(m_f)$ is the condensate cost of Eq.~\eqref{eq:C}
evaluated at the \emph{current} masses.

\begin{center}
\footnotesize
\begin{tabular}{lccc p{0.34\textwidth}}
\toprule
set & $B_{\rm eff}^{1/4}$ & $B_{\rm sea}^{1/4}$ & $B_0^{1/4}$ & published \\
\midrule
RKH & $228.93$ & $\mathbf{217.59}$ & $\mathbf{425.44}$ &
  $217.6$ MeV (Schertler \textit{et al.}, quoted in Ref.~\cite{Pagliara2008});
  $B_0 = 425.4$ MeV (Ref.~\cite{Pagliara2008}) \\
HK  & $230.00$ & $\mathbf{218.28}$ & $444.50$ &
  $(218\,\text{MeV})^4 = 296$ MeV\,fm$^{-3}$ (Ref.~\cite{Baym2018}, Eq.~57) \\
\bottomrule
\end{tabular}
\end{center}
(all entries in MeV.)

So the numbers quoted as ``the NJL bag constant'' by Refs.~\cite{Baym2018}
and~\cite{Pagliara2008} are $B_{\rm sea}$, and this implementation reproduces
both to the digits they print. The function \texttt{bag\_constant} returns
$B_{\rm eff}$, larger by $C(m_f)$ --- $22\%$ in MeV$^4$, $5\%$ in
MeV --- and $B_{\rm eff}^{1/4} = 228.93$ MeV $= 357.49$ MeV\,fm$^{-3}$ is what
the colour-dielectric companion model quotes its own $B_g + B_\chi$ against.
\emph{None of the three is a free parameter}: all are outputs of the vacuum
fit, and $B_0$ in particular is nothing but the vacuum subtraction
$\Omega \to \Omega - \Omega_{\rm vac}$ written as an additive constant. Papers
that \emph{add} a further bag constant by hand shift the deconfinement onset
relative to a hadronic model; they do not fix the NJL normalisation
(Sec.~\ref{sec:literature}).

\subsection{One deliberate discrepancy}

One deliberate discrepancy is recorded rather than reconciled. The
implementation specification reports $M_u, M_d = (11.96, 7.65)$ MeV for the
2SC point above and this implementation gives $(9.73, 8.90)$. The difference is
the sign of the hole amplitudes in the paired scalar density $\delta\rho_{s,f}$
of Eq.~\eqref{eq:drhos}, and the identity $\nB = dP/d\muB$ along the neutral
solution decides it: it holds to the finite-difference floor with the sign used
here and fails by $2.6\times10^{-4}$ with the other. Near chiral restoration
$M-m$ is the small difference of two large numbers, which is why a
percent-level change in one scalar density moves the light masses by twenty
percent and moves $M_s$, $\Delta_3$, $\mu_8$, $\muC$, $\nB$ and $P$ not at all.

\section{This model against the published NJLs}
\label{sec:literature}

Every model below is the Lagrangian of Sec.~\ref{sec:lagrangian}, or a stated
subset of it. The differences sit in four places: which channels are switched
on, how the ultraviolet is regularized, how the vacuum constant is fixed and
whether a further bag constant is added by hand, and whether the pairing
pattern and the neutrality conditions are solved or declared.

\begin{center}
\footnotesize
\begin{tabular}{p{0.14\textwidth}p{0.10\textwidth}p{0.11\textwidth}p{0.09\textwidth}p{0.14\textwidth}p{0.09\textwidth}p{0.18\textwidth}}
\toprule
 & $\GS$, $K$ & diquark & vector & cutoff & $K'$ & counterpart here \\
\midrule
\textbf{\texttt{eos/njl}} & RKH & $\eta_D$ free & three forms &
  RG-consistent, $\lambda = 10$ & omitted & --- \\
Rehberg \textit{et al.}~\cite{Rehberg1996} & \emph{the source} & none & none &
  sharp $\Lam$ & --- & \texttt{Parameters.default()} \\
R\"uster \textit{et al.}~\cite{Ruester2005} & RKH & varied about $3/4$ & none &
  sharp $\Lam$ & --- & \texttt{csc}, $\lambda = 1$ \\
Pagliara--Schaffner-Bielich~\cite{Pagliara2008} & RKH, identical &
  $\GD = 1.0,\,1.2\,\GS$ & $\GV = 0,\,0.2\,\GS$ & sharp $\Lam$ & --- &
  $\eta_D$, $\eta_V$, $\lambda = 1$ \\
Logoteta \textit{et al.}~\cite{Logoteta2012} & RKH, identical & \emph{none} &
  \emph{none} & sharp $\Lam$ & --- &
  \texttt{csc=False}, $\eta_V = 0$, $\lambda = 1$ \\
Lugones~\cite{Lugones2016} (review) & model-dependent & --- & --- & --- & --- &
  bulk only; no finite-size terms \\
Baym \textit{et al.}~\cite{Baym2018} & HK, $\Lam = 631.4$ & $H/G = 1.4$--$1.6$
  & $g_V/G = 0.5$--$1.0$, constant & sharp $\Lam$ & written, set to $0$ &
  $\lambda = 1$, HK numbers \\
Gholami \textit{et al.}~\cite{Gholami2024} & RKH & $\eta_D = 1$; sets 1--3 &
  $\eta_V$ & RG-consistent, massless & --- & \emph{the shipped default} \\
Ivanytskyi~\cite{Ivanytskyi2025} & nonlocal, Gaussian & $\eta_D = 0.27$--$0.44$
  & $\eta_V$, nonlocal & \emph{no cutoff} & --- & none; see below \\
Kunkel \textit{et al.}~\cite{Kunkel2026} & RKH & $\eta_D = 1.45$ &
  $\eta_V = 0.7$ & RG-consistent, massless & --- &
  \texttt{Parameters.\allowbreak named("rg\_njl1")} \\
MUSES NJL module~\cite{MUSESNJL} & RKH & configurable & configurable &
  RG-consistent, four schemes & --- & our scheme $\equiv$ its
  \texttt{analytic} \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Rehberg, Klevansky and H\"ufner~\cite{Rehberg1996}.} The parameter
set is theirs and is reproduced digit for digit
(Sec.~\ref{sec:reproduces}). Their model has no diquark and no vector channel.
Their mass equation is Eq.~\eqref{eq:massgap} unchanged.

\paragraph{R\"uster \textit{et al.}~\cite{Ruester2005}.} The source of two
things this implementation takes literally: the Appendix~A quasiparticle
spectrum of Sec.~\ref{sec:bdg}, and the gap-index convention
$\Delta_{1,2,3} = ds,\,us,\,ud$. Their $\mu_8$ is the halved Gell-Mann one,
$\mu_8^{\rm theirs} = 1.1547\,\mu_8^{\rm ours}$. They work at fixed $\mu$ and
$T$ and map a phase diagram where we solve at fixed $\nB$ and rank by free
energy --- the same criterion read on the conjugate axis.

\paragraph{Pagliara and Schaffner-Bielich~\cite{Pagliara2008}.} The closest
published match to this implementation at $\lambda = 1$: the same RKH
parameters, the same dispersion relations, the same six gap equations and the
same three neutrality conditions $n_Q = n_3 = n_8 = 0$, plus a vector term
$\omega_0 = 2\GV\langle\psi^\dagger\psi\rangle$ shifting
$\mu \to \mu - \omega_0$, which is the constant form of
Sec.~\ref{sec:vector}. Their pressure,
\begin{equation}
  p = \frac{1}{2\pi^2}\sum_{i=1}^{18}\int_0^{\Lam}\!\! dk\,k^2|\epsilon_i|
    + 4K\sigma_u\sigma_d\sigma_s - \frac{1}{4\GD}\sum_c |\Delta_c|^2
    - 2\GS\sum_\alpha \sigma_\alpha^2 + \frac{\omega_0^2}{4\GV} + p_e ,
\end{equation}
is $P = -\Omega$ of Sec.~\ref{sec:totals} term by term: their $-D$, their $-C$
(the $-2\GS\sum\sigma^2 + 4K\sigma\sigma\sigma$ pair), their
$\omega_0^2/4\GV = -(\Sigma_V \nq - W)$ at constant $\GV$, and their
$\sum_i|\epsilon_i|$ integral is our spectrum sum \emph{without} the unpaired
subtraction. The two are algebraically the same and numerically not: the
difference form is what makes $\delta\Omega_{\rm pair}$ vanish identically at
$\Delta = 0$. Their two bag constants are reproduced exactly
(Sec.~\ref{sec:bagconstants}). Expected differences: their $B^{*}$, which makes
deconfinement coincide with chiral restoration, is a \emph{hadronic matching}
choice with no counterpart here; and they use only the eighteen on-shell
eigenvalues, whose cost in CFL is measured in Sec.~\ref{sec:bdg}.

\paragraph{Logoteta \textit{et al.}~\cite{Logoteta2012}.} The nucleation paper:
the same RKH set, the same gap equation, and \emph{neither} colour
superconductivity \emph{nor} a vector channel. Their effective bag pressure
deserves care, because it is not a constant and not the $B_{\rm eff}$ of
Eq.~\eqref{eq:Beff2}. They write $\varepsilon = \sum_i \varepsilon_{{\rm kin},i}
+ B_{\rm eff}$ with $B_{\rm eff} = B_0 - B$ and
$B = \sum_f \varepsilon_{\rm sea}(M_f) - C$, so that
\begin{equation}
  B_{\rm eff}^{\rm Logoteta}
   = \Big[-\sum_f \varepsilon_{\rm sea}(M_f) + C\Big] - \varepsilon_{\rm vac} ,
\end{equation}
which is exactly the pair of terms our $\varepsilon$ of Sec.~\ref{sec:totals}
carries. It is therefore \emph{density-dependent} --- they plot it running from
$90$ to $195$ MeV\,fm$^{-3}$ --- and coincides with \texttt{bag\_constant} only
in the chirally restored limit $M_f \to m_f$. Their $Q^{*}$ phase, quark matter
frozen at the strangeness content of the hadronic phase it nucleated out of, is
a \texttt{fixed\_YC\_YS} call here and not a beta-equilibrium one.

\paragraph{Lugones~\cite{Lugones2016}.} A review of deconfinement nucleation:
quark drops, finite-size effects, surface and curvature terms. \emph{None of
the finite-size physics is in this model, by design} --- it is a bulk
homogeneous equation of state, so surface tension, curvature energy and the
colour-Debye screening of a finite drop are the caller's, and the model
supplies the bulk $\Omega$ they correct. The relevant entry point for a
nucleation study is \texttt{thermo\_from\_mu} (Sec.~\ref{sec:adapter}) together
with a \texttt{fixed\_YC\_YS} point for the flavour-conserving transient.

\paragraph{Baym \textit{et al.}~\cite{Baym2018}.} The QHC18 review, and the
paper that puts the 't~Hooft--diquark cross-term on the record:
\begin{equation}
  M_i = m_i - 4G\sigma_i + K|\epsilon_{ijk}|\sigma_j\sigma_k
        + \frac{K'}{4}|d_i|^2 , \qquad
  \Delta_k = -2d_k\Big(H - \frac{K'}{4}\sigma_i\Big) .
\end{equation}
Their $K|\epsilon_{ijk}|\sigma_j\sigma_k$ is our $2K\phi_j\phi_k$ once the
double sum is written out --- the same term, not a factor-two disagreement.
The genuinely new pieces are the $K'$ terms, and \emph{they set $K' = 0$ in
every published QHC18 result}, on the argument that its effect is absorbed into
$g_V$ and $H$. That is precisely the position taken here, and it is why
$\eta_D$ must be read as an effective coupling. Other differences, all
expected: the HK parameter set (Sec.~\ref{sec:reproduces} shows the same code
reproduces their vacuum masses at it); couplings $H/G \simeq 1.4$--$1.6$ and
$g_V/G = 0.5$--$1.0$, inside which \texttt{rg\_njl1}'s $\eta_D = 1.45$ sits; a
sharp cutoff throughout, with the model used only for $\nB \gtrsim 5n_0$ and
interpolated to APR below, which is a \emph{different} way of avoiding the
artifacts Sec.~\ref{sec:rg} removes --- they stay away from the region where
the cutoff bites, we push the cutoff away; a constant $g_V$ only; and the
$\lambda_8$ convention, so $\mu_8^{\rm ours} = \sqrt3\,\mu_8^{\rm theirs}$.
QHC18 is moreover a polynomial interpolation in $\muB$ between APR and NJL, not
an NJL equation of state; this model returns the NJL branch itself.

\paragraph{Gholami, Hofmann and Buballa~\cite{Gholami2024}.} The model this
implementation defaults to. Their Eq.~(C7) is the counterterm shape
\eqref{eq:countertermshape}, with their per-gap coefficient $2/\pi^2$ equal to our
per-pair $1/\pi^2$ summed over the two colour pairs; their
$\lambda \gtrsim 5$ convergence and their $\lambda = 10$ choice are ours, and
their ``below $1\%$ between $\lambda = 10$ and $20$'' is our measured
$1.4$--$1.9\times10^{-3}$. Two of their conclusions are adopted rather than
re-derived: the \emph{massive} scheme must not be used, since it inverts the
gap ordering to $\Delta_3 < \Delta_1 = \Delta_2$ and predicts the wrong melting
pattern; and the RG-consistent phase diagram melts CFL in a dSC pattern where
the sharp-cutoff one melts it in a uSC pattern --- which is why the asymmetric
\texttt{free} seed is in the enumeration of Sec.~\ref{sec:enumeration}. Two
things their prose does not state and that measurement forced are recorded in
Sec.~\ref{sec:rg}: the vacuum piece of the split is taken at the same
condensates \emph{gaps included}, and the pairing panels must follow
$\Lam_{\rm UV}$.

\paragraph{Ivanytskyi~\cite{Ivanytskyi2025}.} A \emph{nonlocal} three-flavour
NJL: the contact currents are smeared by a Gaussian form factor
$g_k = \exp(-k^2/\Lam^2)$ with $\Lam = 564$ MeV, which makes every momentum
integral converge and removes the cutoff from the model entirely. This is the
\emph{other} published cure for the disease Sec.~\ref{sec:rg} treats. His
Lagrangian is not ours --- no determinant term, mass-degenerate flavours
(``CFLL''), nonlocal vertices, and neutrality automatic because degenerate
masses give equal densities at one common $\mu$ --- so no parameter set of this
model reproduces his numbers, and the comparison is one of \emph{behaviour}.
Where the two agree it matters: he shows that a \emph{local} vector channel
gives $\omega_\infty \to -\mu + (3\pi^2\mu/\GV d)^{1/3}$, hence
$p = \mu^2/4\GV$ and $c_s^2 = 1$ --- exactly the Zel'dovich runaway
Sec.~\ref{sec:vector} identifies for constant $\GV$ --- and that a local
diquark channel gives a divergent gap and $c_s^2 = 1/5$. Our density-dependent
$\GV$ and his momentum-dependent form factor are two ways of suppressing the
same interaction at large momentum, and they reach the same conformal limit;
the difference is that he puts the suppression in the vertex while we put it in
the coupling's density dependence, which keeps the model local and keeps the
RKH vacuum fit intact.

\paragraph{Kunkel \textit{et al.}~\cite{Kunkel2026}.} Proto\-neutron\-star
evolution with colour superconductivity, and the paper
\texttt{Parameters.named("kunkel")} is named for. Their Lagrangian, their mass
equation $M_\alpha = m_\alpha - 4\GS\sigma_\alpha + 2K\sigma_\beta\sigma_\gamma$
and their gap indexing are ours; their regularization is the sharp
$\Lam' = 602.3$ MeV vacuum with the RG-consistent massless scheme in medium,
and their couplings are $\GD = 1.45\,\GS$, $\GV = 0.7\,\GS$. That is
\texttt{rg\_njl1} at $\lambda = 10$, and it is the same model. Their lepton
treatment matches ours in an unusual detail: muons in the neutrino-transparent
equation of state and \emph{no} fixed muon lepton fraction in the trapped one,
which is exactly the \texttt{beta\_eq\_neutrino\_trapped} implemented here ---
a shared deferral rather than a divergence. Expected differences: they add a
bag constant by hand, $B = 10$ MeV\,fm$^{-3}$, chosen so that a 2SC phase
exists at $T = 0$ when matched to DD2, which is a knob on the \emph{hybrid}
construction and not part of the quark equation of state; they work at fixed
$(T,\muB,\mu_Q)$ and interpolate to fixed $Y_{L_e}$ and fixed $s/\nB$
afterwards, where this model solves at those variables directly; their printed
$\mu_8$ convention is the halved one while their code is the full $\lambda_8$;
and their 2SC--CFL mixed region is a linear volume-fraction interpolation,
where this model returns the single lowest-free-energy phase and leaves any
two-quark-phase mixture to the caller. Reproduced from their paper: the $T = 0$
2SC~$\to$~CFL density jump, $3.7\,n_0 \to 4.7\,n_0$.

\paragraph{The MUSES NJL module~\cite{MUSESNJL}.} The authors' own code, and
what Refs.~\cite{Gholami2024} and~\cite{Kunkel2026} are computed with. This
implementation has been checked against it directly, and four traps in that
comparison are recorded because each makes a mismatch look like a physics
disagreement when it is not: its \texttt{RG\_scheme} defaults to
\texttt{minimal}, which keeps only the leading logarithm and is $5\%$ off at
$\Delta = 250$ MeV, while the closed form used here is its \texttt{analytic};
its $\Omega$ includes the leptons and ours does not; its $n$ is the quark
density, $\nB = n/3$; and its $\mu_8$ is the full-$\lambda_8$ convention. That
comparison is also what found the on-shell-reduction defect of
Sec.~\ref{sec:bdg}: the 2SC sector always matched to $10^{-4}$ while the CFL
pressure was $6$--$16\%$ high, because the error is controlled by the paired
pair's mass mismatch --- zero for $u$--$d$, MeV for anything with $s$ --- so it
hid in 2SC and broke CFL.

\section{What is not implemented}
\label{sec:notimplemented}

Recorded in \texttt{docs/DEFERRED.md} rather than approximated:
the 't~Hooft--diquark cross-term $K'$ of
Sec.~\ref{sec:lagrangian}; the trapped muon lepton family as a conserved
charge; the composition and gap freezes of \texttt{eos\_response}; and the
dilaton/colour-dielectric graft, which the specification marks as unverified
for transition order, pairing coexistence and finite temperature.

\section{The self-bound surface and the two-flavour arm}
\label{sec:zeropressure}

\subsection{The quantity}

A parametrization whose pressure crosses zero at finite density describes
\emph{self-bound} matter: the phase ends there, with no crust below it. What is
reported at that endpoint is the energy per baryon,
%
\begin{equation}
  \frac{E}{A} \;=\; \frac{\varepsilon}{n_B}
  \qquad\text{at}\qquad P(n_B) = 0,\quad T = 0 ,
\end{equation}
%
in MeV. The entry point is \texttt{zero\_pressure\_point(par, species)}, which
returns $n_B$, $E/A$, $\mu_B$, $Y_S$, $\mu_S$, the residual of
Eq.~\eqref{eq:gibbsidentity}, the pressure actually reached, the flavour
content requested, and whether $E/A$ fell below the $930.4\;$MeV of iron.

\subsection{The identity that makes the read self-checking}

At $T = 0$ the Euler relation is $\varepsilon + P = \sum_i \mu_i n_i$, so at
$P = 0$ the energy per baryon \emph{is} the Gibbs energy per baryon. Expanding
the species potentials in the conserved-charge basis,
$\mu_i = B_i\mu_B + C_i\mu_C + S_i\mu_S$, and imposing beta equilibrium
$\mu_C + \mu_e = 0$ together with total electric neutrality $n_C = n_e$, the
charge and lepton terms cancel,
%
\begin{equation}
  \sum_i \mu_i n_i
    = \mu_B n_B + \underbrace{\mu_C n_C + \mu_e n_e}_{=\,0} + \mu_S n_S ,
\end{equation}
%
leaving
%
\begin{equation}
  \boxed{\;\frac{E}{A} \;=\; \mu_B + Y_S\,\mu_S\;} ,
  \qquad Y_S = \frac{n_S}{n_B} .
  \label{eq:gibbsidentity}
\end{equation}

\textbf{The full form is the one to use.} $E/A = \mu_B$ is the special case
$Y_S\mu_S = 0$. It holds in every beta-equilibrium mode, where strangeness
self-equilibrates and $\mu_S = 0$, and it fails in a colour-flavour locked
phase: locking pairs the three flavours at equal densities, and equal densities
at unequal masses require unequal potentials. On the CFL surface of
\texttt{eos.alphabag} at $\Delta_0 = 100\;$MeV one finds
$\mu_S = 40.68\;$MeV, so $\mu_B = 895.87\;$MeV against
$E/A = 936.55\;$MeV. There are not two conventions, only one identity and the
cases in which a term of it vanishes. Equation~\eqref{eq:gibbsidentity} is
checked at every located root: a root that misses it is a root of something
other than $P$.

\subsection{Locating the root}

\texttt{eos.general.zero\_pressure.locate\_zero\_pressure} samples $P(n_B)$ on
a grid, takes the \emph{lowest} density at which $P$ rises through zero, and
refines it with Brent's method. The scan is not a convenience: $P(n_B)$ may
cross zero more than once, and a crossing at which $P$ \emph{falls} is the top
of a mechanically unstable region rather than a surface. The locator takes the
state as a callable, so it holds no model; a density at which the solve does
not converge thins the scan rather than aborting it, and a parameter set with
no surface returns a status rather than raising.

\subsection{The Bodmer--Witten window}

The pair of numbers is a two-sided gate on a parameter set: three-flavour
$E/A < 930.4\;$MeV says strange quark matter is absolutely stable, and
two-flavour $E/A > 930.4\;$MeV says ordinary nuclei are not already decaying
into it. A set failing either is excluded. \textbf{Both facts are reported and
neither is asserted}, since whether a set lies in the window is a property of
the set; the same \texttt{below\_iron} flag reads in opposite directions on the
two arms. The content \emph{requested} and the content \emph{found} may differ
and both are returned: a set whose surface lies below the strange quark's
threshold returns $Y_S = 0$ from a three-flavour call, so the content is read
off $Y_S$.

\subsection{The \texttt{two\_flavour} flag}

Two-flavour quark matter is \texttt{beta\_eq\_neutrinoless} with the strange
sector switched off, which is what it physically is, and not
\texttt{fixed\_YC\_YS} at $Y_S = 0$. The distinction is not stylistic: with no
populated species carrying strangeness, $n_S = 0$ holds over a whole range of
$\mu_S$, so the row $n_S = Y_S n_B$ leaves $\mu_S$ undetermined and its
Jacobian column null, and the solve then converges on round-off. Switching the
sector off removes the flavour from the unknown vector instead. This model
therefore \textbf{raises} on \texttt{fixed\_YC\_YS} with the flag on.

The flag defaults to \texttt{False}, the $u,d,s$ matter this model has always
solved, so no existing number moves. It is named for the restriction rather
than for the sector because a sector-voiced \texttt{strange\_quarks} would have
had to default \texttt{True} to preserve that. With the flag on, $n_s = 0$,
$Y_S = 0$ and $\mu_S = 0$ identically; $\mu_B = \mu_u + 2\mu_d$ and
$\mu_C = \mu_u - \mu_d$ are unchanged, neither reading $\mu_s$, so
Eq.~\eqref{eq:gibbsidentity} collapses to $E/A = \mu_B$ with both strange terms
vanishing; and $\mu_s$ is reported as the weak-equilibrium value $\mu_d$, the
relation $s \leftrightarrow d$ still holding with nothing populated at it.

\subsection{What stays when the strange flavour goes}

\textbf{The strange condensate stays; only the strange Fermi sea is emptied.}
The three strange colour-flavour modes contribute nothing to the medium --- no
density, no scalar density, no pressure, energy or entropy --- while the
strange scalar field is still solved from its own field equation and still
feeds the light-quark masses. That is the physics of two-flavour quark matter:
the strange Fermi sea is empty, the strange condensate of the QCD vacuum is
not. Removing the strange field from the equations instead would move the
light-quark masses and the subtracted vacuum constant, changing the model
rather than the flavour content asked of it.

A diquark containing an $s$ quark is likewise not a state two-flavour matter
has, so $\Delta_1$ ($d$--$s$) and $\Delta_2$ ($u$--$s$) have nothing to pair:
the patterns carrying them leave the default enumeration and an explicitly
requested one is refused. \texttt{unpaired} and \texttt{2SC}, the $u$--$d$
condensate, survive.


\begin{thebibliography}{9}
\bibitem{Rehberg1996} P.~Rehberg, S.~P.~Klevansky, J.~H\"ufner,
  Phys.\ Rev.\ C \textbf{53}, 410 (1996), arXiv:hep-ph/9506436.
\bibitem{Buballa2005} M.~Buballa, Phys.\ Rept.\ \textbf{407}, 205 (2005),
  arXiv:hep-ph/0402234.
\bibitem{Ruester2005} S.~B.~R\"uster, V.~Werth, M.~Buballa, I.~A.~Shovkovy,
  D.~H.~Rischke, Phys.\ Rev.\ D \textbf{72}, 034004 (2005),
  arXiv:hep-ph/0503184.
\bibitem{Steiner2002} A.~W.~Steiner, S.~Reddy, M.~Prakash,
  Phys.\ Rev.\ D \textbf{66}, 094007 (2002), arXiv:hep-ph/0205201.
\bibitem{Alford2008} M.~G.~Alford, A.~Schmitt, K.~Rajagopal, T.~Sch\"afer,
  Rev.\ Mod.\ Phys.\ \textbf{80}, 1455 (2008), arXiv:0709.4635.
\bibitem{Braun2018} J.~Braun, M.~Leonhardt, M.~Pospiech,
  SciPost Phys.\ \textbf{6}, 056 (2019), arXiv:1812.08414.
\bibitem{Baym2018} G.~Baym, T.~Hatsuda, T.~Kojo, P.~D.~Powell, Y.~Song,
  T.~Takatsuka, Rept.\ Prog.\ Phys.\ \textbf{81}, 056902 (2018),
  arXiv:1707.04966.
\bibitem{Kunkel2026} S.~Kunkel, I.~A.~Rather \textit{et al.},
  arXiv:2607.11537.
\bibitem{Pagliara2008} G.~Pagliara, J.~Schaffner-Bielich,
  Phys.\ Rev.\ D \textbf{77}, 063004 (2008), arXiv:0711.1119.
\bibitem{Gholami2024} H.~Gholami, M.~Hofmann, M.~Buballa,
  Phys.\ Rev.\ D \textbf{111}, 014021 (2025), arXiv:2408.06704.
\bibitem{AlfordBraby2005} M.~Alford, M.~Braby, M.~Paris, S.~Reddy,
  Astrophys.\ J.\ \textbf{629}, 969 (2005), arXiv:nucl-th/0411016.
\bibitem{Logoteta2012} D.~Logoteta, I.~Bombaci, C.~Provid\^encia, I.~Vida\~na,
  Phys.\ Rev.\ D \textbf{85}, 023003 (2012), arXiv:1203.4159.
\bibitem{Lugones2016} G.~Lugones, Eur.\ Phys.\ J.\ A \textbf{52}, 53 (2016),
  doi:10.1140/epja/i2016-16053-x.
\bibitem{Ivanytskyi2025} O.~Ivanytskyi,
  Phys.\ Rev.\ D \textbf{111}, 034004 (2025), arXiv:2409.05859.
\bibitem{MUSESNJL} M.~Hofmann, H.~Gholami, C.~Pelicer, Y.~Yang, the MUSES NJL
  module, Zenodo doi:10.5281/zenodo.18249033; shipped with the Calliope release
  of the MUSES Calculation Engine.
\bibitem{TypelCompOSE2015} S.~Typel, M.~Oertel, T.~Kl\"ahn \textit{et al.},
  the CompOSE manual, Eur.\ Phys.\ J.\ A \textbf{58}, 221 (2022),
  arXiv:2203.03209.
\end{thebibliography}

\end{document}
