\documentclass[11pt,a4paper]{article}
\usepackage{amsmath,amssymb,bm}
\usepackage[margin=2.5cm]{geometry}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{hyperref}

\title{The chiral colour-dielectric model \\
\large as implemented in \texttt{eos.ccdm}}
\author{}
\date{}

\newcommand{\phib}{\bar\varphi}
\newcommand{\Mstar}{M^{*}}
\newcommand{\mustar}{\mu^{*}}
\newcommand{\code}[1]{\texttt{#1}}

\begin{document}
\maketitle

\begin{abstract}
A mean-field model of deconfined $u,d,s$ matter in which confinement and
chiral symmetry breaking are two faces of one mechanism. A dilaton field
carries the gluon condensate; the dielectric function built from it measures
the medium's transparency to colour and divides the quark masses, so that as
the condensate reaches its vacuum value the effective masses diverge and the
quarks leave the medium entirely. This document states every equation the code
solves and every quantity it returns. The implementation specification is
\code{docs/ccdm\_implementation.md} and is the authority wherever it and this
document differ, with the two exceptions recorded in \S\ref{sec:corrections},
which are places where the specification contradicts the thermodynamic audit
it itself mandates. The model descends from the soliton and dielectric
Lagrangians of Friedberg and Lee \cite{FriedbergLee1977,FriedbergLee1978},
Nielsen and Patk\'os \cite{NielsenPatkos1982} and Mack \cite{Mack1984},
reviewed by Birse \cite{Birse1990} and Pirner \cite{Pirner1992}, in the
three-flavour chiral form of Drago, Fiolhais and Tambini
\cite{DragoFiolhaisTambini1996} and Ghosh and Phatak \cite{GhoshPhatak1995},
applied to strangelets in \cite{AlbericoDragoRatti2002} and to hybrid stars in
\cite{Maieron2004};
the pairing sector follows Buballa \cite{Buballa2005} and Alford
\emph{et al.} \cite{Alford2008}.
\end{abstract}

\tableofcontents

\section{Conventions}

Natural units $\hbar=c=k_B=1$ throughout the physics; masses, chemical
potentials and the dilaton in MeV, densities in MeV$^3$, $\Omega$, $P$ and
$\varepsilon$ in MeV$^4$. The public boundary of the code is fm-based, with
$\hbar c = 197.3269804$ MeV\,fm: at that boundary $n$ is in fm$^{-3}$, $T$ and
every $\mu$ in MeV, $\varepsilon$ and $P$ in MeV\,fm$^{-3}$ and $s$ in
fm$^{-3}$. The conversion is by $(\hbar c)^3 = 7.6835057\times10^{6}$
MeV$^3$\,fm$^3$ and is applied once, at that boundary.

Flavour $f\in\{u,d,s\}$, colour $a\in\{r,g,b\}$, and the nine colour--flavour
modes $j=(f,a)$ indexed flavour-major, $j = 3\,i_f + i_a$. Electric charges
$q_u=+\tfrac23$, $q_d=q_s=-\tfrac13$. \textbf{Strangeness is $S=+1$ per $s$
quark}, the opposite of the PDG sign and the convention used consistently in
this repository. $C$ is the electric charge of strongly interacting matter
only: the leptons are excluded from it and enter through the separate
condition of total electric neutrality. Fractions are per baryon,
$Y_C=n_C/n_B$ and $Y_S=n_S/n_B$.

Colour generators are taken in the normalisation
\begin{equation}
(T_3)_{r,g,b}=\left(+\tfrac12,-\tfrac12,0\right),
\qquad
(T_8)_{r,g,b}=\left(+\tfrac13,+\tfrac13,-\tfrac23\right),
\end{equation}
i.e. $T_8=\lambda_8/\sqrt3$. Three normalisations are in circulation in the
colour-superconductivity literature and mixing them corrupts $\mu_8$ by factors
between $1.15$ and $1.7$; the conversions are documented in
\code{eos.general.pairing}.

\section{The Lagrangian}

Four boson fields --- the dilaton $\varphi$, the light scalar $\sigma$, the
pion triplet $\bm\pi$ and the strange scalar $\zeta$ --- plus the vector meson
$\omega_\mu$ and the diquark interaction:
\begin{align}
\mathcal L &= \sum_f \bar q_f\left[i\gamma^\mu\partial_\mu - \Mstar_f\right]q_f
 + \tfrac12(\partial\varphi)^2 - U(\varphi)
 + \tfrac12(\partial\sigma)^2 + \tfrac12(\partial\bm\pi)^2
 + \tfrac12(\partial\zeta)^2 - V(\sigma,\bm\pi,\zeta) \nonumber\\
&\quad + \mathcal L_{\rm vec} + \mathcal L_{\rm pair},
\\
\mathcal L_{\rm vec} &= -g_\omega\,\bar q\gamma^\mu q\,\omega_\mu
 - \tfrac14 F_{\mu\nu}F^{\mu\nu} + \tfrac12 m_\omega^2\omega_\mu\omega^\mu,
\\
\mathcal L_{\rm pair} &= G_D\sum_{\eta=1}^{3}
 \left|\bar q\,i\gamma_5 C\,\epsilon^{ab\eta}\epsilon_{ij\eta}\,\bar q^{\,T}\right|^2,
\qquad \eta=1,2,3 \leftrightarrow (ds),(us),(ud).
\end{align}
The dilaton potential $U$ is the one the scale anomaly fixes
\cite{Schechter1980,MigdalShifman1982}; it is written in \eqref{eq:U}.

\subsection{The dielectric, and why the fourth power}

The dielectric function is \emph{not} a field: it has no kinetic term, no
potential and no field equation. It is an abbreviation,
\begin{equation}
\boxed{\;\chi(\varphi) = \left[1-\phib^{\,4}\right]^{p},
\qquad \phib \equiv \varphi/\varphi_0,\qquad p=1 \;}
\label{eq:chi}
\end{equation}
and it appears only in the quark mass operator. The fourth power is fixed:
$\phib^{\,4}$, not $\phib$, is the gluon condensate, because the dilaton is a
canonically normalised scalar of mass dimension one while
$\langle G^A_{\mu\nu}G^{A\mu\nu}\rangle$ has dimension four. The scale anomaly
makes this sharp --- the combination the anomaly fixes comes out a pure power
of $\phib$ only for exponent four,
\begin{equation}
4U - \varphi\,\frac{\partial U}{\partial\varphi} = 4B_g\left(1-\phib^{\,4}\right),
\end{equation}
whereas $\phib^{2}$, $\phib^{3}$, $\phib^{5}$ or $\phib^{6}$ leave a residual
$\phib^{\,n}\ln\phib$. So
\begin{equation}
1-\phib^{\,4} = 1 - \frac{\langle G^2\rangle_{\rm med}}{\langle G^2\rangle_{\rm vac}},
\end{equation}
and the medium becomes transparent to colour exactly in proportion to how much
condensate has melted. The two endpoints are then correct by construction:
$\chi\to0$ at $\phib=1$ (confinement, $\Mstar\to\infty$) and $\chi\to1$ at
$\phib=0$ (perturbative, $\Mstar\to m_f$) --- for every $p>0$, since only the
bracket vanishes and diverges. The exponent $p$ outside is a separate matter:
only $\chi^{p}$ enters $\Mstar$, so $p$ and the bracket are meaningful only as
a pair, and $p$ is therefore the knob on how abruptly the medium opens between
those fixed endpoints. It is shipped at $1$ and is not sampled; see
section~\ref{sec:tier2}.

\subsection{The solve variable}
\label{sec:Phi}

Define
\begin{equation}
\Phi \equiv \phib^{\,4}
= \frac{\langle G^2\rangle_{\rm med}}{\langle G^2\rangle_{\rm vac}},
\end{equation}
in which the potential and the dielectric are simply
\begin{equation}
U = B_g\left[\Phi(\ln\Phi-1)+1\right],
\qquad
\chi = (1-\Phi)^{p},
\qquad
\frac{\mathrm dU}{\mathrm d\Phi} = B_g\ln\Phi .
\label{eq:U}
\end{equation}
$U(1)=0$ at the physical vacuum and $U(0)=B_g$ at the perturbative point,
which is what makes $B_g$ the glue part of the effective bag constant
(\S\ref{sec:bag}). The limit $\Phi\ln\Phi\to0$ is taken explicitly at
$\Phi=0$; without it the perturbative point returns NaN.

\textbf{$\varphi$ is the field; $\Phi$ is the solve variable.} $\Phi$ cannot be
the Lagrangian field: its kinetic term
$\tfrac12(\partial\varphi)^2 = \tfrac{\varphi_0^2}{32}\Phi^{-3/2}(\partial\Phi)^2$
is not canonical and diverges at $\Phi\to0$, $[\Phi]=4$ makes
$(\partial\Phi)^2$ dimension ten so no renormalisable Lagrangian exists in it,
and the glueball mass would stop being $U''$. But as a solve variable it is
strictly better, for one concrete reason: written in $\phib$ the dilaton
residual has a \emph{spurious root at $\phib=0$}, where both of its terms vanish
as $\phib^{\,3}$ --- the first as $\phib^{\,3}\ln\phib$, the second as
$\phib^{\,3}$ --- so it vanishes there for \emph{any} scalar density. It is an
artefact of the parametrisation: the Jacobian $\mathrm d\Phi/\mathrm d\phib =
4\phib^{\,3}$ vanishes, not the physics. In $\Phi$, where
$\mathrm dU/\mathrm d\Phi=B_g\ln\Phi\to-\infty$, no such root exists.

$\Phi$ is evaluated only inside
\begin{equation}
\Phi\in\left[\Phi_{\min},\,\Phi_{\max}\right],
\qquad \Phi_{\min}=10^{-14},\qquad \Phi_{\max}=1-10^{-13},
\label{eq:phiguard}
\end{equation}
a clamp and not a physical bound. A Newton step may propose a dilaton outside
$[0,1]$, where the residual is NaN and the solve dies with no information;
evaluating at the edge instead leaves $\mathrm dU/\mathrm d\Phi$ strongly
signed, which pushes the next step back inside. The ceiling is not
$1-10^{-9}$: $\Phi=1$ exactly is where the \emph{confined} branch's solution
genuinely sits (with no quarks $R_1$ reduces to $B_g\ln\Phi$, whose only root
is $\Phi=1$), so the ceiling must be close enough that $|\ln\Phi|=10^{-13}$
falls inside the residual gate, and far enough that $\Mstar=2.8\times10^{15}$
MeV stays an ordinary double. $1-10^{-13}$ is both.

\subsection{Mean fields}

At homogeneous mean field every boson is replaced by its expectation value,
$\bm\pi\to0$ by parity, and $\omega_\mu\to\delta_{\mu0}\omega_0$:
\begin{equation}
\mathcal L_{\rm MF} = \sum_f \bar q_f\left[i\gamma^\mu\partial_\mu
 - g_\omega(n_B)\gamma^0\omega_0 - \Mstar_f\right]q_f
 + \mathcal L^{\rm MF}_{\rm pair}
 - \left[U(\varphi)+V(\sigma,\zeta) - \tfrac12 m_\omega^2\omega_0^2\right],
\end{equation}
with
\begin{equation}
\boxed{\;
\Mstar_u = \frac{g_q\sigma+m_u}{\chi},\qquad
\Mstar_d = \frac{g_q\sigma+m_d}{\chi},\qquad
\Mstar_s = \frac{g_s\zeta+m_s}{\chi}\;}
\label{eq:masses}
\end{equation}
and the chiral potential
\begin{align}
V(\sigma,\zeta) &= \frac{\lambda}{4}\left(\sigma^2-v^2\right)^2
 + \frac{\lambda_\zeta}{4}\left(\zeta^2-v_\zeta^2\right)^2
 - \epsilon_\sigma\sigma - \epsilon_\zeta\zeta + C_0,
\label{eq:V}\\
\frac{\partial V}{\partial\sigma} &= \lambda\sigma(\sigma^2-v^2)-\epsilon_\sigma,
\qquad
\frac{\partial V}{\partial\zeta} = \lambda_\zeta\zeta(\zeta^2-v_\zeta^2)-\epsilon_\zeta .
\label{eq:dV}
\end{align}
$v$ and $v_\zeta$ are constants, not fields: the Mexican-hat radii before
explicit breaking, and not observables. The linear terms shift the true minima
to $\sigma_0,\zeta_0$.

A state whose effective masses are not all positive is marked invalid and
dropped by the enumeration rather than ranked: the Mexican hat admits its
reflected minimum, $\sigma<0$, which solves \eqref{eq:dV} and is not physical
matter.

\subsection{The diquark condensate}

The three condensates
$s_\eta = \langle q^{T}C\gamma_5\,\epsilon^{ab\eta}\epsilon_{ij\eta}\,q\rangle$
give, to quadratic order in the fluctuation,
\begin{equation}
\mathcal L^{\rm MF}_{\rm pair} = \sum_{\eta=1}^{3}
 \left[\frac{\Delta_\eta^*\mathcal O_\eta + \Delta_\eta\mathcal O_\eta^\dagger}{2}
 - \frac{|\Delta_\eta|^2}{4G_D}\right],
\qquad
\Delta_\eta = 2G_D s_\eta .
\end{equation}
The mean-field treatment is exact for an auxiliary field appearing
quadratically, so substituting $\Delta_\eta=2G_Ds_\eta$ back reproduces
$G_D|s_\eta|^2$ exactly; the gap equation
$\partial\Omega/\partial\Delta_\eta=0$ \emph{is} the condensate definition; and
at the solution $\Omega_\eta=-G_D|s_\eta|^2<0$, so condensation lowers the
grand potential as it must. All $\Delta_\eta$ are taken real, which is a
convention since the phases are unobservable in a homogeneous condensate.

\textbf{The sign of each $\Delta_\eta$ is a gauge.} $\Omega$ is invariant under
flipping any subset of the three gaps and each gap kernel flips with its own
gap, so $-\Delta_\eta$ is a root whenever $\Delta_\eta$ is. The code reports
magnitudes; the signed values stay on the internal state and in the unknown
vector, which is what a warm start is built from.

\section{Chemical potentials}
\label{sec:mu}

In the conserved-charge basis, with the colour potentials present only where
pairing is,
\begin{equation}
\mu_{f,a} = \tfrac13\mu_B + q_f\mu_C + s_f\mu_S + (T_3)_a\mu_3 + (T_8)_a\mu_8 .
\label{eq:mu}
\end{equation}
Both colour generators are traceless, so colour drops out of $n_B$. The
potential fed to every momentum integral is shifted by the vector self-energy,
\begin{equation}
\boxed{\;\mustar_{f,a} = \mu_{f,a} - \Sigma_V,
\qquad \Sigma_V = g_\omega(n_B)\,\omega_0 + \Sigma_R\;}
\label{eq:mustar}
\end{equation}
with $\Sigma_R$ the rearrangement term of \S\ref{sec:vector}.

$\mu_C$ is the \emph{charge} chemical potential and $\mu_e=-\mu_C$, so beta
equilibrium reads $\mu_C+\mu_e=0$. Writing $+q_f\mu_e$ in place of $+q_f\mu_C$
flips the $u$--$d$ ordering: at $\mu_B=1200$, $\mu_C=-53.15$ MeV the correct
values are $\mu_u=364.6<\mu_d=417.7$ MeV, whereas the wrong sign gives
$\mu_u=435.4>\mu_d=382.3$ MeV.

\subsection{The lepton sector}
\label{sec:leptons}

Leptons feel no field the quarks feel, carry no colour and no $C$ in the sense
of \S1, and are free Fermi gases of \code{eos.general.thermodynamics\_leptons}.
Masses $m_e=0.5109989$ MeV, $m_\mu=105.6584$ MeV, degeneracy $g=2$ with
antiparticles included; trapped neutrinos are massless with $g=1$, not $2$,
because they are left-handed only. Their thermodynamics is the same ideal gas
as \S\ref{sec:integrals} at $\Mstar\to m_\ell$, $\mustar\to\mu_\ell$ and with
that degeneracy.

The potentials follow from the reactions that are fast:
\begin{equation}
\mu_e = \mu_{\nu_e} - \mu_C \quad (n+\nu_e\leftrightarrow p+e),
\qquad
\mu_\mu = \mu_e - \mu_{\nu_e} \quad (\mu^-\leftrightarrow e^-\bar\nu_e\nu_\mu),
\label{eq:leptonmu}
\end{equation}
with $\mu_{\nu_e}=0$ where the neutrinos free-stream --- which is what
free-streaming \emph{means} here: no lepton number and no pressure of their
own. Only the electron family is ever trapped; the muon neutrinos escape,
which is why $\mu_\mu = \mu_e - \mu_{\nu_e}$ rather than $\mu_e$.

In a mode that \emph{holds} $Y_C$ (\S\ref{sec:modes}), $\mu_e$ is not given by
\eqref{eq:leptonmu} at all: with \code{leptons=True} the neutralizing leptons
are solved \emph{after} the matter, from the single condition
\begin{equation}
n_e(\mu_e,T) + n_\mu(\mu_e,T) = n_C,
\label{eq:neutralizing}
\end{equation}
because nothing about them feeds back into the quark sector. They are
therefore not a row of the residual. With \code{leptons=False} there are no
leptons at all and the phase is electrically charged --- which is what a
mixed-phase construction needs of each pure phase before imposing
\emph{global} neutrality.

\section{The vector coupling and its rearrangement}
\label{sec:vector}

The vector coupling is a function of the state, not a parameter:
\begin{equation}
g_\omega(n_B) = \frac{\bar g_\omega}{1+u^{k}},
\qquad
\frac{\partial g_\omega}{\partial n_B}
 = -\frac{k\bar g_\omega}{n_c}\,\frac{u^{k-1}}{\left[1+u^{k}\right]^2},
\qquad u\equiv\frac{n_B}{n_c},
\label{eq:gomega}
\end{equation}
with $k\equiv k_\omega$ shipped at $2$ and $n_c$ stored in fm$^{-3}$ --- a
density a reader holds in fm --- and converted to MeV$^3$ where $n_B$ is, so a
caller never has to remember which side of the boundary it is on. $n_c=\infty$
is the constant coupling exactly, for every $k$; $\bar g_\omega=0$ switches
the whole sector off, exactly (no $\omega_0$, no $\Sigma_R$, and no
$\Sigma_V$ row in the unknown vector).
A repulsion that dies off at high density keeps the vector energy from growing
without bound: that energy goes as $g_\omega^2 n_q^2$, so a coupling falling
fast enough turns it into a term that stops growing at all. The derivative is
negative everywhere, which is also why the innermost fixed point on $\omega_0$
converges monotonically.

\paragraph{What the decay costs, and where.} Saturating the vector energy is
the same statement as making the vector sector's \emph{pressure} turn over,
because the rearrangement below is mandatory. Collecting the two vector terms
of $P$ --- the field energy and $\Sigma_R n_q$ ---
\begin{equation}
P_{\rm vec}
 = \frac{n_q^2}{m_\omega^2}\,g_\omega
   \left[\frac{g_\omega}{2}+n_B\frac{\partial g_\omega}{\partial n_B}\right]
 = \frac{n_q^2}{m_\omega^2}\,g_\omega^2
   \left[\frac12-\frac{k\,u^{k}}{1+u^{k}}\right],
\label{eq:Pvec}
\end{equation}
so the bracket, and with it the vector contribution to the pressure, turns
negative exactly where the coupling's logarithmic slope passes $-1/2$:
\begin{equation}
\frac{\mathrm{d}\ln g_\omega}{\mathrm{d}\ln n_B}<-\frac12
\quad\Longleftrightarrow\quad
P_{\rm vec}<0
\quad\Longleftrightarrow\quad
u>(2k-1)^{-1/k},
\end{equation}
never for $k\le1/2$, at $u>1$ for $k=1$, at $u>0.577$ for the shipped $k=2$,
at $u>0.615$ for $k=4$. And $P_{\rm vec}$ is already \emph{falling} before it
changes sign: for $k=2$ its maximum and minimum are the roots of
$3u^4-8u^2+1=0$, $u=0.363$ and $u=1.592$. A parameter point whose density axis
reaches past $n_B=0.363\,n_c$ is therefore one where the vector sector removes
pressure as the density rises, and where it does so faster than the kinetic
term adds it the total $P$ is non-monotonic --- mechanically unstable rather
than soft. At $\bar g_\omega=4$, $n_c=3$~fm$^{-3}$ that is what happens: $P$
falls from the onset and $c_s^2$ reaches $-0.59$.

\paragraph{The trade is structural.} $k\le1/2$ keeps the slope above $-1/2$ at
every density and $P_{\rm vec}$ positive everywhere --- but a coupling that
gentle does not saturate the vector energy either, which is what the density
dependence was for. $n_c=\infty$ is the other end: no rearrangement and an
energy that grows forever. What a parameter point chooses is \emph{where} the
turnover sits, and $n_c$ is the lever: keep $0.363\,n_c$ above the top of the
density axis and the sector is monotonic over it.

The field equation is sourced by the \textbf{quark} number density
$n_q=3n_B$, because the coupling in $\mathcal L_{\rm vec}$ is to
$\bar q\gamma^\mu q$; using $n_B$ understates $\omega_0$ by a factor three and
the repulsive energy by nine:
\begin{equation}
\omega_0 = \frac{g_\omega(n_B)\,n_q}{m_\omega^2}.
\label{eq:omega}
\end{equation}
Because $g_\omega$ depends on the density, the shift of $\mu$ is the derivative
of the interaction energy $W=\tfrac12 m_\omega^2\omega_0^2$ with respect to the
quark density, not merely $g_\omega\omega_0$:
\begin{equation}
\boxed{\;
\Sigma_V = \frac{\mathrm dW}{\mathrm dn_q}
 = g_\omega\omega_0 + \Sigma_R,
\qquad
\Sigma_R = \frac{\partial g_\omega}{\partial n_B}\,\omega_0\,n_B \;}
\label{eq:SigmaR}
\end{equation}
(the chain rule through $n_B=n_q/3$ is what turns the naive
$(\partial g/\partial n_q)n_q$ into the $n_B$ written here).
\textbf{$\Sigma_R$ enters $\mu$ and $P$ and never $\varepsilon$.} Note that
$W$ peaks at $n_B=n_c$ and falls beyond it, so $\Sigma_R$ --- and with it
$\Sigma_V$ --- changes sign there; this is a property of the coupling form
\eqref{eq:gomega} and not of the implementation.

The diquark coupling may be dressed by the dielectric,
\begin{equation}
G_D \longrightarrow G_D/\chi^{\,q},\qquad q\in\{0,1\},
\label{eq:GDdress}
\end{equation}
$q=1$ being the exponent a gluon-exchange origin gives and the largest that
leaves the pairing channel confined, the criterion being $q\le p$ because the
critical coupling for vacuum diquark condensation grows only linearly in
$\Mstar\propto\chi^{-p}$.

\section{The ideal-gas integrals}
\label{sec:integrals}

Every momentum integral in this model is a relativistic ideal gas of one
colour--flavour mode with spin degeneracy $g=2$. With
$E_k=\sqrt{k^2+{\Mstar}^2}$ and
\begin{equation}
f^{\pm}_k = \left[1+e^{(E_k\mp\mustar)/T}\right]^{-1},
\end{equation}
the five integrals are
\begin{align}
\mathcal N(\Mstar,\mustar,T) &= \frac{g}{2\pi^2}\int_0^{k_{\max}}\!\!\mathrm dk\;k^2
 \left[f^+_k-f^-_k\right],
&&\text{(antiparticles \emph{subtract})}
\label{eq:N}\\
\mathcal R_s(\Mstar,\mustar,T) &= \frac{g}{2\pi^2}\int_0^{k_{\max}}\!\!\mathrm dk\;k^2\,
 \frac{\Mstar}{E_k}\left[f^+_k+f^-_k\right],
&&\text{(antiparticles \emph{add})}
\label{eq:Rs}\\
\mathcal P(\Mstar,\mustar,T) &= \frac{g}{2\pi^2}\int_0^{k_{\max}}\!\!\mathrm dk\;k^2\,T
 \sum_{\pm}\ln\left(1+e^{-(E_k\mp\mustar)/T}\right),
\label{eq:P}\\
\mathcal E(\Mstar,\mustar,T) &= \frac{g}{2\pi^2}\int_0^{k_{\max}}\!\!\mathrm dk\;k^2 E_k
 \left[f^+_k+f^-_k\right],
\label{eq:E}\\
\mathcal S(\Mstar,\mustar,T) &= \frac{g}{2\pi^2}\int_0^{k_{\max}}\!\!\mathrm dk\;k^2
 \sum_{\pm}\left[\frac{E_k\mp\mustar}{T}f^\pm_k
 + \ln\left(1+e^{-(E_k\mp\mustar)/T}\right)\right].
\label{eq:S}
\end{align}
They satisfy, per mode and to machine precision,
\begin{equation}
\mathcal N = \frac{\partial\mathcal P}{\partial\mustar},\quad
\mathcal R_s = -\frac{\partial\mathcal P}{\partial\Mstar},\quad
\mathcal S = \frac{\partial\mathcal P}{\partial T},\quad
\mathcal E = -\mathcal P + \mustar\mathcal N + T\mathcal S .
\label{eq:modeeuler}
\end{equation}
Note that the single-mode Euler relation carries $T\mathcal S$ and \emph{not}
$\Mstar\mathcal R_s$: the scalar density is a response, not a conserved charge.

$\mathcal S$ is \emph{integrated}, through \eqref{eq:S}, and not obtained from
$\mathcal S=(\mathcal E+\mathcal P-\mustar\mathcal N)/T$. The two are equal
identically --- the identity holds integrand by integrand, so it even survives
the cutoff --- but the Euler route is a difference of three numbers of order
$10^{9}$ divided by $T$, and in a cold nearly degenerate gas, where
$\mathcal S$ is genuinely $10^{-8}$ of them, the cancellation eats every
significant digit. Integrating costs one more array in the same quadrature
pass and leaves \eqref{eq:modeeuler} available as a \emph{check} rather than
spending it as a definition.

$\mathcal P$ is the \textbf{logarithm} form of \eqref{eq:P} and not the
$k^4/E$ form
$\tfrac{g}{6\pi^2}\int\mathrm dk\,(k^4/E_k)(f^+_k+f^-_k)$. The two differ by
the boundary term of the integration by parts,
\begin{equation}
\mathcal P_{\log} - \mathcal P_{k^4}
 = \frac{g}{6\pi^2}k_{\max}^3\,T\sum_\pm
 \ln\left(1+e^{-(E_{k_{\max}}\mp\mustar)/T}\right),
\end{equation}
which does not vanish when the integral is cut and is not small: $0.1\%$ of
$\mathcal P$ at $(\Mstar,\mustar,T)=(100,500,20)$ MeV, $10.5\%$ at
$(40,590,30)$ and $39.9\%$ at $(140,700,50)$. At $T=0$ with $k_F<k_{\max}$ the
two agree, which is exactly why the error would hide until a table was built
at finite temperature.

\textbf{The medium integrals are unregularised.} $k_{\max}$ is a numerical
ceiling at which the integrand has died,
\begin{equation}
k_{\max}=\max(|\mustar|,\Mstar)+45\,T+12\,\Mstar+200\ \text{MeV},
\label{eq:kmax}
\end{equation}
and not a parameter; the $12\Mstar$ term is what covers the antiparticle tail
of a heavy confined mode, whose integrand decays on the scale of $\Mstar$
rather than of $T$. The cutoff $\Lambda$ of \S\ref{sec:pairing} applies to the
\emph{pairing} integral alone. This is why a sharp cutoff is admissible here,
and it implies the validity ceiling $\mu_{\max}=\sqrt{\Lambda^2+m_s^2}$ for the
pairing sector, which the code declares rather than exceeds.

\subsection{Quadrature, and confinement}

The integrals are evaluated by \textbf{panel-split Gauss--Legendre} quadrature
with breakpoints at the Fermi momentum $k_F=\sqrt{{\mustar}^2-{\Mstar}^2}$ and at
$k_F\pm 25T$. This is not a refinement: a single panel cannot resolve a step of
width $\sim T$ inside a range of $\sim1500$ MeV, and single-panel accuracy is
not even monotone in the node count, because whether a node lands near the
Fermi step is accidental. At $T=0.5$ MeV the split scheme reaches with $100$
nodes per panel what a single panel needs $5000$ to reach.

\textbf{At $T=0$ a mode with $\mustar\le\Mstar$ contributes identically zero.}
All five integrals \eqref{eq:N}--\eqref{eq:S} return exactly $0$, not a small
number. This is the confinement mechanism itself: as $\phib\to1$ the dielectric
closes, $\Mstar$ diverges through \eqref{eq:masses}, and the quarks leave the
medium. Smoothing the threshold destroys the pinning that makes the
deconfinement transition first order. At $T>0$ the same statement is a
threshold rather than an identity, and the code applies it where
$\Mstar-|\mustar| > 60\,T$: the occupation there is $e^{-60}\sim10^{-26}$, so
it is exact well inside double precision, and it exists because the confined
branch drives $\Mstar$ to $10^{15}$ MeV, where integrating is not wrong but
pointless.

At $T=0$ and $\mustar>\Mstar$ the closed forms are
\begin{align}
\mathcal N &= \frac{g}{6\pi^2}k_F^3,
\qquad
\mathcal R_s = \frac{g}{4\pi^2}\Mstar\left(k_F\mustar
 - \Mstar{}^2\ln\frac{k_F+\mustar}{\Mstar}\right),
\qquad \mathcal S = 0,
\\
\mathcal P &= \frac{g}{48\pi^2}\left[2k_F^3\mustar
 - 3\Mstar{}^2\left(\mustar k_F - \Mstar{}^2\ln\frac{k_F+\mustar}{\Mstar}\right)\right],
\\
\mathcal E &= \frac{g}{16\pi^2}\left[2k_F^3\mustar
 + \Mstar{}^2\left(\mustar k_F - \Mstar{}^2\ln\frac{k_F+\mustar}{\Mstar}\right)\right].
\end{align}
The code reaches them as the $T\to0$ limit of the same quadrature --- the
occupations become step functions and the upper limit becomes $k_F$ --- rather
than by a separate branch, so the two agree by construction.

The photon gas, where the \code{photons} flag enables it, is the massless
$\mu=0$ boson with $g=2$:
\begin{equation}
\mathcal P_\gamma = \frac{\pi^2}{45}T^4,\qquad
\mathcal E_\gamma = 3\mathcal P_\gamma,\qquad
\mathcal S_\gamma = \frac{4\pi^2}{45}T^3,
\label{eq:photon}
\end{equation}
carrying no conserved charge.

\section{The pairing sector}
\label{sec:pairing}

The pairing machinery is shared with the NJL model of this repository
(\code{eos.general.pairing}) and is stated here in full so that this document
is self-contained. It follows \cite{Buballa2005,Alford2008,Ruester2005}, with
the neutrality treatment of \cite{Steiner2002}.

\subsection{The gap matrix and the quasiparticle spectrum}

Build the $9\times9$ gap matrix on the colour--flavour modes directly from the
antisymmetric tensors,
\begin{equation}
\mathcal G = \sum_{\eta=1}^{3}\Delta_\eta\,\mathcal B_\eta,
\qquad
\left(\mathcal B_\eta\right)_{(ai),(bj)} = \epsilon^{ab\eta}\epsilon_{ij\eta}.
\label{eq:gapmatrix}
\end{equation}
Its eigenvalue multiplicities are \emph{derived}, never assigned: unpaired
$0^{(\times9)}$; 2SC $\pm\Delta^{(\times2)},0^{(\times5)}$; uSC/dSC
$\pm\Delta^{(\times2)},\pm\sqrt{\Delta_a^2+\Delta_b^2},0^{(\times3)}$; CFL
$\Delta^{(\times3)},-\Delta^{(\times5)},2\Delta$.

\textbf{The eigenvalues alone are not enough.} As soon as the quark masses
differ,
$\left[\mathcal G,\ \mathrm{diag}(\Mstar_f)\right]\neq0$, so the gap matrix and
the mass matrix share no eigenbasis and the problem does not factorise into
independent scalar dispersions. At $\Mstar=(60,65,300)$, $\mustar=450$,
$\Delta=60$ MeV the eigenvalue prescription misses branches by up to $33.6$ MeV
in uSC and $27.1$ in CFL. So at each momentum the code assembles and
diagonalises the mean-field inverse propagator in the \textbf{full Dirac
basis}: four components per colour--flavour mode, two particle and two
antiparticle, so $36$ states, with the gap mixing them (R\"uster \emph{et al.},
Phys. Rev. D \textbf{72}, 034004 (2005), Appendix~A). They block-diagonalise
into six $4\times4$ blocks --- two for each pair of modes a single gap couples
--- and one $12\times12$ for the $(u_r, d_g, s_b)$ triple all three gaps
couple. \texttt{eos.general.pairing} builds both, and \texttt{eos.njl}'s
document writes them out entry by entry.

The spectrum comes in $\pm$ pairs and $\Omega$ wants the positive half, taken
as \emph{half the sum over all $36$ of} $|\lambda_a|$, which is smooth through
a gapless window: a branch crossing zero has its partner crossing back, and
$\partial|\lambda|/\partial x = \mathrm{sign}(\lambda)\,\partial\lambda/
\partial x$ carries the branch sign the Hellmann--Feynman derivatives below
need. Write $|V_a\rangle$ for the eigenvector of $\lambda_a$ and
\begin{equation}
  w_a = \tfrac12\,\mathrm{sign}(\lambda_a)\tanh\frac{|\lambda_a|}{2T},
  \qquad
  \xi^{(j),r}(k) = E_{k,f(j)} - r\,\mustar_j .
  \label{eq:bdg}
\end{equation}

Solving the free Dirac problem FIRST --- fixing $E=\sqrt{k^2+\Mstar^2}$ and
pairing the on-shell modes in an $18\times18$ Bogoliubov--de~Gennes problem
$\big(\begin{smallmatrix}\xi & \mathcal G\\ \mathcal G & -\xi\end{smallmatrix}
\big)$ --- is the familiar shortcut and is \emph{not} what this module does. It
drops the particle--antiparticle mixing the gap induces, which is controlled by
the mass mismatch of the pair: exact at equal paired masses, and wrong by MeV
where a light quark pairs with a heavy one, which is every CFL phase here.

\subsection{The pairing potential, as a correction}

\begin{align}
\Omega_{\rm quark} &= -\sum_{j=1}^{9}\mathcal P\!\left(\Mstar_j,\mustar_j,T\right)
 + \delta\Omega_{\rm pair} + \sum_{\eta=1}^{3}\frac{\Delta_\eta^2}{4G_D},
\label{eq:Omegaquark}\\
\delta\Omega_{\rm pair} &= -\frac{1}{2\pi^2}\int_0^{\Lambda}\!\!\mathrm dk\,k^2
 \left[\frac12\sum_{a=1}^{36}\phi\!\left(|\lambda_a|\right)
 - \sum_{r=\pm}\sum_{j\in\mathcal C}
   \phi\!\left(\left|\xi^{(j),r}\right|\right)\right],
\label{eq:dOmega}\\
\phi(x) &\equiv x + 2T\ln\left(1+e^{-x/T}\right),%
\qquad\text{$\mathcal C$ the modes the blocks cover,}
\qquad \phi'(x) = \tanh\frac{x}{2T},
\qquad \frac{\partial\phi}{\partial T}
 = 2\ln\left(1+e^{-x/T}\right) + \frac{2x/T}{e^{x/T}+1}.
\label{eq:phi}
\end{align}
Written as a \emph{correction} --- a difference from the unpaired spectrum ---
every bracket vanishes identically at $\Delta_\eta=0$, so $\delta\Omega_{\rm
pair}=0$ there exactly and the whole expression reduces to the unpaired one
mode by mode. The alternative ``replacement'' form, in which the paired modes'
Fermi integrals are dropped and the $|E|$ integral put in their place, differs
by a $\Delta$-independent but $\Lambda$-dependent constant of about 2\% of the
term: the gap comes out right and the equation of state is wrong. In the clean
weak-coupling limit \eqref{eq:dOmega} obeys the BCS logarithm,
$-\delta\Omega_{\rm pair}/[\mu^2\Delta^2(\ln(2\Lambda/\Delta)-\tfrac12)]
\to 2/\pi^2$, approached from below as $\Delta$ falls.

\textbf{The antiparticle branches are not optional.} At $T=0$ they contribute
$8.8\%$ of the pairing potential at $\Lambda=600$ MeV and $17.1\%$ at
$\Lambda=1000$ MeV, so omitting them is a cutoff-dependent error that cancels
nowhere. Every logarithm in \eqref{eq:phi} is computed as
$T\,\mathrm{logaddexp}(0,-x/T)$ and every occupation through $\tanh$, never as
$\ln(1+e^{-x/T})$ or $1/(1+e^{x/T})$ directly, which overflow for
$x/T\gtrsim700$ --- a $T=1$ MeV point with a $300$ MeV branch reaches that
immediately.

\subsection{The three gap equations}

There is one residual per channel, not one gap:
\begin{equation}
\boxed{\;
R_{\Delta_\eta} = \frac{\Delta_\eta}{2G_D}
 - \frac{1}{2\pi^2}\int_0^{\Lambda}\!\!\mathrm dk\,k^2\sum_{a=1}^{36}
 w_a \left\langle V_a\left|
 \frac{\partial H}{\partial\Delta_\eta}
 \right|V_a\right\rangle = 0\;}
\label{eq:gapeq}
\end{equation}
with $w_a$ from \eqref{eq:bdg}. The matrix element is the Hellmann--Feynman
derivative $\partial\lambda_a/\partial\Delta_\eta$, and
$\partial H/\partial\Delta_\eta$ is the constant placement pattern of that gap
inside the blocks, assembled once.

\textbf{This is never the $\Delta/|E|$ form}, which is obtained by
differentiating $|E|$ as though every branch were positive. Against finite
differences of $\delta\Omega_{\rm pair}$ at $\mu_u=400$, $\mu_d=500$ MeV that
form is wrong by a factor $12.0$ at $\Delta=40$ MeV, $1.7$ at $60$ and $1.3$
at $80$, and it makes the gap \emph{grow} with the Fermi-surface mismatch ---
the opposite of the physics.

\subsection{Paired densities, scalar densities and entropy}
\label{sec:paired}

For the modes the gap touches, the densities are \emph{not} the unpaired Fermi
integrals: pairing redistributes occupation around the Fermi surface. Every
paired quantity is again a \emph{correction}, so it is added to the unpaired
sum without a second code path,
\begin{equation}
n_j = \mathcal N_j + \delta n_j,\qquad
\rho_{s,f} = \sum_{j\in f}\mathcal R_{s,j} + \delta\rho_{s,f},\qquad
s_{\rm quark} = \sum_j \mathcal S_j + \delta s_{\rm pair},
\label{eq:pairedsums}
\end{equation}
and each correction is the Hellmann--Feynman derivative of \eqref{eq:dOmega},
taken on the same blocks in the same quadrature pass. Both
$\partial H/\partial\mu_j$ and $\partial H/\partial\Mstar_f$ are
\emph{diagonal} --- they carry the signs with which that mode's potential and
that flavour's mass enter each block row --- so their expectation values are
sums of squared eigenvector components:
\begin{align}
\delta n_j &= -\frac{\partial\,\delta\Omega_{\rm pair}}{\partial\mu_j}
 = \frac{1}{2\pi^2}\int_0^{\Lambda}\!\!\mathrm dk\,k^2\left[
 \sum_{a} w_a \left\langle V_a\left|\frac{\partial H}{\partial\mu_j}
 \right|V_a\right\rangle
 + \sum_{r=\pm} r\tanh\frac{\xi^{(j),r}}{2T}\right],
\label{eq:dn}\\
\delta\rho_{s,f} &= +\frac{\partial\,\delta\Omega_{\rm pair}}{\partial\Mstar_f}
 = -\frac{1}{2\pi^2}\int_0^{\Lambda}\!\!\mathrm dk\,k^2\left[
 \sum_{a} w_a \left\langle V_a\left|\frac{\partial H}{\partial\Mstar_f}
 \right|V_a\right\rangle
 - \sum_{r=\pm}\sum_{j\in f\cap\,\mathcal C}\frac{\Mstar_f}{E_{k,f}}
   \tanh\frac{\xi^{(j),r}}{2T}\right],
\label{eq:drhos}\\
\delta s_{\rm pair} &= -\frac{\partial\,\delta\Omega_{\rm pair}}{\partial T}
 = \frac{1}{2\pi^2}\int_0^{\Lambda}\!\!\mathrm dk\,k^2\left[
 \frac12\sum_{a=1}^{36}\frac{\partial\phi}{\partial T}\!\left(|\lambda_a|\right)
 - \sum_{r=\pm}\sum_{j\in\mathcal C}
   \frac{\partial\phi}{\partial T}\!\left(\left|\xi^{(j),r}\right|\right)\right],
\label{eq:ds}
\end{align}
with $\partial\phi/\partial T$ from \eqref{eq:phi}. Note the sign of
\eqref{eq:drhos}: the scalar density is $\rho_s=+\partial\Omega/\partial\Mstar$
(equivalently $-\partial P/\partial\Mstar$, which is \eqref{eq:modeeuler}), so
the correction carries the opposite sign to $\delta n_j$; \emph{substituting
the unpaired formula for either is a $15\%$ density error at a realistic gap
and breaks neutrality and Euler simultaneously.} Using the unpaired entropy in
a gapped phase is a four-orders-of-magnitude error at low $T$: the suppression
is a property of the \emph{spectrum}, and at matched Fermi surfaces the entropy
ratio is $2.0\times10^{-4}$ at $T=5$ MeV and $\Delta=60$ MeV, while at
$\Mstar_s=300$ MeV the mismatch pushes the lowest branch from $60$ to $11.9$
MeV and the ratio only to $0.20$.

$T=0$ is a branch of \eqref{eq:phi}, not a limit of it: in a fully gapped phase
every Taylor coefficient in $T$ vanishes, so
$\phi(x)\to x$, $\phi'(x)\to\mathrm{sign}(x)$ and $\partial\phi/\partial T\to0$
are substituted rather than approached.

A state is flagged \textbf{gapless} when the smallest quasiparticle energy on
the quadrature grid has collapsed relative to the gap scale,
\begin{equation}
\min_{k,j,r} E^{(j),r}_\Delta < 10^{-3}\max_\eta\left|\Delta_\eta\right| .
\label{eq:gapless}
\end{equation}
A gapless solution is a real physical state, but it is not a stationary point
of the same functional as a fully gapped one, so comparing $\Omega$ across one
is not valid --- which is why it is \emph{reported} rather than silently
ranked.

Colour neutrality is imposed \emph{within} the pattern:
\begin{equation}
n_3 = \sum_f\left(n_{(f,r)}-n_{(f,g)}\right)=0,
\qquad
n_8 = \sum_f\left(n_{(f,r)}+n_{(f,g)}-2 n_{(f,b)}\right)=0 .
\label{eq:colour}
\end{equation}
These are the generator densities up to the constant factors $\tfrac12$ and
$\tfrac13$ of $T_3$ and $T_8$; a row that must vanish does not care about its
own normalisation, and this is the form the literature states. In an
\emph{unpaired} region $n_3$ and $n_8$ vanish identically at $\mu_3=\mu_8=0$
whatever else the state does, so the colour potentials are unconstrained there
and are pinned rather than solved for; a paired phase's $\mu_8$ cannot be
inherited from an unpaired solution.

\section{Assembly}
\label{sec:assembly}

Write the mode sums
$S_{\mathcal P}=\sum_j\mathcal P_j$, $S_{\mathcal E}=\sum_j\mathcal E_j$,
$S_{\mathcal S}=\sum_j\mathcal S_j$, the pairing cost
$D=\sum_\eta\Delta_\eta^2/4G_D$, and the vector field energy
$W=\tfrac12 m_\omega^2\omega_0^2$. The \textbf{matter block} --- quarks and
fields only, which is what a phase owns and what \code{eos.mixed} consumes ---
is
\begin{align}
\boxed{\;\Omega_{\rm matter} = U(\Phi) + V(\sigma,\zeta) - W - \Sigma_R\,n_q
 - S_{\mathcal P} + \delta\Omega_{\rm pair} + D\;}
\label{eq:Omega}\\
\boxed{\;\varepsilon_{\rm matter} = S_{\mathcal E} + U + V + W
 + \varepsilon_{\rm pair}\;}
\label{eq:eps}
\end{align}
with $P_{\rm matter}=-\Omega_{\rm matter}$,
\begin{equation}
\varepsilon_{\rm pair} = \delta\Omega_{\rm pair} + D
 + T\,\delta s_{\rm pair} + \sum_j \mustar_j\,\delta n_j,
\qquad
s_{\rm matter} = S_{\mathcal S} + \delta s_{\rm pair},
\qquad
\sum_j\mu_jn_j
\end{equation}
the last being taken over the \emph{physical} mode potentials \eqref{eq:mu},
not the shifted ones. The conserved-charge sums are
\begin{equation}
n_f = \sum_a n_{(f,a)},\qquad
n_B = \tfrac13\sum_f n_f = \tfrac13 n_q,\qquad
n_C = \sum_f q_f n_f,\qquad
n_S = n_s .
\label{eq:sums}
\end{equation}

\textbf{The signs.} $U+V$ enter $\varepsilon$ positively and $P$ negatively;
the vector field energy $W$ enters \emph{both} positively; the rearrangement
term $\Sigma_R n_q$ enters $P$ only. No vacuum subtraction appears anywhere,
because $U$ and $V$ both vanish at the physical vacuum by construction
(\S\ref{sec:parameters}).

The audit that must hold at every solved point, on the matter block, and does
to machine precision (measured $-7.0\times10^{-16}$ relative at
$n_B=1.5$ fm$^{-3}$, $T=0$, beta equilibrium) is
\begin{equation}
\boxed{\;\varepsilon_{\rm matter} + P_{\rm matter}
 = T s_{\rm matter} + \sum_j \mu_j n_j\;}
\label{eq:euler}
\end{equation}
together with $f=\varepsilon-Ts=-P+\sum_j\mu_jn_j$ and
$P=-\Omega$ computed from the two assemblies independently.

\subsection{The totals}
\label{sec:totals}

The sectors that are shared by the system rather than owned by the phase ---
leptons, photons, and the neutrino flavours not tracked in the composition ---
are added on top of the matter block:
\begin{align}
P &= P_{\rm matter} + \sum_\ell \mathcal P_\ell
 + [\gamma]\,\mathcal P_\gamma + [\nu]\,N_\nu\,\mathcal P_{\nu}(0,T),
\label{eq:Ptot}\\
\varepsilon &= \varepsilon_{\rm matter} + \sum_\ell \mathcal E_\ell
 + [\gamma]\,3\mathcal P_\gamma + [\nu]\,N_\nu\,\mathcal E_{\nu}(0,T),
\\
s &= s_{\rm matter} + \sum_\ell \mathcal S_\ell
 + [\gamma]\,\tfrac{4}{45}\pi^2T^3 + [\nu]\,N_\nu\,\mathcal S_{\nu}(0,T),
\qquad f = \varepsilon - Ts .
\label{eq:stot}
\end{align}
$[\gamma]$ and $[\nu]$ are the \code{photons} and \code{thermal\_neutrinos}
flags of \S\ref{sec:flags}, and the sum over $\ell$ runs over the electrons,
the muons where \code{muons} is set, and the trapped electron neutrinos where
$\mu_{\nu_e}\neq0$; every one of those is the ideal gas of
\S\ref{sec:leptons}. $N_\nu$ is the number of \emph{untracked} massless
$\mu=0$ neutrino flavours,
\begin{equation}
N_\nu = \begin{cases} 3 & \text{free-streaming } (\mu_{\nu_e}=0),\\
                      2 & \text{trapped},\end{cases}
\end{equation}
two in the trapped case because the electron flavour is then already counted
at its own potential. These carry no conserved charge and enter $P$,
$\varepsilon$ and $s$ only.

\subsection{The scalar density, and why it is not a trace identity}

The model returns per-flavour scalar densities $\rho_{s,f}$, and they are the
integral \eqref{eq:Rs} plus the pairing correction \eqref{eq:drhos}, computed
directly. \textbf{The identity $n_s = (\varepsilon-3P)/m^{*}$ does not apply
here} and is not used: it holds for a single ideal Fermi gas, whereas
$\varepsilon$ and $P$ of \eqref{eq:Omega}--\eqref{eq:eps} carry $U$, $V$, $W$,
$\Sigma_R n_q$ and the pairing terms, and there are three distinct $\Mstar_f$
rather than one $m^{*}$ to divide by. What replaces it as the audit is
\eqref{eq:euler} together with the per-mode relations \eqref{eq:modeeuler},
which is the stronger statement: $\rho_{s,f}$ is checked as
$-\partial\mathcal P/\partial\Mstar_f$, at every flavour, rather than as one
combination of totals. The quantity called $n_S$ in this model is
\emph{strangeness}, \eqref{eq:sums}, not a scalar density; the collision of
names is why this paragraph exists.

\subsection{Two corrections to the specification}
\label{sec:corrections}

\code{docs/ccdm\_implementation.md} is the authority for this model, and two
places in its assembly do not survive the audit \eqref{eq:euler} that its own
\S9.6 mandates.

\begin{enumerate}
\item Its \S4.3 writes $\varepsilon$ with $-\tfrac12 m_\omega^2\omega_0^2$.
\textbf{The sign must be $+$.} A repulsive vector interaction adds energy
density: the Hamiltonian density of the mean vector field is
$g_\omega\omega_0 n_q - \tfrac12 m_\omega^2\omega_0^2 = +\tfrac12
m_\omega^2\omega_0^2$ once \eqref{eq:omega} is used. This is the standard
density-dependent mean-field result, in which the scalar potentials enter
$\varepsilon$ positively and $P$ negatively while the vector term enters both
positively.

\item Its \S4.1 carries $\Sigma_R$ in $\mustar$ but omits the compensating
$-\Sigma_R n_q$ from $\Omega$. Without it, $n=-\partial\Omega/\partial\mu$ and
\eqref{eq:euler} both fail as soon as $g_\omega$ depends on the density. The
rearrangement term belongs in $\mu$ and $P$ and never in $\varepsilon$.
\end{enumerate}

With either error present the Euler residual is of order percent while every
other quantity still looks like a reasonable equation of state; with both
corrections it is $\sim10^{-16}$.

\section{The residual}
\label{sec:residual}

The unknown vector, in the order the solver assembles it, is
\begin{equation}
\vec X = \bigl(\Phi,\ \sigma,\ \zeta,\ [\Sigma_V],\
 [\Delta_\eta]_{\eta\in\rm free},\ [\mu_3,\mu_8],\
 \mu_B,\ \mu_C,\ [\mu_S],\ [\mu_{\nu_e}]\bigr),
\label{eq:unknowns}
\end{equation}
the bracketed entries being present only where the model or the mode has them:
$\Sigma_V$ where $\bar g_\omega\neq0$, the gaps as the pairing pattern
declares, the colour potentials whenever the pattern pairs at all, $\mu_S$ iff
the mode holds $Y_S$, and $\mu_{\nu_e}$ iff it holds $Y_{L_e}$. Where $\mu_S$
is \emph{not} an unknown it is zero, because strangeness-changing weak
reactions are fast on any astrophysical timescale.

The rows, in the same order:
\begin{align}
R_1 &= B_g\ln\Phi + \frac{p}{1-\Phi}\sum_f \Mstar_f\,\rho_{s,f} &&\text{dilaton}
\label{eq:R1}\\
R_2 &= \frac{\partial V}{\partial\sigma}
 + \frac{g_q}{\chi}\left(\rho_{s,u}+\rho_{s,d}\right) &&\text{light scalar}
\label{eq:R2}\\
R_3 &= \frac{\partial V}{\partial\zeta}
 + \frac{g_s}{\chi}\,\rho_{s,s} &&\text{strange scalar}
\label{eq:R3}\\
R_4 &= \Sigma_V - \left[g_\omega(n_B)\,\omega_0 + \Sigma_R\right] &&\text{vector}
\label{eq:R4}\\
R_{\Delta_\eta} &= \frac{\Delta_\eta}{2G_D} - \mathcal K_\eta
 \quad\text{[Eq.~\eqref{eq:gapeq}]} &&\text{one per free gap}\\
R_{n_3} = n_3,\quad R_{n_8} &= n_8
 \quad\text{[Eq.~\eqref{eq:colour}]} &&\text{colour neutrality}\\
R_{n_B} &= n_B - n_B^{\rm target} &&\text{the density}\\
&\quad\text{+ the mode's charge rows, \eqref{eq:chargerows}}. \nonumber
\end{align}
with $\mathcal K_\eta$ the Hellmann--Feynman kernel of \eqref{eq:gapeq}. The
charge rows are, in this order and only where the mode has them,
\begin{equation}
\begin{aligned}
&\text{$C$ held:}    &&n_C - Y_C\,n_B, &&\qquad
 \text{$C$ equilibrated:} &&n_C - \left[n_e(\mu_e,T)+n_\mu(\mu_\mu,T)\right],\\
&\text{$S$ held:}    &&n_S - Y_S\,n_B, &&\qquad
 \text{$L_e$ held:} &&n_e(\mu_e,T)+n_{\nu_e}(\mu_{\nu_e},T) - Y_{L_e}\,n_B,
\end{aligned}
\label{eq:chargerows}
\end{equation}
with $\mu_e,\mu_\mu$ from \eqref{eq:leptonmu}. Exactly one of the two $C$ rows
is present. In a fixed-fraction mode the neutralizing leptons of
\eqref{eq:neutralizing} are \emph{not} a row: they are solved after the matter,
from the charge the matter turned out to carry.

Equation \eqref{eq:R1} is $\partial\Omega/\partial\Phi$, obtained from
$\partial\Omega/\partial\phib$ by dividing out the Jacobian $4\phib^{\,3}$;
that division is exactly what removes the spurious root of
\S\ref{sec:Phi}. Equations \eqref{eq:R1}--\eqref{eq:R3} are minima of $\Omega$,
with the boundary minimiser $\sigma\to0$ admitted --- that is how chiral
restoration appears.

\textbf{$R_4$ carries $\Sigma_V$, not $\omega_0$.} The vector field is
circular: $\omega_0$ sets $\mustar$, which sets $n_B$, which sets $\omega_0$.
Carried as the total shift $\Sigma_V$ everything downstream is explicit ---
$\mustar$ from $\Sigma_V$, the densities from $\mustar$, $\omega_0$ and
$\Sigma_R$ from the densities --- and \eqref{eq:R4} is the single statement
that the returned field is the one the returned densities source. This is also
this repository's declared convention for a density-dependent coupling: the
unknown vector uses the effective potentials, so the rearrangement and the
large vector shift cancel out of the iteration.

\subsection{Row scales and the convergence gate}
\label{sec:scales}

Each row is divided by the scale of the quantity it balances \emph{before} it
reaches the root finder, so the vector handed to it is dimensionless:

\begin{center}
\begin{tabular}{lll}
\toprule
row & scale & what it is \\
\midrule
$R_1$ & $B_g$ & an energy density \\
$R_2,R_3$ & $|\epsilon_\sigma|,\ |\epsilon_\zeta|$
 & scalar densities, against what they balance in vacuum \\
$R_4$ & $\max(|\mu_B|,1)/3$ & a chemical potential \\
$R_{\Delta_\eta},R_{n_3},R_{n_8}$ & $\max\!\left[(\mu_B/3)^3/\pi^2,\,1\right]$
 & densities in MeV$^3$, against the quark scale \\
$R_{n_B}$, charge rows & $\max(n_B,10^{-3})$ & baryon densities in fm$^{-3}$ \\
\bottomrule
\end{tabular}
\end{center}

Unscaled, the rows span twenty-two orders of magnitude --- the dilaton row is
an energy density in MeV$^4$ against a charge row in fm$^{-3}$ --- and the
norm would report whichever carries the largest units.

The solve is Powell's hybrid method, then Levenberg--Marquardt, then (when a
warm start was given) one more hybrid attempt from the mode's own cold guess,
each with a bounded iteration count; the root finder's own tolerance is
$10^{-13}$, and \code{converged} is the separate repository-wide gate
\begin{equation}
\max_i \left|R_i\right|/\text{scale}_i < 10^{-10},
\end{equation}
so that ``converged'' means one thing in every model of this repository.
\textbf{Non-convergence is a return value}, never an exception and never a
hang: the returned point carries the best iterate reached and a status the
caller tests.

\subsection{The cold start}
\label{sec:guess}

With $n_q = \max(3n_B (\hbar c)^3, 1)$ MeV$^3$ and
$\mu_q=\max\!\left[(\tfrac12\pi^2 n_q)^{1/3},50\right]$ MeV --- the free
massless relation at one flavour per colour, floored so a vanishing density
does not give a vanishing potential --- the default guess is
\begin{equation}
\begin{aligned}
(\Phi,\sigma,\zeta) &= \text{the branch seed of Table~\ref{tab:branches}}, &
\Sigma_V &= 0, \\
\Delta_\eta &= \text{the pattern seed} \times \max(0.1\mu_q,20), &
(\mu_3,\mu_8) &= (0,\,-0.02\mu_q), \\
\mu_B &= 3\mu_q, &
\mu_C &= \begin{cases}0 & \text{CFL}\\ -0.05\mu_q & \text{otherwise,}\end{cases}\\
\mu_S &= -0.6\,\mu_q\left[1-\min(Y_S,1)\right], &
\mu_{\nu_e} &= 0.3\mu_q .
\end{aligned}
\end{equation}
Three of these are choices rather than conveniences. $\Sigma_V=0$ because
$R_4$ is nearly linear in $\omega_0$, so the first Newton step lands close and
a nonzero seed only risks starting on the far side of the density the coupling
is evaluated at. $\mu_C=0$ in CFL because CFL matter is electrically neutral
\emph{without} electrons, and seeded with an electron-bearing potential a CFL
solve converges to a spurious point. $\mu_S$ is seeded on the sign that
\emph{suppresses} strangeness: with $S=+1$ per $s$ quark the $s$ modes sit at
$\mustar_s = \mu_B/3-\mu_C/3+\mu_S-\Sigma_V$, so a negative $\mu_S$ is what
pushes them below their own effective mass, and seeded at zero the strange
sector starts fully populated.

That seed does \emph{not} make $Y_S=0$ converge and is not meant to. Once
$\Mstar_s$ rises above $\mustar_s$ the strange density is identically zero, the
strangeness row is satisfied for a whole \emph{range} of $\mu_S$, and its
column of the Jacobian vanishes, so the solve stalls on the threshold with
$\mu_S$ wherever its path reached. That is the general statement that a
potential is only pinned as tightly as its conjugate density responds, and it
is recorded in \code{docs/DEFERRED.md}.

A warm start replaces this seed with a converged neighbour's unknown vector.
The fields are \textbf{not} carried across a branch --- a branch \emph{is} a
choice of field root, so seeding the restored branch from the confined one's
fields hands it the very root it is meant to be an alternative to --- and a
seed belongs to the layout it was solved in, since a 2SC vector has one gap in
it and a CFL vector three.

\section{The internal system: the phase-adapter surface}
\label{sec:internal}

\code{eos.mixed} couples this model to a hadronic one through one function,
which maps $(\mu_B,\mu_C,\mu_S,T)$ to a thermodynamic block. That function
solves the model's own \emph{internal} self-consistency at those fixed
potentials and nothing else: the unknowns are
\begin{equation}
\vec X_{\rm int} = \bigl(\Phi,\ \sigma,\ \zeta,\ [\Sigma_V],\
 [\Delta_\eta]_{\eta\in\rm free},\ [\mu_3,\mu_8]\bigr),
\end{equation}
and the rows are $R_1,R_2,R_3,[R_4]$, the free gap equations and, where the
pattern pairs, the two colour-neutrality rows --- that is,
\S\ref{sec:residual} without the density row and without any charge row. The
scales are those of \S\ref{sec:scales} with $\mu_B$ the given potential.

Colour neutrality stays \emph{inside}: it is a structural property of a
colour-superconducting phase rather than a condition a caller chooses, so the
engine never learns that $\mu_3$, $\mu_8$ or a dilaton exist. The branch and
the pattern are \emph{declared} to this function, not discovered by it;
choosing between them is a comparison across separate self-consistent solves
and belongs to the caller (\S\ref{sec:branches}).

\section{Branches and patterns: what is enumerated}
\label{sec:branches}

Neither the chiral/dielectric \textbf{branch} nor the pairing \textbf{pattern}
is a mode: both are decided by which candidate minimises the free energy.

Below the deconfinement onset two chiral branches coexist at fixed dilaton ---
a confined one, where $\sigma\simeq f_\pi$, the dielectric is nearly opaque and
the quarks are too heavy to appear at all, and a restored one, where $\sigma$
has collapsed and the quarks are present. \textbf{A solver that alternates
between updating $\sigma$ and $\omega_0$ two-cycles between them} and exits
with a mixed state --- $\sigma$ from one branch, $\omega_0$ from the other ---
which reads as a spuriously deep minimum at zero quark density. So each branch
is seeded separately, solved to self-consistency, and compared; a branch that
fails to converge is reported missing, never replaced by a
neighbour, because substituting a converged neighbouring point is how a fake
phase boundary appears.

\begin{table}[h]
\centering
\caption{The three chiral/dielectric branch seeds, as
$(\Phi,\ \sigma/\sigma_0,\ \zeta/\zeta_0)$. The fractions are of the
\emph{vacuum} condensates, so a parameter point with a different $m_\sigma$ or
$f_K$ is seeded to its own vacuum rather than to the shipped one.}
\label{tab:branches}
\begin{tabular}{llll}
\toprule
branch & seed & at fixed $\mu$ & at fixed $n_B$ \\
\midrule
\code{confined} & $(1-10^{-6},\ 1,\ 1)$ & yes & \textbf{no} \\
\code{restored} & $(0.4^4,\ 0,\ 0)$ & yes & yes \\
\code{partial}  & $(0.4^4,\ 0.5,\ 0.7)$ & yes & yes \\
\bottomrule
\end{tabular}
\end{table}

The confined seed sits just inside the guard \eqref{eq:phiguard}: starting
exactly at $\Phi=1$ overflows $\Mstar$ rather than approaching it. The
partially restored seed exists because the transition is first order and the
intermediate root is a real one over a window of densities, not because the
other two sometimes fail.

The confined branch is enumerated at fixed \emph{potential}, where it is what
the deconfined branch must beat --- it carries no quarks, so its pressure is
exactly zero, and the onset is where the deconfined pressure crosses it ---
and \emph{not} at fixed density: with the dielectric closed $n_B=0$
identically, so no nonzero density row can be met and the Jacobian's field
columns all vanish.

\textbf{Which potential decides depends on what is held.} At fixed potential
the winner is the largest $P$, i.e. the smallest $\Omega$; at fixed density the
winner is the smallest $f=\varepsilon-Ts$, which is the right potential there.
The code uses each in its own place, and a candidate that did not converge is
dropped rather than substituted.

\begin{table}[h]
\centering
\caption{The pairing patterns: which $\Delta_\eta$ are unknowns, and the
relative seed each is given. $\eta=1,2,3$ pair $(ds),(us),(ud)$. The default
enumeration at \code{csc=True} is \code{unpaired}, \code{2SC}, \code{CFL},
\code{free}; with \code{csc=False} only \code{unpaired} exists.}
\label{tab:patterns}
\begin{tabular}{lll}
\toprule
pattern & free gaps & seed $(\Delta_1,\Delta_2,\Delta_3)/\Delta_{\rm scale}$ \\
\midrule
\code{unpaired} & none                     & $(0,0,0)$ \\
\code{2SC}      & $\Delta_3$               & $(0,0,1)$ \\
\code{uSC}      & $\Delta_2,\Delta_3$      & $(0,0.6,1)$ \\
\code{dSC}      & $\Delta_1,\Delta_3$      & $(0.6,0,1)$ \\
\code{CFL}      & $\Delta_1,\Delta_2,\Delta_3$ & $(1,1,1)$ \\
\code{free}     & $\Delta_1,\Delta_2,\Delta_3$ & $(0.3,0.6,1)$ \\
\bottomrule
\end{tabular}
\end{table}

A pattern is a declaration of which $\Delta_\eta$ are unknowns, and it adds no
code. The gap equation has three roots at any Fermi-surface mismatch (zero, a
barrier maximum, and the physical BCS root), so which root a solve lands on is
decided by the seed; enumerating the seeds is what makes the answer an answer
rather than one solve repeated. \code{free} is the asymmetric seed that lets a
CFL-layout solve fall to something that is not CFL. Gapless states,
\eqref{eq:gapless}, are \emph{flagged} rather than ranked.

The candidate set of a fixed-density solve is the \emph{product} of the two
enumerations, because which pattern survives depends on the strange quark's
effective mass, which is a property of the branch.

\textbf{The first-order transition is real physics.} Between the onset and the
point where the deconfined branch turns around, $\mathrm dP/\mathrm dn_B<0$:
the mechanically unstable side. A raw branch may carry it; a construction
(Maxwell, Gibbs, or the $\eta$-mixed phase) removes it before any table reaches
a structure solver. Sound speeds must be taken one-sided on each branch, never
across the transition. At the shipped parameter point the deconfined pressure
crosses zero near $n_B\simeq1.35$ fm$^{-3}$; below the onset there is no
deconfined root at fixed density at all, and a solve there returns a status
rather than a fabricated state.

\section{Species flags}
\label{sec:flags}

\begin{center}
\begin{tabular}{lll}
\toprule
flag & default & what it does \\
\midrule
\code{csc} & \code{False}
 & the three gaps become unknowns, $\delta\Omega_{\rm pair}$ and all the \\
 & & corrections of \S\ref{sec:paired} enter, and $\mu_3,\mu_8$ are solved
 from \eqref{eq:colour} \\
\code{muons} & \code{True} & the muon family, at \eqref{eq:leptonmu} \\
\code{thermal\_neutrinos} & \code{False}
 & the untracked flavours as $\mu=0$ gases; $P,\varepsilon,s$ only \\
\code{photons} & \code{False} & \eqref{eq:photon}; $P,\varepsilon,s$ only \\
\midrule
\code{hyperons} & \code{False} & fixed: there are no baryons here to be strange \\
\code{deltas} & \code{False} & fixed: no baryon resonances in a quark model \\
\code{thermal\_mesons} & \code{False}
 & fixed: $\sigma$, $\bm\pi$, $\zeta$ and $\varphi$ \emph{are} the mean \\
 & & fields that give the quarks their masses \\
\bottomrule
\end{tabular}
\end{center}

The three fixed flags \textbf{raise} if set otherwise; they are never quietly
ignored. Giving the scalars a thermal population would double-count the
condensate they already are, and the mesonic fluctuations that would populate
them are beyond mean field.

\section{Modes}
\label{sec:modes}

The closure rows of the specification map onto the repository's four modes.
\textbf{The specification labels them $R1\ldots R5$; they are relabelled
$M1\ldots M5$ here}, because $R_1\ldots R_4$ are the residual rows of
\S\ref{sec:residual} and one document cannot carry two meanings of one symbol.

\begin{table}[h]
\centering
\begin{tabular}{lllp{5.6cm}}
\toprule
 & mode & variables & rows added to \eqref{eq:R1}--\eqref{eq:R4} and $R_{n_B}$ \\
\midrule
M1 & \code{beta\_eq\_neutrinoless} & $(n_B,T)$ &
 $n_C=n_e+n_\mu$. $\mu_S=0$, $\mu_e=-\mu_C$, no $\mu_{\nu_e}$ \\
M3 & \code{beta\_eq\_neutrino\_trapped} & $(n_B,Y_{L_e},T)$ &
 $n_C=n_e+n_\mu$ and $n_e+n_{\nu_e}=Y_{L_e}n_B$; $\mu_{\nu_e}$ joins
 \eqref{eq:unknowns}; $\nu$ with $g=1$ \\
M2 & \code{fixed\_YC}, \code{leptons=True} & $(n_B,Y_C,T)$ &
 $n_C=Y_Cn_B$; the leptons then solve \eqref{eq:neutralizing} \emph{after}
 the matter. \textbf{No} weak equilibrium \\
M4 & \code{fixed\_YC\_YS}, \code{leptons=False} & $(n_B,Y_C,Y_S,T)$ &
 $n_C=Y_Cn_B$ and $n_S=Y_Sn_B$; $\mu_S$ joins \eqref{eq:unknowns}; no leptons
 at all \\
M5 & \code{fixed\_YC\_YS} at $Y_C=\tfrac12,Y_S=0$ & $(n_B,T)$ &
 as M4: symmetric matter is a parameter choice, not a fifth mode \\
\bottomrule
\end{tabular}
\end{table}

M2 is the one to state twice: it imposes total electric neutrality
\emph{without} imposing beta equilibrium, which is what merger and
core-collapse matter is on a dynamical timescale. \textbf{Weak equilibrium is a
per-row closure, never an identity built into $\Omega$}; hardwiring it would
make such matter unrepresentable. $Y_C$ and $Y_S$ are per baryon, which differ
by a factor three from the per-quark $n_s/(3n_B)$ often plotted.

\code{leptons=False} is refused in the two beta modes, which are \emph{defined}
by their leptons. The muon lepton family is not available as a conserved
charge: \code{beta\_eq\_neutrino\_trapped} takes $(n_B,Y_{L_e},T)$ and a
$Y_{L_\mu}$ raises (\code{docs/DEFERRED.md}); the muon \emph{species} is
available through the flag.

Wherever a temperature is accepted, entropy per baryon is accepted in its
place, through an outer one-dimensional solve for $T$ of $s/n_B = (s/n_B)_{\rm
target}$, since $s$ is not a variable the residual carries.

\section{Parameters}
\label{sec:parameters}

Three tiers, and the split is what makes the model usable in an inference run:
tier 1 is fixed by vacuum data and never sampled, tier 2 is structural and
declared per run, tier 3 is the Bayesian vector. Everything is an argument;
nothing is module state.

\subsection{Tier 1: fixed by vacuum data}

\begin{center}
\begin{tabular}{llll}
\toprule
symbol & value & what it is & source \\
\midrule
$f_\pi$    & $93$ MeV   & pion decay constant & \cite{ParticleDataGroup2024} \\
$m_\pi$    & $138$ MeV  & pion mass (isospin average) & \cite{ParticleDataGroup2024} \\
$f_K$      & $113$ MeV  & kaon decay constant & \cite{ParticleDataGroup2024} \\
$m_K$      & $496$ MeV  & kaon mass & \cite{ParticleDataGroup2024} \\
$m_u,m_d$  & $5$ MeV    & current masses & \cite{ParticleDataGroup2024} \\
$m_s$      & $95$ MeV   & current mass & \cite{ParticleDataGroup2024} \\
$m_\zeta$  & $980$ MeV  & the strange scalar, $f_0(980)$ & \cite{ParticleDataGroup2024} \\
$m_\varphi$& $1600$ MeV & the scalar glueball & \cite{DragoFiolhaisTambini1996} \\
$m_\omega$ & $783$ MeV  & the vector meson & \cite{ParticleDataGroup2024} \\
\bottomrule
\end{tabular}
\end{center}

Everything below follows in closed form, so the scalar sector has no free
normalisation left once tier 1 and $(m_\sigma, B_g)$ are chosen:
\begin{align}
\sigma_0 &= f_\pi, &
\zeta_0 &= \sqrt2 f_K - \frac{f_\pi}{\sqrt2}, &
\varphi_0 &= \frac{4\sqrt{B_g}}{m_\varphi}, \\
\epsilon_\sigma &= f_\pi m_\pi^2, &
\epsilon_\zeta &= \sqrt2 f_K m_K^2 - \frac{f_\pi}{\sqrt2}m_\pi^2, & & \\
\lambda &= \frac{m_\sigma^2-m_\pi^2}{2f_\pi^2}, &
v^2 &= f_\pi^2 - \frac{m_\pi^2}{\lambda}, & & \\
\lambda_\zeta &= \frac{m_\zeta^2-\epsilon_\zeta/\zeta_0}{2\zeta_0^2}, &
v_\zeta^2 &= \zeta_0^2 - \frac{\epsilon_\zeta}{\lambda_\zeta\zeta_0}, & &
\end{align}
and $C_0$ from $V(\sigma_0,\zeta_0)=0$. Numerically at the shipped
$m_\sigma=550$ MeV and $B_g^{1/4}=150$ MeV:
$\sigma_0=93$ MeV, $\zeta_0=94.0452$ MeV,
$\epsilon_\sigma=1.77109\times10^{6}$ MeV$^3$,
$\epsilon_\zeta=3.80625\times10^{7}$ MeV$^3$,
$\lambda=16.3866$, $v^2=7486.83$ MeV$^2$ ($v=86.5265$ MeV),
$\lambda_\zeta=31.4135$, $v_\zeta^2=-4039.30$ MeV$^2$,
$C_0=2.43517\times10^{9}$ MeV$^4$, $\varphi_0=56.25$ MeV,
$B_g=5.0625\times10^{8}$ MeV$^4$.

\textbf{$v_\zeta^2$ is negative} at the baseline $m_\zeta$: the strange quartic
is convex, explicit breaking dominating, so the strange sector does not break
chirally on its own in this truncation. The sign flips between $m_\zeta=1100$
and $1150$ MeV. Never assume it is positive and never write $v_\zeta$ as a
square root --- it appears only inside the bracket of \eqref{eq:V}, squared off.

\subsection{Tier 2: structural}
\label{sec:tier2}

$p>0$, shipped at $p=1$ --- $\chi$ and $p$ are meaningful only as the pair
$\chi^p$, which is why $p$ and not the bracket is the knob: the anomaly
argument above fixes the \emph{fourth power inside}
the bracket, that combination being the gluon condensate, and says nothing
about the power of the bracket itself. Both endpoints are $p$-independent,
$\chi(0)=1$ and $\chi(\bar\phi=1)=0$ for every $p>0$, so $p$ moves only the
approach: a larger $p$ keeps the dielectric shut further from the vacuum
dilaton, making $M^*$ heavier at given $\bar\phi$, while $p<1$ opens the
medium while $\bar\phi$ is still near unity.

What $p$ moves is the dilaton \emph{profile} rather than, mainly, the onset.
At the shipped $B_g^{1/4}=150$~MeV with $\bar g_\omega=4$, in $\beta$
equilibrium at $T=0$, $\bar\phi$ at $n_B=1.5$~fm$^{-3}$ is $0.598$, $0.405$,
$0.176$, $0.074$ for $p=0.5,1,2,3$ --- a factor of eight --- while the density
at which $P$ crosses zero moves only $1.35\to1.40$~fm$^{-3}$ between $p=1$ and
$p=3$, and the lowest density carrying a deconfined solution at all moves
$1.00\to0.80$. $B_g$ stays the onset knob ($150\to230$~MeV moves that floor
$1.00\to1.65$~fm$^{-3}$); $p$ is the knob on how fast the medium opens once it
has. Below unity it changes the character rather than the numbers: at $p=0.5$
the pressure is positive over the whole axis, so the phase is self-bound and
there is no $P=0$ crossing to be an onset at all.
Only the dielectric and the dilaton
residual $R_1$ read it, and $R_1$ carries it analytically, so no other equation
changes with it. $q\in\{0,1\}$, shipped at $q=0$: the dielectric dressing
\eqref{eq:GDdress} of the diquark coupling, declared per run rather than
sampled, with $q=1$ the gluon-exchange exponent and the largest that $q\le p$
admits --- a constraint that now binds, since $p$ moves.

\subsection{Tier 3: the sampled vector, and the shipped values}

\begin{center}
\begin{tabular}{lllp{5.3cm}}
\toprule
symbol & shipped & prior support & status \\
\midrule
$B_g^{1/4}$ & $150$ MeV & $120$--$250$ MeV
 & the glue bag scale; sets $\varphi_0$ and the onset \\
$g_q$ & $3.0$ & $3$--$6$
 & \textbf{pinned}: the specification's \S10 table quotes
   $\Mstar_{u,d}=826$ MeV at $\phib=0.90$ and $1531$ MeV at $0.95$ in the
   confined branch, and both invert to $g_q=3.00$ \\
$g_s$ & $3.0$ & $3$--$8$
 & \emph{not} pinned; $3.0$ is the flavour-symmetric choice $g_s=g_q$ \\
$m_\sigma$ & $550$ MeV & $450$--$700$ MeV & fixes $\lambda$ and $v$ \\
$\bar g_\omega$ & $4.0$ & $0$--$12$
 & \emph{not} pinned; mid-prior. $0$ switches the vector sector off (the
   L1 $\to$ L0 reduction) \\
$n_c$ & $1.0$ fm$^{-3}$ & $0.3$--$3$ fm$^{-3}$
 & \emph{not} pinned; mid-prior \\
$G_D$ & $5\times10^{-6}$ MeV$^{-2}$ & (with $\Lambda$) 
 & calibrated here, not quoted: at this value the gap sits inside the
   specification's $20$--$150$ MeV window at $\mu_q\simeq450$ MeV --- $30.0$
   MeV in 2SC and $119.4$ MeV in CFL at $\mu_B=1450$, $\mu_C=-30$ MeV,
   $T=0$. Below $\sim4.5\times10^{-6}$ the 2SC gap equation has no root but
   the trivial one there \\
$\Lambda$ & $600$ MeV & $550$--$800$ MeV
 & the \textbf{pairing} cutoff only; nearly degenerate with $G_D$ \\
\bottomrule
\end{tabular}
\end{center}

Three of these eight are calibration knobs at mid-prior values rather than
measurements, and the code says so rather than dressing them otherwise.
\paragraph{The vector sector has an awkward middle, and it is the
rearrangement term.} $\Sigma_R=(\mathrm{d}g_\omega/\mathrm{d}n_B)\,\omega_0
n_B$ is negative wherever the coupling falls, and it enters $P$ (never
$\varepsilon$) as $+\Sigma_R n_q$: a \emph{negative} pressure growing with
both $\omega_0$ and $|\mathrm{d}g/\mathrm{d}n_B|$. At small $n_c$ the coupling
has already collapsed by the onset and both stay small; at $n_c=\infty$ the
term vanishes identically. In between it is neither: at $n_c=3$~fm$^{-3}$ and
$\bar g_\omega=4$, in $\beta$ equilibrium at $T=0$, $\Sigma_R n_q$ reaches
$-3400$~MeV/fm$^3$ by $n_B=2.6$~fm$^{-3}$ and $\mathrm{d}P/\mathrm{d}n_B<0$
over the whole branch --- mechanically unstable rather than soft. The
constant-coupling reference is stiff but well behaved, $c_s^2\in[0.68,0.80]$
over $n_B=1.1$--$2.6$~fm$^{-3}$ at $\bar g_\omega=4$, so the density-dependent
form is a choice about stiffness and not a repair of a causality problem.

$m_\omega$ is a normalisation convention, only $g_\omega/m_\omega$ entering;
$m_\varphi$ cancels from the bulk equation of state at fixed $B_g$, pricing
gradients and the glueball spectrum instead.

$\Lambda$ implies a declared validity ceiling for the pairing sector,
\begin{equation}
\mu_{\rm ceiling} = \sqrt{\Lambda^2+m_s^2} = 607.47\ \text{MeV},
\end{equation}
above which the paired Fermi surface has left the region the sharp cutoff
describes. A point past it is \emph{flagged and returned}, not refused: the
unpaired thermodynamics is untouched by it, and a sampler must be able to score
the point.

\subsection{The published sets}

\begin{center}
\begin{tabular}{ll}
\toprule
name & what it is \\
\midrule
\code{baseline} & the shipped default above; \code{Parameters.default()} \\
\code{novector} & $\bar g_\omega=0$: the L1 $\to$ L0 reduction \\
\code{dressed}  & $q=1$: the gluon-exchange dressing \eqref{eq:GDdress} \\
\code{stiff}    & $B_g^{1/4}=190$ MeV: a heavier glue scale, later onset \\
\code{constvector} & $n_c=\infty$: the vector coupling stops depending on the density \\
\bottomrule
\end{tabular}
\end{center}

The four levels the specification names are what the flags and a zero coupling
select, not separate models: L0 quarks in the dielectric with no vector and no
pairing, L1 adds the density-dependent repulsion ($\bar g_\omega\neq0$), L2 is
L1 at finite temperature, L3 adds colour superconductivity (\code{csc=True}).

\subsection{The effective bag constant}
\label{sec:bag}

Not an input. At the physical vacuum the field energy is zero by construction;
at the perturbative point it is
\begin{equation}
B_{\rm eff} = \underbrace{U(0)-U(\varphi_0)}_{B_g}
 + \underbrace{V(0,0)-V(f_\pi,\zeta_0)}_{B_\chi},
\end{equation}
with $U$ from \eqref{eq:U} and $V$ from \eqref{eq:V}, both written out above,
so nothing here is named without being defined. At $B_g^{1/4}=150$,
$m_\sigma=550$, $m_\zeta=980$ MeV this gives $B_\chi^{1/4}=229.89$ MeV and
$B_{\rm eff}=(239.66\ \mathrm{MeV})^4=429.39$ MeV\,fm$^{-3}$.
\textbf{The chiral sector supplies the larger part}: quoting $B_g$ alone as
``the bag constant'' of this model is wrong by a factor of six in energy
density.

\paragraph{Three routes to a parameter set.} Model parameters are always
arguments, so all three have to exist. \emph{By name:}
\texttt{Parameters.default()} is the baseline set, and
\texttt{Parameters.named(name)} takes any of the five published sets ---
\texttt{baseline}, \texttt{dressed}, \texttt{novector}, \texttt{stiff},
\texttt{constvector} --- an
unknown name raising \texttt{KeyError} that lists them. \emph{A new set:} every
field carries a default, so \texttt{Parameters(g\_q=..., B\_g4=...)} names only
what changes; the dataclass is frozen, so \texttt{dataclasses.replace} is how a
set already in hand is modified. \emph{From nuclear-matter parameters:} no
route, and none is missing --- CCDM has no nuclear sector, so there is no
\texttt{nmp.py} and nothing to invert; the tier-1 parameters are fixed by
vacuum data instead.

\section{What is returned}
\label{sec:returns}

\subsection{One point}

A point carries a convergence status first --- \code{ok}, a message, and the
point itself, which is present even when \code{ok} is false so that a caller
can see where the solve got to. The point carries:

\begin{center}
\begin{longtable}{lll}
\toprule
group & fields & units \\
\midrule
\endhead
status & \code{converged}, \code{error} (the scaled residual of
 \S\ref{sec:scales}), \code{mode} & \\
state point & \code{n\_B}, \code{T}, \code{Y\_C}, \code{Y\_S}, \code{Y\_L}
 & fm$^{-3}$, MeV, --- \\
the answer & \code{branch}, \code{pattern}, \code{Delta} $=(|\Delta_\eta|)$,
 \code{gapless}, \code{beyond\_cutoff} & MeV \\
fields & \code{phi\_bar} $=\Phi^{1/4}$, \code{chi}, \code{sigma},
 \code{zeta}, \code{omega\_0}, \code{Sigma\_R} & ---, MeV \\
masses & \code{M\_star} $=(\Mstar_u,\Mstar_d,\Mstar_s)$ & MeV \\
potentials & \code{mu\_B}, \code{mu\_C}, \code{mu\_S}, \code{mu\_3},
 \code{mu\_8}, \code{mu\_e}, \code{mu\_nu} & MeV \\
densities & \code{n\_u}, \code{n\_d}, \code{n\_s}, \code{n\_e},
 \code{n\_mu}, \code{n\_nu} & fm$^{-3}$ \\
totals & \code{P\_total}, \code{e\_total}, \code{f\_total} $=\varepsilon-Ts$;
 \code{s\_total} & MeV\,fm$^{-3}$; fm$^{-3}$ \\
fractions & \code{Y\_u}, \code{Y\_d}, \code{Y\_s}, \code{Y\_e}, \code{Y\_nu}
 & --- \\
internals & \code{state} (the matter block below), \code{x} (the unknown
 vector \eqref{eq:unknowns}, the warm start) & \\
\bottomrule
\end{longtable}
\end{center}

Five of those are part of the \emph{answer} rather than diagnostics.
\code{branch} and \code{pattern} say which candidate won; \code{Delta} are the
gap magnitudes, zero where the pattern does not pair; \code{gapless} says
\eqref{eq:gapless} fired, so ranking by $\Omega$ across this point is not
valid; \code{beyond\_cutoff} says $\max_j|\mustar_j| > \mu_{\rm ceiling}$.

The matter block on \code{state} carries, in natural units, everything of
\S\ref{sec:assembly} before the leptons: $T$, $\Phi$, $\phib$, $\chi$,
$\sigma$, $\zeta$, $\omega_0$, $\Sigma_V$, $\Sigma_R$, $\Mstar_f$,
$\Delta_\eta$, the five conserved-charge potentials, the nine $\mu_j$ and
$\mustar_j$, $\rho_{s,f}$, the nine $n_j$, the three $n_f$, $n_q$, $n_B$,
$n_C$, $n_S$, $n_3$, $n_8$, $U$, $V$, $\Omega$, $P$, $\varepsilon$, $s$,
$\sum_j\mu_jn_j$, the residual arrays $(R_1,\ldots,R_4)$ and
$R_{\Delta_\eta}$, the branch and pattern it was solved in, \code{gapless},
\code{valid} (all $\Mstar_f>0$), $\delta\Omega_{\rm pair}$ and $D$; plus the
fm-based conversions of $n_B$, $n_C$, $n_S$, $P$, $\varepsilon$, $s$ and the
Euler residual \eqref{eq:euler} as a callable audit.

\subsection{A table}

A table is a set of \emph{lines} --- one per temperature (or entropy per
baryon) and per combination of the fractions the mode fixes --- each swept
along the baryon density with a warm start, the winning candidate's seed
carried forward and the others left cold, so that a candidate seeded only from
itself can never fail to be displaced. A missed density step is bisected back
towards the last solved point, at most six times; the thresholds that earn it
are the strange quark's onset, the pairing onset, and the branch change, where
the fields move discontinuously. A non-converged point is dropped from its line
rather than aborting the table, which is what the sub-onset densities need.

Each row carries \code{n\_B}, \code{T}, \code{chi} $=1$ and \code{phase}
$=$ \code{'Q'} (the shared mixed-phase columns: this matter is entirely
deconfined), \code{P}, \code{eps}, \code{s}, \code{S\_per\_B}, \code{mu\_B},
\code{mu\_C}, \code{mu\_S}, \code{mu\_e}, \code{Y\_C}, \code{Y\_S},
\code{Y\_u}, \code{Y\_d}, \code{Y\_s}, \code{Y\_e}, \code{Y\_mu-},
\code{M\_u}, \code{M\_d}, \code{M\_s}, \code{branch}, \code{pattern},
\code{gapless}, \code{beyond\_cutoff}, \code{phi\_bar}, \code{chi\_diel},
\code{sigma}, \code{zeta}, \code{omega\_0}, \code{Sigma\_R}, \code{Delta\_1},
\code{Delta\_2}, \code{Delta\_3}, \code{mu\_3}, \code{mu\_8}, and where the
neutrinos are trapped \code{Y\_nue} and \code{mu\_nue}. \textbf{\code{chi} in a
table row is the mixed-phase quark volume fraction}, the column name every
quark and hadronic table shares; this model's dielectric is \code{chi\_diel},
and the collision is why it carries a suffix.

Progress is reported through the repository's shared callback, once per
completed line, with the dictionary
\{\code{mode}, \code{line}, \code{n\_lines}, \code{temp\_key}, \code{temp},
\code{fracs}, \code{n\_solved}, \code{n\_requested}, \code{elapsed\_s}\}
and two keys added here, \code{branch} and \code{pattern} --- which phase a
line ended in is the thing a reader of this table wants first. Deep solver code
never prints.

\subsection{Response functions}
\label{sec:response}

The quantities are those of the CompOSE manual \cite{TypelCompOSE2015}
\S3.6. They are taken by central differences along re-solved sequences:
every neighbour of the stencil re-runs the whole equilibrium \emph{and both
enumerations}. Only one conditioning is implemented, \code{frozen='equilibrium'}
--- nothing is held, the composition and both enumerations re-equilibrate ---
and any other raises. With $\varsigma\equiv s/n_B$ the entropy per baryon, and
every partial derivative taken along the mode's own sequence,
\begin{align}
c_{s,\rm iso}^2 &= \left(\frac{\partial P}{\partial n_B}\right)_T
 \Big/ \left(\frac{\partial\varepsilon}{\partial n_B}\right)_T,
&
C_V &= T\left(\frac{\partial\varsigma}{\partial T}\right)_{n_B},
\\
C_P &= T\left[\left(\frac{\partial\varsigma}{\partial T}\right)_{n_B}
 - \left(\frac{\partial P}{\partial T}\right)_{n_B}
 \frac{(\partial\varsigma/\partial n_B)_T}{(\partial P/\partial n_B)_T}\right],
&
c_{s,\rm ad}^2 &= \frac{C_P}{C_V}\,c_{s,\rm iso}^2,
\\
\Gamma_{\rm th} &= 1 + \frac{P(n_B,T)-P(n_B,0)}
 {\varepsilon(n_B,T)-\varepsilon(n_B,0)}. & &
\end{align}
Both sound speeds are named for the thermal variable they hold, never as a
bare $c_s^2$ whose meaning would depend on the arguments. At $T=0$ the ratio
$C_P/C_V$ is $1$ by construction and the two coincide, so the cold limit needs
no special case; $C_V$, $C_P$ and $\Gamma_{\rm th}$ are returned at $T>0$ only.
$\Gamma_{\rm th}$ returns NaN where the hot and cold states are on different
candidates, since the difference is then not a thermal one at all.

A sixth quantity is returned, and it is a warning rather than a number:
\code{branch\_changed} says whether the density stencil straddled a branch or
pattern change, in which case every value above is a \emph{chord across a
first-order jump} rather than a tangent and the derivative should be retaken
one-sided by restricting the enumeration. There is no way to see that from the
returned number alone, which is why it is returned rather than left to the
caller to suspect. In a fully gapped phase $C_V$ is exponentially small at low
$T$ --- the paired entropy goes as $e^{-\Delta/T}$ --- and that suppression is
physics, not a numerical failure: it is what makes a colour superconductor cool
differently from unpaired quark matter.

$c_{s,\rm iso}^2$ \emph{may come out negative} on a raw branch between the
onset and the turnaround, because $\mathrm dP/\mathrm dn_B$ is genuinely
negative there. That is the mechanically unstable side of the first-order
transition, and what removes it is a construction, applied before any table
reaches a structure solver.

The composition freezes of the repository's response contract --- held $Y_i$,
held $Y_C$, held $\Delta_\eta$, held fields --- and the susceptibility matrix
$\chi_{ab}=\partial n_a/\partial\mu_b$ are \textbf{not} implemented here and
are recorded in \code{docs/DEFERRED.md}: holding a composition needs the
species fractions carried through the solve as constraints, and holding the
gaps or the fields needs them fixed against their own equations.

\section{What is not in the model}

The dilaton gradient and finite-size terms (this is a bulk, homogeneous mean
field). The de Carvalho contact coupling $h(\varphi)$ \cite{deCarvalho2010} as
a replacement for $G_D$: their construction is sound --- integrating out the
confining field's fluctuation at Gaussian order gives a contact interaction,
which is why $\mathcal L_{\rm pair}$ is a legitimate leading term rather than
an ad hoc addition, and it gives ``zero range'' the physical meaning $1/M_\chi$
--- but what it predicts is a coupling negligible where the quarks are light
and enormous where they are heavy, which removes pairing from the deconfined
phase where a star's core lives and would condense a diquark in the confining
vacuum. It carries $q=4$ at $p=1$, violating $q\le p$. What is taken from it is
the argument, not the number.

The muon lepton family as a conserved charge (\S\ref{sec:modes}), and every
response conditioning but \code{equilibrium} (\S\ref{sec:response}). Both are
in \code{docs/DEFERRED.md}.

\section{The self-bound surface and the two-flavour arm}
\label{sec:zeropressure}

\subsection{The quantity}

A parametrization whose pressure crosses zero at finite density describes
\emph{self-bound} matter: the phase ends there, with no crust below it. What is
reported at that endpoint is the energy per baryon,
%
\begin{equation}
  \frac{E}{A} \;=\; \frac{\varepsilon}{n_B}
  \qquad\text{at}\qquad P(n_B) = 0,\quad T = 0 ,
\end{equation}
%
in MeV. The entry point is \texttt{zero\_pressure\_point(par, species)}, which
returns $n_B$, $E/A$, $\mu_B$, $Y_S$, $\mu_S$, the residual of
Eq.~\eqref{eq:gibbsidentity}, the pressure actually reached, the flavour
content requested, and whether $E/A$ fell below the $930.4\;$MeV of iron.

\subsection{The identity that makes the read self-checking}

At $T = 0$ the Euler relation is $\varepsilon + P = \sum_i \mu_i n_i$, so at
$P = 0$ the energy per baryon \emph{is} the Gibbs energy per baryon. Expanding
the species potentials in the conserved-charge basis,
$\mu_i = B_i\mu_B + C_i\mu_C + S_i\mu_S$, and imposing beta equilibrium
$\mu_C + \mu_e = 0$ together with total electric neutrality $n_C = n_e$, the
charge and lepton terms cancel,
%
\begin{equation}
  \sum_i \mu_i n_i
    = \mu_B n_B + \underbrace{\mu_C n_C + \mu_e n_e}_{=\,0} + \mu_S n_S ,
\end{equation}
%
leaving
%
\begin{equation}
  \boxed{\;\frac{E}{A} \;=\; \mu_B + Y_S\,\mu_S\;} ,
  \qquad Y_S = \frac{n_S}{n_B} .
  \label{eq:gibbsidentity}
\end{equation}

\textbf{The full form is the one to use.} $E/A = \mu_B$ is the special case
$Y_S\mu_S = 0$. It holds in every beta-equilibrium mode, where strangeness
self-equilibrates and $\mu_S = 0$, and it fails in a colour-flavour locked
phase: locking pairs the three flavours at equal densities, and equal densities
at unequal masses require unequal potentials. On the CFL surface of
\texttt{eos.alphabag} at $\Delta_0 = 100\;$MeV one finds
$\mu_S = 40.68\;$MeV, so $\mu_B = 895.87\;$MeV against
$E/A = 936.55\;$MeV. There are not two conventions, only one identity and the
cases in which a term of it vanishes. Equation~\eqref{eq:gibbsidentity} is
checked at every located root: a root that misses it is a root of something
other than $P$.

\subsection{Locating the root}

\texttt{eos.general.zero\_pressure.locate\_zero\_pressure} samples $P(n_B)$ on
a grid, takes the \emph{lowest} density at which $P$ rises through zero, and
refines it with Brent's method. The scan is not a convenience: $P(n_B)$ may
cross zero more than once, and a crossing at which $P$ \emph{falls} is the top
of a mechanically unstable region rather than a surface. The locator takes the
state as a callable, so it holds no model; a density at which the solve does
not converge thins the scan rather than aborting it, and a parameter set with
no surface returns a status rather than raising.

\subsection{The Bodmer--Witten window}

The pair of numbers is a two-sided gate on a parameter set: three-flavour
$E/A < 930.4\;$MeV says strange quark matter is absolutely stable, and
two-flavour $E/A > 930.4\;$MeV says ordinary nuclei are not already decaying
into it. A set failing either is excluded. \textbf{Both facts are reported and
neither is asserted}, since whether a set lies in the window is a property of
the set; the same \texttt{below\_iron} flag reads in opposite directions on the
two arms. The content \emph{requested} and the content \emph{found} may differ
and both are returned: a set whose surface lies below the strange quark's
threshold returns $Y_S = 0$ from a three-flavour call, so the content is read
off $Y_S$.

\subsection{Where this model sits in the window}

\textbf{No point in the tier-3 priors clears the three-flavour arm, and the
obstruction is $B_{\rm eff}$ rather than the search.} Measured at $T = 0$ in
\texttt{beta\_eq\_neutrinoless} with \texttt{csc = True} over
$n_B \in [0.3, 2.0]\;$fm$^{-3}$ at 16 scan densities, on
$B_g^{1/4} = 120\;$MeV with $\bar g_\omega = 4$ and every other knob at its
shipped value except where the row says:

\begin{center}
\begin{tabular}{lllll}
\toprule
moved knob & $n_B$ [fm$^{-3}$] & $E/A$ [MeV] & $Y_S$ & pattern \\
\midrule
none & 1.2585 & 1333.05 & 1.0000 & CFL \\
$G_D = 2\times10^{-5}$, $q = 1$ & 1.1433 & 1387.60 & 0.5739 & unpaired \\
$g_s = 8$ & 1.6567 & 1415.62 & 1.0000 & CFL \\
$n_c = 0.3$; $n_c = \infty$; $\bar g_\omega = 12$; $p = 2$
 & \multicolumn{4}{l}{no $P = 0$ crossing} \\
\bottomrule
\end{tabular}
\end{center}

The deepest surface in the box is $1333.05\;$MeV against the $930.4\;$MeV of
iron. Colour superconductivity works and is not enough: at otherwise identical
parameters it takes $E/A$ from 1387.60 to $1333.05\;$MeV, a recovery of
$54.5\;$MeV, by locking to CFL --- $Y_S = 1$ exactly, which is what the locking
means.

One number carries the bound. Across the scanned points
%
\begin{equation}
E/A = 5.92\, B_{\rm eff}^{1/4} \qquad \text{to } 0.3\%,
\end{equation}
%
the ideal-bag relation with this model's own $B_{\rm eff}$, so
$E/A < 930.4\;$MeV needs $B_{\rm eff}^{1/4} < 157.2\;$MeV, or $163.6\;$MeV once
the CFL recovery is credited --- $B_{\rm eff} < 93.2\;$MeV/fm$^3$. But
$B_{\rm eff} = B_g + B_\chi$ and \textbf{the chiral part is the larger part and
is not a tier-3 knob}: $B_\chi$ follows from the tier-1 vacuum data through
$C_0$, dominated by the strange terms $\epsilon_\zeta \zeta_0$ and
$\lambda_\zeta(\zeta_0^2 - v_\zeta^2)^2/4$, and the only tier-3 parameter
touching it is $m_\sigma$, through the light sector alone. Over the whole prior
box
%
\begin{align}
B_{\rm eff}^{1/4} &\ge 231.90\;\text{MeV} \quad (376.4\;\text{MeV/fm}^3)
 &&\text{at } B_g^{1/4} = 120,\ m_\sigma = 450\;\text{MeV}, \\
B_\chi^{1/4} &= 227.63\;\text{MeV} \quad (349.4\;\text{MeV/fm}^3)
 &&\text{at } B_g \to 0,\ m_\sigma = 450\;\text{MeV},
\end{align}
%
so switching the glue bag off entirely still leaves the field energy a factor
four above what absolute stability needs. This model describes hybrid-star
quark matter, not a self-bound strange star, and a crustless strange-quark-star
sequence is not reachable by a choice of \texttt{Parameters} in it.

\textbf{The two-flavour arm has no deconfined solution at these densities at
all.} With \texttt{two\_flavour = True} the model converges at 0 of 20 scan
densities over $[0.3, 2.2]\;$fm$^{-3}$, paired and unpaired alike, at both
$B_g^{1/4} = 120\;$MeV and the shipped set: with the strange Fermi sea emptied
nothing opens the dielectric against the vacuum over that range, so there is no
branch to carry a surface. The arm reports that as a status and fails nothing.

\subsection{The \texttt{two\_flavour} flag}

Two-flavour quark matter is \texttt{beta\_eq\_neutrinoless} with the strange
sector switched off, which is what it physically is, and not
\texttt{fixed\_YC\_YS} at $Y_S = 0$. The distinction is not stylistic: with no
populated species carrying strangeness, $n_S = 0$ holds over a whole range of
$\mu_S$, so the row $n_S = Y_S n_B$ leaves $\mu_S$ undetermined and its
Jacobian column null, and the solve then converges on round-off. Switching the
sector off removes the flavour from the unknown vector instead. This model
therefore \textbf{raises} on \texttt{fixed\_YC\_YS} with the flag on.

The flag defaults to \texttt{False}, the $u,d,s$ matter this model has always
solved, so no existing number moves. It is named for the restriction rather
than for the sector because a sector-voiced \texttt{strange\_quarks} would have
had to default \texttt{True} to preserve that. With the flag on, $n_s = 0$,
$Y_S = 0$ and $\mu_S = 0$ identically; $\mu_B = \mu_u + 2\mu_d$ and
$\mu_C = \mu_u - \mu_d$ are unchanged, neither reading $\mu_s$, so
Eq.~\eqref{eq:gibbsidentity} collapses to $E/A = \mu_B$ with both strange terms
vanishing; and $\mu_s$ is reported as the weak-equilibrium value $\mu_d$, the
relation $s \leftrightarrow d$ still holding with nothing populated at it.

\subsection{What stays when the strange flavour goes}

\textbf{The strange condensate stays; only the strange Fermi sea is emptied.}
The three strange colour-flavour modes contribute nothing to the medium --- no
density, no scalar density, no pressure, energy or entropy --- while the
strange scalar field is still solved from its own field equation and still
feeds the light-quark masses. That is the physics of two-flavour quark matter:
the strange Fermi sea is empty, the strange condensate of the QCD vacuum is
not. Removing the strange field from the equations instead would move the
light-quark masses and the subtracted vacuum constant, changing the model
rather than the flavour content asked of it.

A diquark containing an $s$ quark is likewise not a state two-flavour matter
has, so $\Delta_1$ ($d$--$s$) and $\Delta_2$ ($u$--$s$) have nothing to pair:
the patterns carrying them leave the default enumeration and an explicitly
requested one is refused. \texttt{unpaired} and \texttt{2SC}, the $u$--$d$
condensate, survive.


\bibliographystyle{unsrt}
\bibliography{../../docs/eos}

\end{document}
